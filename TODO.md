# Value Logic Project: Context and TODO

Last updated: 2026-07-14

## Resume here

This file is the project control document. On a fresh chat, read this file first, then complete **exactly the first unchecked work item**—a numbered task or a named checkpoint—unless the user explicitly selects a different item. One task or checkpoint is the intended scope of one user prompt.

When finishing a task or checkpoint:

1. mark it `[x]`;
2. add its completion date and a short result note beneath it;
3. link every file it produced or materially changed;
4. record unresolved questions in the claim ledger or decision log named by the task;
5. stop before beginning the next work item.

When the workspace is a Git repository, make one local commit per completed task or checkpoint after validation, with the task/checkpoint ID in the message. Do not automatically push unless the user asks. This makes the repository history mirror the project's append-only provenance discipline.

### Artifact discipline

Each new research artifact should begin with a compact durable summary of definitions, decisions, results, and unresolved obligations. Aim for roughly 3,000 words of main-line exposition per task when the mathematics permits; place long proofs, exhaustive examples, or raw audits in clearly linked appendices rather than deleting qualifications. Task 13 must establish a central notation/glossary index so later consolidation does not silently change meanings.

### Checkpoint protocol

Checkpoints are roadmap-review tasks, not ceremonial status updates. At each checkpoint:

1. read the artifacts produced since the previous checkpoint, plus this TODO, the project specification, claim ledger, and decision log;
2. compare the accumulated results, counterexamples, literature findings, and implementation constraints with the project question and required final artifacts;
3. re-evaluate **every still-unfinished work item after the checkpoint** for necessity, order, dependencies, scope, and feasibility;
4. split, merge, reorder, add, narrow, or remove future work when justified, while preserving completed artifacts and their historical completion notes;
5. update stale task references and the `Next task` pointer if the roadmap changes;
6. create the checkpoint note named by the checkpoint, recording evidence considered, course corrections, rejected changes, new risks, and the revised pending roadmap;
7. if no correction is needed, record why the current roadmap remains appropriate;
8. stop without beginning the newly selected next task.

Checkpoint edits apply prospectively. They may add follow-up work for a completed result, but they do not silently mark completed work undone or rewrite its history. Prefer stable checkpoint labels and suffixed new tasks over renumbering tasks that are already referenced elsewhere.

**Next task: Task 21 — Run and analyze the experiment.**

## Project question

Physics develops through sequences of theories. A successor can show that a predecessor is not universally correct while retaining it as a useful approximation on a restricted domain. The sequence may be indefinitely extendable, and a bounded agent may never be entitled to conclude that its present theory is final.

The project asks:

> What kind of logic can represent theory use under indefinite, domain-sensitive supersession without requiring an agent to claim possession of final truth, and how can that logic be represented or learned by a basic ReLU multilayer perceptron, preferably with cross-entropy or a closely related loss?

## Four opening design questions

These questions should appear near the beginning of both final artifacts, before the mature formalism is presented. The reader should see which parts of the formal object are forced by the motivating problem and which parts are later modeling choices.

### 1. Does the main question itself yield `Pi(M,D,epsilon)`?

Not in its exact final form. The main question immediately motivates two indices:

- `M`, because the object under evaluation is a model or theory;
- `D`, because the motivating phenomenon is precisely that a superseded theory can remain usable on a restricted domain.

The pragmatic background then motivates a performance comparison: a bounded agent may be unable to certify that `M` is true, while still being able to compare `M` with observations or with outcomes on a background task. To turn that comparison into a yes/no permission to rely on `M`, one introduces a discrepancy or loss functional `L` and a tolerated upper bound `epsilon`:

```text
Pi(M; D,L,epsilon)  iff  Risk_L(M on D) <= epsilon.
```

Thus `epsilon` is not “the probability that the theory is false.” It is a task- and loss-relative tolerance: the largest measured or estimated discrepancy the application will treat as negligible. The compressed notation `Pi(M,D,epsilon)` suppresses `L` and other indices only after those are fixed by context.

The exact scalar form is not forced. A vector of losses and tolerances, a partial order, a regret bound, or a calibrated chance constraint may be more appropriate. Sampling uncertainty also means that observed risk is an estimate, so a fuller form may require a confidence/calibration level. The paper must present the derivation in stages:

```text
indefinite succession
    -> domain-relative use
    -> observable/task-relative performance
    -> loss or discrepancy
    -> tolerance epsilon
    -> uncertainty/calibration and agent/stage indices.
```

### 2. What is `Pi(M,D,epsilon)` represented as, and where is its information?

The project must not identify these distinct objects without argument:

1. the structured logical judgment `Pi(M;D,L,epsilon,...)`;
2. vector encodings of `M`, cases, and/or `D`;
3. a predicted statistic or uncertainty region;
4. a signed point-estimate margin;
5. a conservative certificate-relative margin and exact atom state; and
6. the rectified ReLU output.

A candidate loss-first construction must distinguish a point prediction from an
accepted conservative margin:

```text
s_pred_i(x,epsilon) = epsilon - J_hat(M_i,x),
z_pred_i(x,epsilon) = ReLU(s_pred_i(x,epsilon)),

m_support_i(x,epsilon) = epsilon - upper(U_i_safe(x)),
z_support_i(x,epsilon) = ReLU(m_support_i(x,epsilon)/sigma_i).
```

The interpretation is a ladder, not an intrinsic property of ReLU:

1. For arbitrary `s`, `ReLU(s)>0` says only that this preactivation is positive.
2. For the named learned score `s_pred`, `z_pred>0` means the estimator predicts
   positive slack. It is not evidence that the task threshold is actually met.
3. If `U_i_safe` is bound to separately accepted evidence, `z_support>0` means
   strict certificate-relative surplus for this one atom, scope, and mode. It
   is not truth, probability, global superiority, or a complete use license.
4. The full grant still requires exact evidence validity and polarity, `WF`,
   the inclusive boundary state, and every atom in the selected profile.

At supported equality, `m_support=0` and therefore `z_support=0`; the same zero
also represents negative, open, refuted, invalid, and missing cases. Biases and
bypass paths may remain active. Exact symbolic state and active-set masking—not
rectification alone—supply authorization and quarantine. A positive rectified
value may be passed to a named downstream consumer only after this separation
has been made explicit.

The vectors should, where possible, be derived backward from operational data rather than stipulated. A model-by-case loss matrix or tensor can be factorized so that model embeddings encode capability profiles and case/domain embeddings encode demand profiles. Physics can add constrained coordinates from dimensionless groups and perturbative correction sizes. These encodings are generally identifiable only up to transformations that preserve predicted loss, so “the network learned the ontology” requires additional evidence.

### 3. In what sense do domains form an atlas and get stitched together?

There are two candidate atlases, and the paper must not conflate them:

- **Scientific-model atlas:** separately meaningful models `M_i` have licensed regions `D_i(epsilon)`. These regions may overlap or leave gaps. Stitching requires an explicit router and bridge conditions that bound predictive or decision disagreement on overlaps. Hard routing can be discontinuous; soft routing or matched bridge models can preserve continuity. Abstention is required where no chart is licensed.
- **Intrinsic ReLU atlas:** a fixed ReLU network partitions its input space by activation pattern into finitely many polyhedral regions, and computes an affine map on each region. Because the network is continuous, adjacent affine pieces agree on their shared boundary, although their Jacobians can differ.

Fable's strongest observation is valid at the structural level: the second atlas is native to the ReLU function class, and depth permits much richer piecewise-linear region maps than a single half-space test. But this does **not** by itself show that the regions correspond to Newtonian, relativistic, or other scientific domains. The central neural question is whether a loss-grounded architecture and training process can align the intrinsic activation atlas with the scientific-model atlas closely enough to implement the intended license logic.

### 4. Where does one model end when modeling choices are themselves models?

The word “model” must be understood relative to an evaluation interface, not as an assertion of ontological atomicity. A single evaluated use plan may be a finite composition of choices:

```text
object/idealization -> frame/coordinates -> formulation -> solver -> prediction/action.
```

Newtonian mechanics with different object boundaries, reference frames, Hamiltonian or Lagrangian formulations, numerical solvers, and stopping rules can therefore yield distinct use plans even when their final predictions coincide. If their time, memory, robustness, or interpretability costs differ, they can receive different vector losses, constraint diagnostics, or comparative status. A license for the composite plan does not automatically license every component in isolation, and component licenses do not automatically license the composition without a typed error/cost propagation rule.

The loss criterion and a model of loss must also remain distinct. `L_q` states what the request treats as loss or value; an approximate procedure that predicts downstream outcomes, computation cost, regret, or `L_q` itself is another evaluated component or evidence-producing procedure. That evaluator can receive a higher-order license under a new request, and alternative loss specifications can be compared only under a further stated context. This permits finite, well-founded nesting without pretending that the regress ends in an unmodeled universal loss. Genuine cycles—a request whose own grant changes the loss model used to grant it—require explicit fixed-point or iterative semantics and are not licensed by ordinary finite composition.

The author-designated ambitious target is stronger than finite reification. A certified component should return a typed computational payload, a quantitative grade or bound, and checkable evidence/provenance. Plan constructors should transform all three, so structural induction yields a certificate-carrying composite computation and a separately checked license-lifting rule. This is analogous to proof-carrying programs or Curry–Howard only for formal derivations and checked proof terms; empirical confidence, calibration, or neural margins remain mode-relative certificates rather than unconditional proofs of world facts.

A ReLU channel may have a genuine dual use under a declared semantics. If channel `i` is tied to a named hypothesis and `m_i(x)` is a commensurate, certificate-valid signed adequacy margin—or a certified approximation with sufficient boundary separation—then `ReLU(m_i(x))` is both positive certificate-relative surplus and a nonnegative feature available downstream. It is not automatically a posterior probability, world-level adequacy fact, or complete model output. Inclusive boundary support, open/refuted status, missingness, and negative magnitude require the signed or additional channels; equal adequacy margins can hide different predictions, costs, or traces. Task 17's [`ml/03_representation_theorems.md`](ml/03_representation_theorems.md) gives the minimal named state-plus-surplus construction and its scalar, boundary, approximation, and scale obstructions.

An implementation of value logic can itself be reified as a plan and assessed under an independently fixed meta-context. This finite system-level license must be grounded in external or lower-ranked evidence and must state whether it concerns decoder fidelity, derivation checking, calibration, routed risk, resources, or world soundness. The system's own self-endorsement is not sufficient evidence. Task 14C distinguishes object-model results, internal value-logic derivations, external metatheorems, and empirical certificate judgments, and proves grounded-provenance and stratified self-evaluation theorems for the finite DAG/rank fragment.

This compositional recursion is distinct from the later recursive-judgment hypothesis in Tasks 22–22A. The former asks how models, evaluators, and losses are typed and composed; the latter asks whether repeated judgment reveals latent task structure.

## Required final artifacts

1. `paper.md`: a rigorous, LaTeX-heavy, GitHub-Gist-compatible Markdown paper.
2. `substack_post.txt`: a Substack-compatible version with no Markdown syntax, minimal equations/LaTeX, and the same argument in readable prose.

Research notes, code, data, figures, and verification scripts may be added as supporting artifacts, but these two are the required deliverables.

## Cross-project interpretability goal

The parallel repository [`TristanMiano/policy_value_isomorph`](https://github.com/TristanMiano/policy_value_isomorph) is a concrete companion experiment. Its current scope is deliberately narrower than a universal policy–value isomorphism: it freezes a policy in small deterministic games, estimates policy-induced `V^pi(s)` or `Q^pi(s,a)` from rollouts, trains scalar value models, and measures how well argmax/argmin decisions reconstruct the original policy.

As currently implemented, this is primarily **behavioral reconstruction**. The policy MLP and value/Q MLPs receive raw state encodings and are trained separately. The recovered value network need not share the policy network's hidden representation or causal mechanism. Consequently, high action agreement establishes a faithful surrogate on the evaluated distribution, not yet mechanistic interpretability.

One late-stage goal of this project is to determine whether the license logic supplies a representation that improves transparency into such a policy network. The desired bridge is:

```text
opaque policy behavior
    -> rollout- or preference-induced V/Q constraints
    -> loss-grounded adequacy/license representation
    -> domains/charts, margins, bridges, and decision traces
    -> a transparent surrogate or probe whose limits are explicit.
```

Transparency must be evaluated at several strengths:

1. **behavioral fidelity:** reconstructed actions and outcome distributions match the policy;
2. **value fidelity:** the recovered `V`/`Q` predicts rollout returns and action rankings;
3. **domain fidelity:** the transparent logic states where the reconstruction is licensed and abstains outside supported regions;
4. **representational alignment:** recovered charts/features correspond stably to policy hidden states or directions;
5. **causal faithfulness:** interventions on the proposed value/license features change policy behavior as predicted;
6. **human inspectability:** the representation compresses decisions into understandable margins, comparisons, local rules, or chart transitions.

The paper must not collapse these grades into a single word, “interpretability.” A simple surrogate may satisfy the first two while failing the last three.

The universal target also needs a qualification exposed by the companion code: a policy alone does not determine the usual return-based `V^pi`. Reconstruction requires an environment/transition model, a return or preference convention, a sufficiently Markov state, and a distribution over evaluated states or histories. The companion repository supplies these through game rules, terminal utilities, perspective conventions, and rollout sampling. Task 22 will state the strongest general reconstruction problem that survives these dependencies.

## Current working thesis (provisional)

The most promising object is not a replacement truth value but a **provisional, indexed license**. A bounded agent at stage `t` licenses a model `M` only relative to a domain `D`, loss functional `L`, tolerance `epsilon`, evidential confidence/calibration level, computational cost or budget, and the model library actually searched. A license can be defeated by new observations or by discovery of a model that dominates it. A superseded model can remain licensed on subdomains or because it remains Pareto-optimal in accuracy, coverage, cost, and robustness.

A candidate neural realization is loss-first:

```text
s_i(x, epsilon) = log(epsilon) - predicted_log_loss(M_i, x)
license_margin_i(x, epsilon) = ReLU(s_i(x, epsilon)).
```

An accepted, boundary-aware certificate can make a positive normalized margin
available as adequacy surplus for model `M_i` on case `x`; the margin does not
license itself, and zero does not assert negation. The signed preactivation,
exact atom state, and evidence handles remain available whenever boundary,
degree, or cause matters. A domain-level license must aggregate or certify
claims over a set or distribution `D`. Deep ReLU networks induce continuous
piecewise-affine regions, providing one possible finite atlas of local
computations; a separate argument is needed to align those regions with a
scientific-model atlas. Task 17 proves the scoped CPWL representation and seam
limits, while semantic alignment remains an empirical hypothesis.

When the score coordinates have a named, certificate-valid semantics and common scale—or certified approximation envelopes with boundary separation—the positive-margin vector can also be the input to later computation. For example, hypothesis-indexed channels for `flower`, `dog`, `cat`, `person`, and similar classes may expose their positive certificate-relative adequacy surpluses, which later layers can compare or combine. This score-as-content interpretation is conditional: arbitrary hidden features are not adequacy claims, and general model payloads still require separate content channels plus evidence that the joint representation is sufficient.

The intended logic must keep three levels distinct:

- **object level:** a model's predictions or rules within a stated regime;
- **license level:** an agent's defeasible judgment that a model is adequate for a domain and purpose;
- **limit/meta level:** external claims about eventual stability, finality, or convergence, which a finite-stage agent generally cannot certify.

## Non-negotiable distinctions

The following distinctions must remain explicit throughout the work:

- truth versus empirical or decision-theoretic adequacy;
- a single case `x` versus a domain `D` (a set, distribution, or task family);
- a theory, a fitted model, a representation/frame, and a model-selection policy;
- a policy alone versus a policy coupled to dynamics, returns/preferences, and a state distribution;
- predictive loss versus decision regret versus computational cost;
- a Boolean license versus its real-valued safety margin;
- a signed adequacy score versus the information-erasing zero branch of ReLU;
- a computational payload versus its quantitative grade/bound versus the proof or empirical certificate supporting that grade;
- a dual-use adequacy feature under declared joint-sufficiency/scale conditions versus an arbitrary hidden activation given a post-hoc semantic label;
- a logical judgment versus its vector encoding, scalar preactivation, and gated output;
- “no better retrieved model was found” versus “no better model exists”;
- retention of past records versus monotonic retention of current beliefs;
- representability by a ReLU network versus learnability by gradient descent;
- architecture-neutral semantic compatibility versus ReLU-specific representation results versus empirical architecture choice;
- behavioral surrogate fidelity versus representational alignment and causal/mechanistic explanation;
- the geometry of a network's linear regions versus their interpretation as scientific regimes;
- a scientific-model atlas/router versus the intrinsic activation-region atlas of one ReLU function;
- internal certification/provability versus external eventual stability;
- an object-model result versus an internal value-logic derivation versus a metatheorem about the calculus versus an empirical certificate judgment;
- stratified system-level assessment versus circular self-authorization or an unrestricted reflection principle;
- ordinal preference, scalar utility, and multi-objective/Pareto comparison.

## Source map and inherited ideas

All six pre-existing files were read before this TODO was created.

### `llm_convos/chatgpt.txt`

The most operational recent line of thought. It develops:

- `Adequate(T,D,L,epsilon,alpha)` as a loss-relative, confidence-sensitive predicate;
- nested adequacy for model-building choices, frames, units, idealizations, parameters, solvers, and selection policies;
- error budgets and vertical versus horizontal composition;
- ReLU activations as positive adequacy margins;
- model and domain encodings, bilinear compatibility, tiled adequacy regions, and routing;
- a later correction from hand-labeled vectors to a record-derived loss matrix;
- log dimensionless coordinates, omitted-correction profiles, tied `log(epsilon)` dependence, and abstention when no model is licensed.

### `llm_convos/claude.txt`

The most ambitious formal line. It develops:

- agent-, time-, library-, and search-budget-indexed licenses;
- empirical defeat versus discovery of a dominator;
- Pareto retention of old theories;
- atlases of local theories with seams/bridges;
- a separation between finite internal certificates and external stability;
- Löbian claims for stage-local certification and anti-Löbian claims for open-ended refinement;
- a proposed identification of ReLU linear regions with affine atlas charts;
- depth as nested licensing, tropical and many-valued interpretations, and training as supersession;
- loss-tensor factorization, Buckingham-pi/log coordinates, perturbative correction profiles, calibrated routing, and abstention.

The Fable discussion makes a useful structural case: activation patterns index polyhedral regions; a ReLU network is affine on each region; continuity supplies exact agreement of the network's adjacent affine pieces on shared facets; and depth constructs bounded, disconnected, or combinatorially rich regions unavailable to a single affine threshold. It also proposes interpreting a neuron as an incoming license test plus outgoing licensed content and emphasizes that a positive ReLU value can represent unused tolerance or margin. The later audit corrects the unqualified form of that intuition: positivity inherits semantics only from a deliberately constructed and, where authorization is at issue, externally accepted conservative margin. An arbitrary positive hidden unit is merely a positive preactivation, while even certificate-relative surplus settles only one named atom rather than the full profile license. In addition, an intrinsic activation-region decomposition is not automatically a decomposition into human-interpretable scientific models, and separately implemented physical models usually require approximate overlap conditions, routing, or bridge models rather than the exact continuity inherited by the affine pieces of one network.

Many theorem names, exact statements, and citations in this conversation have not yet been verified.

### `posts/utility_preference_logic_nn.md`

A survey-style draft connecting preference logic, planning, differentiable logic, score semantics, open-set/Heyting semantics, ReLU min/max constructions, and loss-induced preference orders. It contains a useful bibliography and several strong technical claims that require source and proof audits before reuse.

### `posts/judgement_value_fact_recovery.txt`

Argues that recursively evaluating judges pressures them to recover stable factual structure; introduces “excellence centroids” and the “latent geometry of excellence”; treats truth as a pragmatic limit of increasingly successful theories; and gives a cautious policy-to-value reconstruction argument.

### `posts/utility_all_you_need.txt`

Develops belief/value duality, task delineation `T`, judgment `J`, the informal claim `T = J^2`, recursive valuation, and arguments against simple value-fragility pictures. These are motivating intuitions, not established results.

### `posts/condensed-response-hidden-complexity.txt`

States the strongest policy–utility “isomorphism” and value-complexity claims. These require substantial qualification: occupancy or visitation counts are not generally a utility function, policies do not uniquely identify rewards, and utility-to-policy conversion requires an environment model and decision rule.

### Parallel repository: `TristanMiano/policy_value_isomorph`

The `main` branch was inspected through GitHub on 2026-07-10. The repository has progressed beyond its original tabular slice:

- a heuristic tic-tac-toe policy and trainable one-hidden-layer policy MLP;
- rollout generation for fixed-perspective `V^pi(s)` targets;
- separate state-value and action-value MLP regressors;
- successor-value and direct-Q policy recovery;
- action agreement, top-k, W/D/L, and value-calibration utilities;
- symmetry, sampling, telemetry, checkpointing, tests, and a partial Connect Four port;
- an active plan to migrate manual list-based/tanh training to PyTorch while preserving behavior and artifacts.

The policy model is presently trained by masked cross-entropy imitation; `V` and `Q` are trained by MSE on rollout-derived terminal returns. Tests establish basic loss reduction, sign conventions, legal actions, modest agreement/MAE thresholds, and recovery of simple forced moves. The current code does not inspect policy hidden activations, share a latent space between policy and value models, learn a ReLU atlas, or establish causal correspondence between recovered value features and the policy's internal computation. Those are opportunities for the cross-project interpretability task rather than existing results.

## Main research risks

1. **Relabeling risk:** calling loss minimization a “logic” without defining syntax, semantics, consequence, and proof/update rules.
2. **Conflation risk:** treating `x`, `D`, model descriptions, theory families, and neural embeddings as the same kind of object.
3. **Circularity risk:** hand-encoding “handles strong gravity” and then claiming the embedding discovered adequacy.
4. **Scalarization risk:** hiding coverage, error, robustness, cost, and risk in one arbitrary number.
5. **Finality risk:** covertly reintroducing final truth through a stability, best-model, or complete-library operator.
6. **Neural overclaim risk:** CPWL representability does not show that SGD learns scientifically meaningful charts.
7. **Loss mismatch risk:** ordinary softmax cross-entropy forces a choice even when several models are adequate or none is; multi-label, ranking, selective, or calibration losses may be necessary.
8. **Citation risk:** the LLM conversations contain plausible but unverified attributions, theorem formulations, and complexity claims.
9. **Philosophical overclaim risk:** pragmatic adequacy can guide bounded belief without entailing that truth is identical to usefulness.
10. **Identifiability risk:** behavior and loss matrices generally underdetermine utilities, latent coordinates, mechanisms, and ontologies.
11. **Rectification risk:** `ReLU(s)=0` collapses every negative margin, so the rectified value alone cannot encode the kind or magnitude of inadequacy.
12. **Atlas-equivocation risk:** the CPWL regions of one neural function may be called an atlas in a precise structural sense, but that does not identify them with the domains of a library of scientific theories.
13. **Surrogate-interpretability risk:** an independently trained value network can reproduce actions without identifying the internal computation or reasons implemented by the original policy network.
14. **Proof/evidence conflation risk:** a quantitative bound, learned confidence, accepted empirical certificate, formal proof term, and target-world fact may be treated as interchangeable.
15. **Dual-use activation risk:** a useful downstream feature may be post-hoc relabeled as adequacy without a named hypothesis, calibrated common scale, joint-sufficiency result, or boundary/negative-status channel.
16. **Self-authorization risk:** a value-logic implementation may cite its own grant as its only evidence, hiding an ungrounded cycle or unrestricted reflection principle.

## Definition of done

The project is complete only when:

- the formal language, semantics, consequence relation, and update dynamics are explicit;
- object-model results, internal value-logic derivations, metatheorems, and empirical certificate judgments are separated;
- finite recursive plans have a proved/countermodeled payload-grade-certificate composition rule, and system-level assessment is grounded/stratified rather than circular;
- at least three paper-carrying results from distinct stability, update/profile/diagnostic, proof-carrying/system-assessment, transport/routing, or neural representation clusters are proved or replaced by precise countertheorems;
- the precise ReLU representation claim, including any dual-use activation conditions and obstructions, is stated and proved or cited correctly;
- representability, trainability, and empirical interpretation are separated;
- a reproducible toy experiment tests the proposed encoding and at least one failure case;
- the connection to policy-to-value reconstruction is evaluated with separate behavioral, domain, representational, causal, and inspectability criteria;
- cross-entropy is either justified in a precise role or replaced with a documented alternative;
- all technical citations are checked against primary sources;
- counterexamples and limitations are presented alongside positive results;
- both final artifacts agree on claims while matching their respective formatting constraints.

## Claim-status discipline

The project will not assign early truth-likelihood labels merely because a claim is ambitious, unfamiliar, positively framed, or negatively framed. The claim ledger separates three dimensions:

1. **Role:** motivation, definition proposal, mathematical conjecture, empirical hypothesis, interpretive thesis, or known result to verify.
2. **Testability:** formally falsifiable by proof/countermodel, empirically falsifiable after operationalization, addressed by an identifiable literature, currently underspecified but potentially testable, or likely unfalsifiable in its present form.
3. **Evidence state:** uninvestigated, evidence located but not checked, test proposed, test performed and supportive within scope, test performed and falsifying within scope, or inconclusive.

“Likely false,” “refuted,” and comparable negative dispositions may be used only after one of the following:

- a sufficiently similar, precisely stated claim is found in reliable research literature and the relevant falsification/result is verified; or
- this project first gives the claim an explicit falsifiable formal or empirical reading and then supplies a valid proof, countermodel, counterexample, or experiment against it.

The same rule applies to negative claims. Statements such as “no policy can reveal value,” “no ReLU representation can align with scientific domains,” or “the theory sequence never terminates” do not receive a lower evidential burden merely because they deny something. Positive and negative formulations must expose their quantifiers, scope, and possible counterevidence symmetrically.

Whenever a claim receives a falsified/refuted disposition, its ledger row must also state the result's **project impact**: whether it threatens the central goal or only narrows a mechanism/theorem; which definitions, proofs, experiments, neural outputs, roadmap items, and final-publication claims must change; what narrower claim survives; and whether a repair task or checkpoint action is required. A falsification is not fully propagated until this consequence analysis is recorded.

## Numbered task queue

- [x] **Task 0 — Inventory the repository and create the resumable TODO.**

  Completed 2026-07-10. Read all six pre-existing files, synthesized the project question, recorded the source map, identified high-risk inherited claims, and created this control document.

- [x] **Task 1 — Build the inherited-claim ledger.**

  Create `notes/claim_ledger.md`. Extract the major mathematical, logical, philosophical, and ML claims from the six local source files and the inspected `policy_value_isomorph` companion repository. For each claim, record: source; exact quotation location or paraphrase; precise restatement with quantifiers and scope; role; testability class; current evidence state; dependencies; a candidate proof, countermodel, experiment, or literature comparison; and what outcome would count as support, falsification, or inconclusive evidence. Initial testability labels should include `formally falsifiable`, `empirically falsifiable after operationalization`, `literature-addressable`, `underspecified but potentially testable`, and `likely unfalsifiable as stated`. Do not label claims likely false or true, and do not settle them in this task.

  Completed 2026-07-10. Created [`notes/claim_ledger.md`](notes/claim_ledger.md) with 65 scoped claims across theory succession, recursive judgment, policy–value correspondence, preference logic, finite-stage licenses, ReLU semantics/atlases, and the companion implementation. Each row records role, testability, evidence state, and explicit support/falsification/inconclusive conditions. Added cross-claim dependencies, immediate formal sanity checks, urgent operationalization targets, and handoffs to later tasks. No truth-likelihood or final evidence dispositions were assigned.

- [x] **Task 2 — Derive `Pi(M,D,epsilon)` and explain `epsilon`.**

  Create `notes/motivation_pi_epsilon.md`. Begin only from the main theory-succession question and derive which arguments of `Pi` are forced, which are pragmatically motivated, and which are optional refinements. Explain the progression from observations or background tasks to discrepancy, loss, domain risk, tolerance, calibration, and decision regret. Determine when a scalar `epsilon` is justified and when a vector tolerance or partial order is required. This note is intended to become the opening conceptual derivation in both final artifacts.

  Completed 2026-07-10. Created [`notes/motivation_pi_epsilon.md`](notes/motivation_pi_epsilon.md). Derived `M` and `D` directly from domain-sensitive theory succession, then derived loss, domain risk, tolerance, calibration, and library-relative comparison as separate pragmatic steps. Added the fallback/status-quo route `epsilon_B(D)=J(B,D)-Delta`, giving ReLU a positive-surplus interpretation, while proving why baseline improvement must remain distinct from hard adequacy. Recommended factoring `Pi` into certified adequacy, improvement over fallback, and admissibility among retrieved alternatives. Recorded scalar/vector conditions, elementary monotonicity and retention results, the inclusive-threshold/ReLU boundary issue, worked examples, and a draft opening argument. Added claims A08–A09 to the claim ledger.

- [x] **Task 3 — Separate the representations and locate their information content.**

  Create `notes/representation_layers.md`. Distinguish the typed judgment `Pi`, encodings of models/cases/domains, the signed adequacy score, the ReLU output, and the downstream licensed content. Analyze exactly what information is preserved or lost at each map. Develop the loss-first/inverse-utility idea far enough to state the central encoding problem without yet selecting a final architecture.

  Completed 2026-07-10. Created [`notes/representation_layers.md`](notes/representation_layers.md). Separated the structured license, evidence, model/case/domain encodings, predicted risks, component margins, ReLU gates, routing, content, and actions. Defined lossless, task-sufficient, decision-sufficient, approximate, and interpretive preservation; compared model and domain encoding strategies; formulated a loss-tensor-based central encoding problem; separated negative loss from recovered utility; and audited information loss at every layer. Proved elementary results for ReLU non-injectivity, exact paired-ReLU signed encoding, calibration buffers, bilinear non-identifiability, and conjunction compression. Preserved provenance and signed component margins as transparency requirements without choosing a final architecture. Added claims F21–F26 to the claim ledger.

- [x] **Task 4 — Disambiguate and evaluate the two atlas claims.**

  Create `notes/atlas_questions.md`. Closely reconstruct Fable's argument about ReLU activation regions, affine charts, seams, exact bridges, and depth. Separately define the desired scientific-model atlas with overlapping domains, approximate bridges, gaps, routing, and abstention. State the alignment question connecting the two and list the claims that will later need proof or empirical evidence.

  Completed 2026-07-10. Created [`notes/atlas_questions.md`](notes/atlas_questions.md). Distinguished the scientific licensed model cover, the intrinsic ReLU activation complex, and the router's selection partition. Made overlapping, nested, coincident, and uncovered domains first-class; derived active-set cells for every licensed model subset; separated adequacy, dominance, selection, and archival retention; and defined exact, approximate, statistical, decision, asymptotic, translated, and unresolved bridge types. Showed conditionally how continuous overlap weights can blend commensurate predictions without exact seam equality, while warning against interpolating incompatible ontologies. Audited Fable's strongest atlas claims, formulated a many-to-one alignment target, and limited “remember every model/domain” to finite-stage capacity or a hybrid expandable registry. Added claims E11–E14 and F27–F30 to the claim ledger.

- [x] **Task 5 — Fix the project’s target and non-goals.**

  Create `notes/project_spec.md`. Use Tasks 2–4 to turn the working thesis into explicit design requirements. Decide what “logic,” “fit onto an MLP,” “theory,” “domain,” and “handles indefinite succession” must mean for this paper. State non-goals, including any metaphysical thesis about truth the formalism does not need.

  Completed 2026-07-10. Created [`notes/project_spec.md`](notes/project_spec.md). Fixed the target as a finite-stage logic of scoped, defeasible model-use licenses rather than a new truth value. Defined the project meanings of logic, theory/model, domain, neural “fit,” and indefinite succession; made representability, learnability, semantic alignment, and open-ended implementation separate success grades; converted Tasks 2–4 into logical, neural, empirical, and interpretability design requirements; and stated strong non-goals concerning truth, scientific realism, policy–value uniqueness, neuron/proposition identifications, SGD, and infinite fixed-network capacity. Used the posts only to establish the motivational order—action and comparison before certainty, recursive evaluation, and value/policy reconstruction—not as evidence for factual claims.

- [x] **Task 6 — Audit related fields and create a verified bibliography.**

  Create `notes/literature_map.md` and `references.bib`. Research primary sources for belief revision/nonmonotonic logic, formal learning and truth approximation, scientific structuralism and theory reduction, preference/decision logic, selective prediction and abstention, mixture-of-experts/model routing, ReLU CPWL geometry, differentiable logic, inverse reinforcement learning/reward identifiability, and provability/GL where relevant. Verify every inherited citation before including it.

  Completed 2026-07-10. Created [`notes/literature_map.md`](notes/literature_map.md) and [`references.bib`](references.bib), using primary publisher, proceedings, journal, author, or archival records. Mapped the distinct contributions and limits of eleven neighboring fields; identified selective prediction as the closest formal precedent for fallback-induced thresholds and abstention; verified finite CPWL/ReLU representation and qualified tropical and Łukasiewicz correspondences; separated formal-learning convergence from truthlikeness; constrained the policy/value goal with modern reward-identifiability results; and rejected automatic transfer of Löb/GL results to empirical licenses. Audited the inherited numbered bibliography, corrected the Neural LP authors/title and several publication versions, corrected the Seidenfeld–Schervish–Kadane DOI, and omitted unneeded or insufficiently verified entries rather than carrying them forward. Updated claims C07, D01–D03, and F14/F16/F17 in [`notes/claim_ledger.md`](notes/claim_ledger.md) with scoped `S1` or `I1` dispositions.

- [x] **Task 7 — Choose the core formal objects and notation.**

  Create `formalism/01_signature.md`. Define the sorts for agents, stages, records, cases, domains, model/theory objects, predictions, losses, tolerances, costs, libraries, searches, and provenance. Resolve the point/domain distinction and specify whether a domain is a set, probability distribution, task family, or a typed object supporting more than one of these.

  Completed 2026-07-11. Created [`formalism/01_signature.md`](formalism/01_signature.md). Fixed a many-sorted, task-typed finite-stage signature covering agents, stages, budgets, tasks, cases/histories, predictions/outcomes/actions, theory frameworks, versioned models, frames, domains, losses, risk aggregators, tolerances, uncertainty specifications, fallbacks, costs, records, libraries, searches, provenance, selectors, and bridges. Resolved a domain as a typed evaluation scope supporting carrier-set, distribution/sampler, task-family, condition, frame, and provenance views while keeping loss and risk aggregation outside domain identity. Introduced `EvalSpec(D,L,rho)` to type risk and scalar/vector/partially ordered tolerances; distinguished point domains from domain guarantees; made records, libraries, and search scope finite and versioned; stated full license-request well-formedness; and excluded global truth/finality/completeness predicates from the base object language. Deferred truth conditions and update rules to Tasks 8–10.

- [x] **Task 8 — Define finite-stage license semantics.**

  Create `formalism/02_license_semantics.md`. Give a fully typed definition of a license such as `Lic(a,t,b; M,D,L,epsilon,alpha,cost,trace)`. Define empirical adequacy, calibrated adequacy, the closure/search clause, abstention, and the two defeat modes (new evidence and a newly retrieved dominator). Include small examples.

  Completed 2026-07-11. Created [`formalism/02_license_semantics.md`](formalism/02_license_semantics.md). Defined target, empirical, and certificate-relative adequacy; introduced valid typed certificate objects with distinct deterministic, frequentist, Bayesian, conformal/selective, vector, and empirical-only readings; and gave the full factored use-license semantics. Added four operational outcomes—granted, refused by a countercertificate, withheld for insufficient/unresolved evidence, and undefined for ill-typed requests—so failure to certify is not treated as certified failure. Required every full license to name an explicit fallback, formalized conservative fallback improvement, hard constraints, relative versus declared-library search closure, library-relative admissibility, provenance, active sets, selection, mandatory fallback on gaps, coverage/selective risk, evidence rebuttal versus evidential lapse, and defeat by a newly retrieved dominator. Included five examples and six elementary semantic consequences. Recorded open dominance/update questions for Tasks 9–10.

- [x] **Task 9 — Define consequence and update rules.**

  Create `formalism/03_consequence_update.md`. Specify what follows from a set of licenses, which object-level rules remain local/classical, how contradictions are quarantined by domains, how record heredity differs from belief monotonicity, and how revision changes licenses, domains, tolerances, and provenance.

  Completed 2026-07-11. Created [`formalism/03_consequence_update.md`](formalism/03_consequence_update.md). Separated model-local object consequence, monotone typed consequence at a fixed finite stage, and current nonmonotonic consequence derived from active licenses and admissible evidence. Defined labeled reliance rather than truth detachment; component, case-application, active-set, selection, and fallback rules; risk-specific scope transport; and contradiction quarantine across model/version/frame/task/domain/purpose/stage contexts. Established raw-record heredity alongside non-hereditary admissible views, certificates, licenses, and selections. Added dependency-directed, stratified update transactions for evidence, corrections, retractions, models/searches, domains, tolerances, certificate conventions, fallbacks, costs/purposes, expiry, provenance, branches, and merges. Included seven examples and seven elementary results covering status functionality, nonmonotonic current licenses, non-explosion, conservative persistence, tolerance transport, merge behavior, and dominator defeat. Deferred exact dominance and distribution-aware splitting to Task 10.

- [x] **Task 10 — Formalize dominance, Pareto retention, and domain splitting.**

  Create `formalism/04_dominance_retention.md`. Define scalar and multi-objective dominance over loss, coverage, cost, and robustness. Prove a first retention result: a globally superseded model may remain undominated on a subdomain or on the full resource vector. State exact conditions for split, retain, or revoke.

  Completed 2026-07-11. Created [`formalism/04_dominance_retention.md`](formalism/04_dominance_retention.md). Defined eligible model–gate–fallback use plans, typed comparison profiles, task risk, coverage deficit, resource cost, and robustness coordinates; explicit monotone scalar dominance; product-preorder Pareto dominance; mode-correct joint comparison certificates; pairwise comparison statuses; target, undefeated, and certified operational frontiers; and four distinct retention layers. Formalized whole-domain versus cell/local dominance, prespecified relation cells, distribution-aware child carriers, conditional measures/samplers, expected/worst-case/coverage reconstruction, split readiness, and exact retain, supersede, split/withhold, adequacy-revoke, and archive-removal conditions. Proved eight results, including two retention theorems showing that global risk supersession does not imply subdomain or full-resource supersession, a conditional split-retention theorem, and a countermodel to total revocation from partial dominance. Scoped and adjudicated inherited claims A05, E06, and E07.

- [x] **Task 11 — Formalize scientific atlases, charts, seams, and bridges.**

  Create `formalism/05_atlas.md`. Turn the distinctions from Task 4 into definitions for local model charts and compatibility on overlaps without assuming exact equality. Distinguish a router over models from a single CPWL predictor. Provide Newtonian/relativistic and simpler toy examples while avoiding the claim that approximate physical theories literally form a differential-geometric atlas unless the axioms support it.

  Completed 2026-07-11. Created [`formalism/05_atlas.md`](formalism/05_atlas.md). Defined the licensed model cover as the weakest object and a scientific licensed atlas as a finite cover plus typed chart, overlap, bridge, seam, dominance/frontier, router, fallback, and provenance records; reserved literal differential-atlas status for structures satisfying explicit manifold, open-chart, invertible-transition, regularity, and cocycle axioms. Formalized archive/hard/use/frontier chart views, active and generalized status cells, certified gaps versus unknown scope, thick overlaps versus routing seams, directional purpose-specific bridge kinds, compatibility ledgers, bridge obligations by operation, error composition and cycle defects, exact gluing, approximate blending, safe routing, atlas revision, and alignment among the scientific cover, router partition, and ReLU activation complex. Added temperature, overlapping-regressor, gap, same-domain, unresolved-overlap, and narrowly scoped Newtonian/relativistic kinetic-energy examples. Proved nine elementary results, including active-set decomposition, exact gluing, blend continuity/deviation and bridge-composition bounds, gap fallback, a hard-routing/ReLU discontinuity counterexample, non-bijective activation/chart alignment, a low-speed kinetic-energy error bound, and exact signed-margin active-set recovery versus argmax insufficiency. Updated atlas-related claim-ledger dispositions without adding a new falsification.

- [x] **Checkpoint A — Reassess the roadmap after the finite-stage logic and atlas foundations.**

  Create `notes/checkpoints/A_finite_stage_foundations.md` and apply the checkpoint protocol. In particular, determine whether Tasks 8–11 produced a coherent enough finite-stage object to justify the planned open-endedness, core-calculus, and metatheory tasks; identify missing countermodels or literature dependencies; and revise Task 12 onward before further formalization.

  Completed 2026-07-11. Created [`notes/checkpoints/A_finite_stage_foundations.md`](notes/checkpoints/A_finite_stage_foundations.md). Determined that Tasks 7–11 share a coherent typed finite-stage spine but do not yet form one publication-ready calculus. Identified a subtle interface ambiguity: Task 8's full `Lic` includes comparative non-domination, while the retention/atlas results distinguish continued adequacy, possible use, comparison status, current selection, and archival memory after supersession. Added Task 11A to compare alternative judgment factorizations, choose terminology only after testing them in one integrated finite witness, and ensure the chosen design represents both continued old-model use and mere historical retention without conflation. Narrowed Task 12 to continuation-based open-endedness rather than speculative Löb/anti-Löb analogy; narrowed Tasks 13–14 to a small core and genuine proof/countermodel audit; refocused the ML and experiment tasks on four-way status, component factors, active sets, calibration, fallback, comparison-driven status changes, external registry pointers, scaling, and semantic/non-neural alignment distinctions; and made policy/value work optional to the core. Updated [`notes/project_spec.md`](notes/project_spec.md), [`notes/claim_ledger.md`](notes/claim_ledger.md), and a stale Task 8 handoff in [`formalism/02_license_semantics.md`](formalism/02_license_semantics.md). No new claim was marked `X1`.

- [x] **Task 11A — Resolve the finite-stage license/comparison interface and build an integrated witness model.**

  Create `formalism/05a_integration.md`. Treat the relation among `CertAdeq`, pragmatic usability/reliance warrant, finite comparison status, preferred use, `SelectedNow`, and archival retention as an open design question. Compare at least: (A) layered basic warrant plus separate preference; (B) one parameterized `Lic_P` whose requirement profile optionally includes comparison; and (C) a strong preferred-use `Lic` plus a separately named adequacy/usability judgment. Decide the unqualified meaning of `Lic` only after evaluating semantic clarity, continued use of older models under another purpose/profile/subdomain, update behavior, normative interpretation, and neural target complexity. In every design distinguish `UndefeatedRelative` from `CertifiedUndominated` and specify how unknown comparisons behave. Then construct one finite end-to-end witness with at least three model/use plans, an explicit fallback, overlap and gap, simultaneous usable/adequate models, comparison and selection changes without automatic evidence/archive erasure, both continued old-model use and mere retention cases, all four operational statuses, lapse versus rebuttal, model-addition supersession, a successful and withheld split, multiple bridge statuses, and complete provenance. Record the selected design, rejected alternatives, inconsistencies found, and exact repairs Tasks 12–14 must inherit.

  Completed 2026-07-11. Created [`formalism/05a_integration.md`](formalism/05a_integration.md). Compared layered, parameterized-profile, and strong-license interfaces without assuming a winner, then selected mandatory profile-indexed licensing `Lic_P` because it subsumes the other designs as named profiles, supports a provisional literal-inclusion profile order, and lets selectors declare their required authorization level. Defined finite required/report-only atoms; `P_rely`, `P_pref-rel`, and `P_pref-cert`; four-way atom/profile assessment; mandatory profile references; relative-undefeated versus certified-undominated behavior under unknown comparisons; profile-local updates; selector-required profiles; and profile-indexed current/stability views. Built a four-plan, three-stage witness with explicit fallback, overlap, gap, all four statuses, simultaneous usability, continued old-model use on an edge subdomain, usable-but-unselected and archive-only retention, model-addition supersession, unknown comparison, successful and withheld splits, five bridge statuses, lapse, rebuttal, and complete provenance. Proved nine structural results and mapped repairs into Tasks 8–14. The later external audit repaired one comparison-scope certificate gap and clarified E05: universal closure is a superseded design default (`D1`), while the narrower claim that closure is forced is falsified (`X1`); the profile-indexed replacement and project impact remain supported.

- [x] **Unscheduled Checkpoint A1 — Adjudicate the Fable 5 external audit.**

  Read [`llm_convos/claude_audit_2026-07-11.md`](llm_convos/claude_audit_2026-07-11.md) as an independent audit of Tasks 0–11A and apply the checkpoint protocol without beginning the next formal task. Verify each correction against the source files and primary literature; repair accepted local defects; record rejected or deferred recommendations; and revise the pending roadmap where the theorem deficit, executable-semantics risk, literature gaps, motivation debt, or corpus-management risks warrant a change.

  Completed 2026-07-11 and amended 2026-07-12. Created and extended [`notes/checkpoints/A1_external_audit_response.md`](notes/checkpoints/A1_external_audit_response.md). The first pass accepted and repaired C1–C10 with one bibliographic correction to the audit itself; introduced explicit world/stage semantics, substantive versus stage-bound request fields, typed action-authorizing roles, overlap-scope witness certificates, transparent joint-score certificates, `D1` design dispositions, and top-of-file Task 11A interface notices. It corrected F14's attribution and added Tasks 11B, 12A, and 22A. The second pass converted the theorem-light criticism into a paper-carrying theorem spine, reduced the planned mathematical core from 28 nominal sorts to three principal carriers plus semantic indices, replaced flat reason codes with indexed witnesses over a `WF + K_3` kernel, added Task 14A for transport/routing bounds, and strengthened Tasks 11B–19. At checkpoint completion Task 11B was next; it is now complete.

- [x] **Task 11B — Mechanize and test the integrated finite witness.**

  Create a small standard-library Python reference implementation under `verification/` plus tests and a reproducible command. Implement the compact kernel rather than one class per Task 7 sort: use plans `E`, evaluation contexts `Q`, finite epistemic states `S`, finite profiles, a separate `WF` check, meaningful atom values `K_3={refuted,open,supported}`, finite-meet aggregation, and lossless indexed diagnostics containing support/counterwitnesses, obstacles, and provenance. Derive the four public outcomes from `WF + K_3`; do not create a closed `ReasonCode` enum. Encode the three-stage Task 11A witness as fixtures over this kernel, including comparison eligibility on the exact evaluation scope, profile-local update, lapse (`+ -> ?`) versus rebuttal (`+ -> -`), selectors, and provenance links. Assert every table and transition in `formalism/05a_integration.md`, including the legacy-label rendering and the difference between marginal score intervals and tighter joint certificates. Test that well-formed atoms never return `Undefined`, empty active sets are selector-level outcomes rather than atom reasons, and safety/provenance projections are lossless. Add a lightweight repository check for broken local Markdown links and run both checks in a minimal GitHub Actions workflow. This is semantic verification infrastructure, not the neural experiment or proof-assistant formalization.

  Completed 2026-07-12. Added the standard-library [`verification/`](verification/) reference implementation: [`kernel.py`](verification/kernel.py) separates `WF` from meaningful `K_3` atoms and derives all four public outcomes, while [`witness.py`](verification/witness.py) encodes the complete three-stage Task 11A structure without a closed reason-code type. Added 28 executable assertions covering the witness tables, exact-scope eligibility, profile-local comparison updates, routing and retention distinctions, unknown comparisons, split and bridge cases, marginal versus joint certificates, lapse versus rebuttal, selector-level empty active sets, safety projections, and append-only lossless provenance. Added a local Markdown link checker, one-command instructions in [`verification/README.md`](verification/README.md), and the minimal [verification workflow](.github/workflows/verify.yml). `python -m verification` passes. No neural experiment, proof-assistant claim, or new claim-ledger disposition was introduced.

- [x] **Task 12 — Analyze open-endedness and non-finality.**

  Create `formalism/06_open_endedness.md`. Starting from Task 11A's profile-indexed interface and the Task 11B compact executable reference, define stage/refinement DAGs over world/stage pairs, compatible continuations, live alternatives, current `Lic_P` grant, eventual profile-indexed stability, known/certified stability, semantic finality, and optional meta-level truth. Hold `(e,q,P)` fixed when asserting stability; changing one creates a new substantive question. Prove or sharply refute a **stability trichotomy**: (i) deterministic proof-backed atoms can be internally certified stable when every allowed continuation freezes their premises and proof-validity rules; (ii) statistical atoms separated from their tolerance boundary can stabilize under explicit convergence/simultaneous-coverage assumptions while finite-prefix indistinguishability prevents a zero-error announcement of arrival in a rich family; and (iii) open-library comparison atoms cannot receive a sound permanent-stability certificate when every stage admits a history-preserving `AddModel` continuation with a validly evaluated dominator. Also give countermodels showing that extendability permits eventual stabilization and endless change and that different profiles can behave differently. Treat absence of `Final` from the base language as syntactic non-expressibility, not a substantive theorem. Include Löb/GL only if a genuine provability translation with derivability/fixed-point conditions is constructed; otherwise record a concise rejection, and do not use “anti-Löbian” as a theorem name.

  Completed 2026-07-12. Created [`formalism/06_open_endedness.md`](formalism/06_open_endedness.md). Defined compatible refinement DAGs over finite world/stage pairs, fixed queries `(e,q,P)`, stage indistinguishability, live alternatives, current grant, pathwise eventual stability, permanent current stability, scheme-relative certified stability, semantic finality, and optional target truth. Proved a finite-prefix non-certifiability theorem, deterministic freeze theorem, margin-separated statistical stabilization theorem, zero-error finite-announcement impossibility theorem, finite-profile lifting result, and directional open-library dominator theorem. Added countermodels separating stabilization, known arrival, endless change, profile dynamics, and finality. Sharply refuted the polarity-unrestricted comparison-instability target: a persistent dominator can make refutation stably certifiable, while currently supported non-domination remains non-final under valid dominator extensions. Updated [`notes/claim_ledger.md`](notes/claim_ledger.md) with the scoped replacement and full project impact, superseded the unsupported Löb framing, and retained `Final`, `Complete`, and `True` only in the metalanguage. No new literature claims were imported in Task 12; the now-completed Task 12A supplied primary-source positioning.

- [x] **Task 12A — Fill the core-related literature gaps before freezing the calculus.**

  Create `notes/literature_core_supplement.md` and update `references.bib`, `notes/literature_map.md`, and affected ledger rows using verified primary sources. Audit input/output logic for output-producing rather than truth-detaching consequence; prioritized reasons/defaults; justification and labelled deductive logics for explicit evidence/provenance terms; awareness/unawareness logics for library versus conceivable alternatives; strong-Kleene and logic-of-partial-functions precedents for meaningful `K_3` aggregation, with Bochvar-style infectious undefinedness treated as a comparison to the superseded four-chain presentation; conformal prediction's primary sources; and high-confidence/safe policy improvement over a fallback. Determine exact structural similarities and mismatches without importing representation, completeness, or soundness theorems whose hypotheses are absent. Treat the Fable audit's L1–L5 list as search leads, not verified authority.

  Completed 2026-07-12. Created [`notes/literature_core_supplement.md`](notes/literature_core_supplement.md) and verified fifteen primary sources spanning input/output logic, prioritized defaults/reasons, justification and labelled deduction, awareness/unawareness, Strong-Kleene and partial-function logic, conformal prediction, and safe baseline policy improvement. Added their checked metadata to [`references.bib`](references.bib), integrated the results into [`notes/literature_map.md`](notes/literature_map.md), and updated `A08`, `E02`, `E09`, `E10`, and new algebraic row `E16` in [`notes/claim_ledger.md`](notes/claim_ledger.md). Established one direct import—the Strong-Kleene finite-meet algebra after explicit relabeling—and classified every other connection as structural or an elaboration template requiring a hypothesis-preserving translation. Kept empirical evidence terms non-factive, separated task tolerance from confidence and conformal miscoverage, treated Bochvar infection only as a comparison to the superseded four-chain, and found no new falsified inherited claim. Task 13 may now freeze the core without silently inheriting neighboring soundness, completeness, coverage, or safety theorems.

- [x] **Task 13 — Select the smallest publishable logic and state its metatheory goals.**

  Create `formalism/07_core_calculus.md`. Consolidate Tasks 7–12A into a compact mathematical calculus rather than reproducing the typed implementation inventory. Target three principal carriers: evaluated use plans `E`, reliance/evaluation contexts `Q`, and finite epistemic states `(S,->)`, with target worlds `W` as a semantic index and profiles as finite families of parameterized requirement schemata. Package cases, domains, losses, risk posets/acceptable sets, task interfaces, and frames as dependent data of `q`; package record, library/evaluated set, budget, search, certificate support, dependency, and abstract provenance as data of `s`. Treat theory frameworks, exhaustive event/reason taxonomies, full provenance DAGs, bridges, Pareto/splitting machinery, and policy/value work as elaborations or extensions. Use a compact request `(s,e,q,P)` and prove or countermodel that `e,q,s,P` are independently indispensable.

  Canonical assessment is two-phase: `WF(P,e,q,s)` handles type/denotation/executability and yields `Undefined` on failure; every well-formed required atom has value in `K_3={refuted,open,supported}`, aggregated by meet to yield `Refused`, `Withheld`, or `Granted`. Define `Diag(r)` through atom identity, positive/counterwitness, obstacle, and provenance; flat names such as `HardRiskViolation` are renderings, not primitive constructors, and `NoLicensedModel` is derived from an empty active set. Define safety projections without a global reason enum. Separate profile shape from atom parameters, define typed atom entailment/refinement (including tolerance, fallback margin, constraints, trace, and certified-to-relative comparison), and lift it to a profile preorder. Give syntax, `<W,S>` semantics, consequence rules, update dynamics, a typed-elaboration map back to the detailed records, a central glossary, and a theorem-dependency/novelty table distinguishing definitions, standard lemmas, new characterizations, impossibility results, countermodels, and conjectures.

  Completed 2026-07-12. Created the canonical [`formalism/07_core_calculus.md`](formalism/07_core_calculus.md). Compressed the prior implementation inventory to three principal carriers `E,Q,S`, with `W` semantic and profiles finite parameterized syntax; fixed the request `(s,e,q,P)` and proved all four coordinates independently indispensable using finite separating models. Defined the two-phase `WF + K_3` assessment, operational atom clauses, lossless indexed diagnostics and safety projections, typed atom refinement, request-local and uniform profile preorders, labelled output-producing consequence, fallback behavior, update/dependency semantics, `<W,S>` models, and typed compression/elaboration maps. Added a canonical glossary and a novelty/dependency table that reserves the paper-carrying characterization work for Task 14. Updated the [`README.md`](README.md), [`notes/project_spec.md`](notes/project_spec.md), [`notes/claim_ledger.md`](notes/claim_ledger.md), and the historical [`formalism/05a_integration.md`](formalism/05a_integration.md) notice. Added supported ledger result `E17`; no inherited claim was newly falsified.

- [x] **Task 14 — Prove or refute the core calculus results.**

  Create `formalism/08_metatheory.md`. Prove or refute the actual compact core against the Task 13 `<W,S>` semantics, revising the calculus when necessary. The paper-carrying targets are: (1) **robust update persistence**, seeking necessity and sufficiency for grant/diagnostic invariance over an update class via dependency-complete impact cones plus an explicit path-realizability condition, with a countertheorem showing why path absence is only sufficient without realizability; (2) **profile refinement soundness and relative completeness**, proving semantic antitonicity and constructing a finite separating countermodel for missing syntactic entailments in an independent-atom fragment; and (3) **minimal diagnostic representation**, characterizing the quotient of meaningful atom-state vectors that answers every supported profile query and deriving a `3^n` distinguishable-state / `n log_2(3)`-bit lower bound for well-formed requests when singleton profiles are available, plus a separate channel for `WF` failure and its derivation. Also settle the `WF + K_3` status normal form, algebra, four-outcome minimality, safety projection, typed elaboration invariance, mode-scoped soundness, labeled non-explosion, tolerance monotonicity, safe fallback on gaps, raw heredity without current-status monotonicity, comparative defeat by profile, failure of unrestricted rational monotony, and failure of global closure from finite search. Audit closure under finite well-founded composition of use plans and higher-order evaluation: distinguish the target criterion `L_q` from a plan or certificate that estimates it, show why licensing components alone does not license their composition without propagation hypotheses, and keep cyclic self-reference outside the core unless explicit fixed-point semantics are supplied. Treat KLM premise postulates only as structural comparisons because nonmonotonicity primarily lives in state transitions. Label definitional consequences and standard algebra separately; they do not satisfy the theorem contribution by themselves. Every refutation must update the claim ledger with project impact.

  Completed 2026-07-12. Created [`formalism/08_metatheory.md`](formalism/08_metatheory.md) and retained the three-carrier core. Proved the observable-indexed robust impact-cone characterization with path realizability and a spurious-edge countertheorem; soundness of every frozen atom rule, profile antitonicity, and relative completeness via a finite independent-atom separator; and the query-induced minimal diagnostic quotient. Corrected the planned lower bound: singleton profiles distinguish the realizable vector set `V`, while `3^n` and `ceil(n log_2 3)` require full ternary independence; a two-atom refinement leaves seven, not nine, realizable vectors. Proved/scoped the `WF + K_3` normal form, four-outcome observation bound, safety projections, typed elaboration invariance, labelled non-explosion, tolerance monotonicity, operational gap fallback, raw heredity, profile-local defeat, and finite DAG reification. Countermodeled unqualified world-factivity, target-safe fallback, unrestricted rational-style update persistence, global closure, unconditional component-license composition, and implicit cyclic self-reference, recording project impact in updated ledger rows `E01`, `E03`, `E05`, `E06`, `E14`, `E16`, and new `E18`–`E27` in [`notes/claim_ledger.md`](notes/claim_ledger.md). Added five finite regression witnesses in [`verification/test_metatheory.py`](verification/test_metatheory.py), updated [`verification/README.md`](verification/README.md), the audited [`formalism/07_core_calculus.md`](formalism/07_core_calculus.md), [`notes/project_spec.md`](notes/project_spec.md), and [`README.md`](README.md). All 33 repository tests pass.

- [x] **Task 14A — Prove the domain-transport and routed-cover bounds.**

  Create `formalism/08a_transport_routing.md`. Build a second, cross-layer theorem cluster rather than adding more record definitions. At minimum prove: (1) for nonnegative integrable loss on a probability space, every positive-measure subdomain has conditional expected loss at most `epsilon` iff the loss is at most `epsilon` almost surely, exactly characterizing when expected-risk adequacy restricts freely; (2) a hard-router expected-risk bound decomposing correct-cell local risks plus an explicit misrouting/fallback penalty under bounded loss; and (3) a Lipschitz bridge/blending bound translating prediction disagreement into excess task risk. State measurability, boundedness, coverage, and selection assumptions exactly, and give counterexamples when they fail. If concise, connect these horizontal transport results to a finite plan-DAG error/cost budget, explicitly accounting for interactions that defeat naive summation. Optionally prove the group-valued bridge cocycle characterization—global frame potentials exist iff cycle products are identity—only if it remains concise and relevant. Classify these as extension theorems, but use them to connect the abstract license calculus to the later neural router.

  Completed 2026-07-12. Created [`formalism/08a_transport_routing.md`](formalism/08a_transport_routing.md). Proved that expected adequacy restricts to every positive-measure measurable subdomain iff loss is bounded by the tolerance almost surely, with a parent-average counterexample; gave the exact hard-router correct/misroute/fallback decomposition, bounded penalty theorem, conservative whole-cell alternative, and counterexamples for selection bias, unbounded rare failures, and missing coverage; proved Lipschitz prediction-bridge and convex-blend excess-task-risk bounds with discontinuous/unbounded-sensitivity counterexamples; derived a finite plan-DAG path-sensitivity error budget, outer risk corollary, and resource-specific cost qualification; and proved the optional exact group-valued cycle/potential characterization. Added seven finite regression witnesses in [`verification/test_transport_routing.py`](verification/test_transport_routing.py), updated [`verification/README.md`](verification/README.md), and recorded supported replacements/refutation impacts in updated `E04`, `E13`, `E14`, `F28`, and new `E28`–`E32` in [`notes/claim_ledger.md`](notes/claim_ledger.md). Updated the extension notices in [`formalism/05_atlas.md`](formalism/05_atlas.md) and [`formalism/07_core_calculus.md`](formalism/07_core_calculus.md), plus [`notes/project_spec.md`](notes/project_spec.md) and [`README.md`](README.md). All 40 repository tests pass.

- [x] **Checkpoint B — Reassess the roadmap after the core calculus and metatheory.**

  Create `notes/checkpoints/B_core_metatheory.md` after Tasks 14 and 14A and apply the checkpoint protocol. Decide whether the proved, refuted, or weakened theorem spine supports the proposed ReLU correspondence; identify which formal objects and theorems actually need neural representation; and revise Task 15 onward so the ML work targets the surviving calculus rather than the earlier aspirations. Treat ReLU as the reference witness, not a uniqueness claim: identify which requirements are architecture-neutral and nominate a small set of plausible alternatives—for example monotone/lattice models, maxout or mixture-of-experts routers, graph/set architectures for composed plans or variable libraries, or neuro-symbolic decoders—using explicit selection criteria rather than breadth. Require an explicit tally of paper-carrying characterizations/impossibility results versus definitions and standard lemmas; if the core remains theorem-light, add a focused proof repair before ML work.

  Completed 2026-07-12. Created [`notes/checkpoints/B_core_metatheory.md`](notes/checkpoints/B_core_metatheory.md) after independently checking the new [`llm_convos/claude_audit_2026-07-12.md`](llm_convos/claude_audit_2026-07-12.md). Counted profile relative completeness as the cleanest current logic result, counted the stability package only after precision/attribution repair, and found that the update characterization remains an abstract graph theorem until the actual atom clauses receive a canonical reads-from/locality theorem. Added Task 14B as a pre-ML gate; froze architecture-neutral, robust-margin, finite-ReLU, hard-seam, and expandable-library targets for Task 17; restricted alternatives to monotone/lattice and hard-MoE hypotheses; deferred graph/set models until Task 15 supplies a reason; and rejected broader metatheory, immediate Lean work, and a notation script as current gates. Revised every pending task prospectively. No Task 14B proof or neural implementation was begun.

  Amended 2026-07-12 from project-author guidance. Added Task 14C for proof-carrying recursive plans, the meanings of theoremhood within and about value logic, and grounded/stratified assessment of the value-logic system itself. Accepted dual-use ReLU activations as a conditional construction when channels have named, commensurate, certificate-valid margin semantics—or verified approximation envelopes with boundary separation—and the representation is jointly sufficient for license queries and downstream computation; rejected that interpretation for arbitrary hidden activations or as a replacement for payload, negative/open status, and certificate channels. Expanded Tasks 15–19 and the final audit accordingly. At the time of this amendment, Task 14B remained the next narrow repair.

- [x] **Task 14B — Repair and instantiate the theorem spine.**

  Create `formalism/08b_audit_repairs.md` as a compact proof-and-correction note and update the affected canonical files, reference implementation, tests, bibliography/literature positioning, specification, and ledger. Define, for each frozen operational atom clause, a finite typed **query/read footprint** over record keys and relations—not only currently existing provenance nodes—including positive and negative certificate reads, validity/correction closure, verifier versions, exact evaluated sets, search validity, and relevant pair records. Define event-schema write footprints. Prove by cases that agreement on an atom's footprint preserves its complete diagnostic and `K_3` value, and derive that write/read disjointness gives invariance and that the canonical event-to-key-to-atom graph is change-complete; keep necessity conditional on observable-specific path realizability. Do not add another broad metatheory layer or new principal carrier.

  Repair the accepted audit items: Task 12 Theorem 3's joint eventual index; Theorem 4's regime-versus-trajectory declaration, stopping-time/adaptedness conditions, and classical-pattern citations/novelty classification; Task 14 Theorem 10's base-local profile notation and independent-realizability wording; Theorem 14's canonical-tag rather than literal-code reading; and the finite discrete-code rather than neural-width meaning of its bit bound. Give a concrete core realization for the seven-vector countermodel. In `verification/`, represent invalid comparison/search traces as open, enforce mutually exclusive diagnostic payloads, document missing-diagnostic invalidity, turn the spurious-impact regression into a real state/assessment witness, and upgrade or explicitly demote the arithmetic-only composition check. Update the theorem tally and affected ledger rows without inflating standard results. Run the complete verification and link suite.

  Completed 2026-07-14. Created [`formalism/08b_audit_repairs.md`](formalism/08b_audit_repairs.md). Distinguished current explanatory provenance from finite typed slot read footprints, made negative certificate/search/pair reads explicit through collection-index keys, defined event write contracts, proved complete-diagnostic locality by cases, derived the disjoint-write frame rule, and proved the canonical event-to-key-to-slot graph change-complete for diagnostics, values, assessment, and grant. Necessity remains conditional on observable-specific path realizability. Repaired Task 12's joint eventual index and adapted regime-declaration theorem; verified and added Wald, Darling–Robbins, Robbins, and Bahadur–Savage primary sources with conservative transfer boundaries; repaired Task 14's instantiation-fiber profile notation, concrete seven-vector region witness, decoded `Ill/Well` normal form, and finite-discrete-code qualification. Hardened [`verification/kernel.py`](verification/kernel.py), added executable typed footprints in [`verification/footprints.py`](verification/footprints.py), and added real state/assessment witnesses in [`verification/test_audit_repairs.py`](verification/test_audit_repairs.py). Diagnostics now form a true disjoint sum; invalid/missing comparison searches are open; missing required or report diagnostics invalidate a fixture rather than denote missing evidence; and the composition regression uses actual requests. Updated the canonical calculus/metatheory, bibliography, literature map, specification, checkpoint tally, README, and claim ledger. All 48 tests and the repository link checker pass. No inherited claim was newly falsified; the update cluster now clears the calculus-specific theorem bar.

- [x] **Task 14C — Formalize proof-carrying recursive plans and stratified system assessment.**

  Create `formalism/08c_proof_carrying_plans.md`. Begin by distinguishing: (1) labelled object-model results `[e,q]phi`; (2) internal value-logic derivations `Gamma;s |-VL J`; (3) external metatheorems about the calculus; and (4) empirical certificate judgments accepted under a named mode. State exactly where a Curry–Howard/proof-term analogy applies and why a neural score, confidence region, or accepted empirical certificate is not unconditionally a proof of a target-world fact. Audit verified primary literature on Curry–Howard, refinement/dependent or graded types, Hoare/program logics, certifying algorithms, and proof-carrying code before making novelty claims.

  Define a finite well-founded typed plan language whose certified primitive executor returns a bundle `(payload, quantitative grade/bound, certificate/provenance)`. Give sequential/parallel composition, frame bridges, tagged choices/routers, loss estimators, and resource aggregators only to the extent needed for one compact theorem. Each constructor must have an output transformer, grade transformer, certificate transformer, and exact interface/scope/termination assumptions. Prove or countermodel a **proof-carrying recursive-plan theorem** by structural/topological induction: every plan built from certified primitives has an annotated executor whose proof erasure is the ordinary computation. Prove a **license-lifting corollary** from a valid composite certificate and acceptable root grade; do not compose licenses by meeting component grants. Instantiate at least one constructor with Task 14A's path-sensitivity/tube-valid error bound and keep resource aggregation typed.

  Reify a frozen value-logic implementation/checker as a plan `e_VL` under an independently fixed meta-context `q_VL`. Prove or sharply delimit a **grounded-provenance theorem** for finite acyclic support graphs and a **stratified self-evaluation theorem** for finite-ranked evaluator/request dependencies with total deterministic local evaluators. In the DAG fragment, prove that every supported required atom traces to typed base evidence and note that closed mutual support is syntactically inadmissible. Keep direct self-license cycles outside the core unless an explicit monotone immediate-consequence operator, fixed-point selection, and soundness theorem are supplied; only under such a selected cyclic semantics may the task ask whether an ungrounded mutually supporting component can create a grant. In every case, show that system self-endorsement alone is not target-factive. Classify imported program-logic machinery separately from any project-specific integration result, update the ledger with every countertheorem's project impact, and stop before neural encoding.

  Completed 2026-07-14. Created [`formalism/08c_proof_carrying_plans.md`](formalism/08c_proof_carrying_plans.md). Distinguished labelled object results, internal value-logic derivations, external metatheorems, and accepted empirical/formal certificate judgments; audited Howard, Martin-Löf, Hoare, refinement and quantitative types, proof-carrying code, and certifying algorithms against seven primary sources; and limited Curry–Howard to proof terms checked in named formal systems. Defined one compact generic finite-DAG constructor interface with payload, typed grade, and certificate/provenance transformers; proved annotated execution and proof erasure by topological induction; instantiated the grade proof with Task 14A's tube-valid path-sensitivity bound; and lifted an acceptable checked root certificate through the actual `WF + K_3` kernel without composing component grants. Proved that every supported required atom in the declared finite DAG fragment reaches a typed base, and that strict finite-rank dependencies with total deterministic local evaluators yield a unique grounded assessment of a frozen `e_VL`. Countermodels retain the exact limits: untyped zero-premise success is not grounding, same-rank cycles are underdetermined, and self-endorsement is not target-factive. Added the standard-library executable witness [`verification/proof_plans.py`](verification/proof_plans.py) and nine regressions in [`verification/test_proof_plans.py`](verification/test_proof_plans.py), including typed energy/latency aggregation and an actual composite grant. Updated the canonical calculus/metatheory/transport notes, bibliography, literature map, specification, checkpoint, README, verification guide, and claim ledger. All 57 tests and the repository link checker pass. No new inherited claim was falsified: the countermodels sharpen existing `E21`, `E22`, and `E27`; the finite formal parts of `E33–E34` are supported, while empirical system adequacy remains open.

- [x] **Task 15 — Specify operational encodings for cases, domains, and models.**

  Create `ml/01_encodings.md`. Encode the surviving core and Task 14C annotated-plan interface before optional extensions, specifying a **query-family-specific, architecture-neutral neural/symbolic contract** before choosing a scorer. Begin from atom addresses and Task 14B query footprints: define dependency-scoped inputs `x(s,e,q,a)` and keep `P` primarily as the consumer that selects/aggregates atom records rather than a hidden cause of atom value. Specify which fields are explicit typed inputs, mechanically fixed context, learned sufficient statistics, validity/missingness indicators, external checker/certificate/provenance pointers, or forbidden compression targets. Separate the status-minimal `WF` plus `V/~_F` code from the stronger audit-preserving interface containing atom identity, diagnostics, safety projections, and future-profile information. For a plan node, separate computational payload, quantitative grade/bound, certificate/checker/version/assumptions, and provenance/dependency rank. State invariances under provenance-ID renaming, candidate permutation where appropriate, and finite plan-DAG isomorphism. Do not turn a discrete-code bit bound into a neural-width bound or a predicted grade into a proof.

  Compare a fixed finite model-indexed baseline with a candidate-conditioned shared scorer over a variable library; analyze sparse/on-demand pair comparisons versus quadratic matrices. Compare flattened plan identity with an explicit DAG encoding under the concrete hypothesis that structurally distinct, extensionally equal methods differ in cost, robustness, grade propagation, or explanation. Retain loss-estimator and checker identity either way. Divide mandatory core outputs from theorem-indexed routing, bridge, and plan-composition quantities so one network is not required to predict every extension field. State identifiability/equivalence classes and interpretation tests; do not treat latent coordinates as ontology. Define the joint license/computation consumer family needed for the dual-use activation question. End by checking that the encoding can state every Task 17 theorem target frozen at Checkpoint B without silently assuming it.

  Completed 2026-07-14. Created [`ml/01_encodings.md`](ml/01_encodings.md). Defined the exact atom input as its typed address plus Task 14B dependency projection, factored slot instantiation from address-local evaluation, and made profiles consumers of stable atom records. Separated exact typed content, mechanical context, missingness/validity, learned statistic/envelope proposals, and opaque checker/certificate/provenance/loss-estimator handles; fixed the stronger audit interface alongside the consumer-relative `WF + V/~_F` status code. Specified fixed indexed and candidate-conditioned variable-library designs, the exact limits of sparse comparison evidence, payload/grade/evidence channels for annotated plans, provenance/candidate/DAG invariances, and an explicit matched flat-versus-DAG hypothesis. Defined joint license/computation sufficiency and the scalar adequacy obstruction without assigning ontology to latent coordinates. Added eight standard-library regressions in [`verification/encodings.py`](verification/encodings.py) and [`verification/test_encodings.py`](verification/test_encodings.py), including the equal-margin/different-payload witness. Checked all seven frozen Task 17 targets without assuming their hypotheses. All 65 tests and the repository link checker pass. No inherited claim was falsified; `F29`, `F31`, and `F32` are now more sharply scoped, `F34` records the supported dependency contract, and `F33` records the still-open explicit-DAG empirical hypothesis.

- [x] **Task 16 — Derive the ReLU license architecture.**

  Create `ml/02_relu_architecture.md`. Define a hybrid ReLU **reference** architecture whose learned part predicts continuous atom sufficient statistics—preferably certificate regions or dual support/refutation margins plus calibrated error/validity—while exact metadata, mechanically decidable `WF`, proof/certificate checking, profile roles, `K_3` aggregation, active-set masking, and fallback remain symbolic. Expose separate payload, quantitative-grade, validity/missingness, and certificate/provenance channels for annotated plan nodes. Explain why ReLU is analytically convenient without claiming uniqueness or empirical optimality. Preserve inclusive-zero boundary conventions; rectified channels alone cannot distinguish boundary support from open/failure.

  Give an explicit hypothesis-indexed hidden-layer construction in which `m_i` is the registered conservative margin from accepted evidence and `z_i=ReLU(m_i)` is both strict positive normalized certificate-relative atom surplus and a downstream feature. State the channel map, common scale, calibration, domain, and downstream use; compare it with separate content/grade channels. Explicitly distinguish this construction from an arbitrary positive hidden unit, a merely predicted positive margin, and a full profile license. Do not use raw `argmax` as global adequacy or multiply payloads by margin magnitude without licensing the resulting plan. Do not train an aggregate-status head that can contradict its atoms, a flat reason classifier, or a self-grant head except as an ablation. Preserve diagnostics, safety projections, invalid/missing/conflict states, exact scope/mode, registry/checker/provenance/loss-estimator pointers, and direct-composite or Task 14A propagation certificates. Keep licensing separate from selection/routing and prohibit selection outside the active mask. Provide fixed-library and candidate-conditioned shared ReLU designs; analyze monotone/lattice and hard-MoE variants as already scoped.

  Completed 2026-07-14. Created [`ml/02_relu_architecture.md`](ml/02_relu_architecture.md). Selected a hybrid ReLU statistic scorer plus exact symbolic decoder: the learned heads emit center/region, nonnegative spread, uncertainty/validity proposals, payload, grade, and post-license utility where applicable; separately versioned calibration/checker records supply accepted error radii and polarity. Derived inclusive-support/strict-refutation margins for adequacy and fallback improvement, retained signed/paired-ReLU and complete diagnostic channels, and made learned validity conservative-only. Specified separate annotated-plan payload/grade/evidence paths, direct-root versus certified Task 14A propagation, fixed-indexed and candidate-conditioned shared designs, named normalized dual-use hypothesis channels, exact active-mask selection/fallback, forbidden production heads, and the scoped monotone/lattice, hard-MoE, and graph/set variants. Added a canonical three-requirement toy that distinguishes negative margin, boundary zero, open, refuted, missing, and invalid cases; separates slack, normalized surplus, atom support, full profile license, and selection; encodes `A and I and C` as a vector; and implements its fixed grant bit with ReLU over exact state bits. The example now distinguishes parameterized learned heads from scalar output and fixed-decoder channels: several channels per atom are generally required, the two rectified channels alone still collide on boundary/open/missing cases, and one shared interval head can nevertheless serve several threshold atoms. Added [`verification/relu_architecture.py`](verification/relu_architecture.py) and thirteen regressions in [`verification/test_relu_architecture.py`](verification/test_relu_architecture.py). The suite checks the interface-level boundary cases, including supported equality at zero activation, one-sided certificate withholding, invalid-state override, mask-safe selection, and a concrete counterexample to ReLU-zero quarantine. All 78 tests and the repository link checker pass. No central project claim was lost: `F18` is falsified only in its unqualified ReLU-only form and replaced by exact symbolic masking with full project-impact propagation; `F05`, `F06`, `F19`, `F21`, `F23`, `F31`, and `F32` are sharpened at their supported or still-open scopes.

- [x] **Task 17 — State and verify the ReLU/atlas representation results.**

  Create `ml/03_representation_theorems.md`. Prove or sharply delimit the seven targets frozen at Checkpoint B: (1) architecture-neutral exact factorization through the required `WF` observations and query quotient `V/~_F`, with a finer contract for diagnostics; (2) margin-robust approximate realization, where bounded statistic error plus boundary separation yields exact decoding and the uncertainty band becomes open; (3) exact finite ReLU computation of fixed-dimensional CPWL atom statistics followed by an external boundary-aware decoder; (4) the conforming-polyhedral hard-routing seam characterization and discontinuity obstruction; (5) fixed model-indexed outputs versus candidate-conditioned scoring over an expandable registry; (6) joint sufficiency for license queries and downstream computation, with scalar insufficiency, boundary, and scale counterexamples; and (7) exact ReLU realization of the proof-erased joint payload/grade map for a fixed finite annotated CPWL plan, leaving certificate terms to the external checker.

  Make explicit that discrete-code bits do not bound real output width, `Ill/Well` is a canonical decoded form, and direct `K_3` comes from the external decoder. For the author's classifier example, prove the positive score-as-content construction under named, commensurate, certificate-valid margins—or verified approximation envelopes with boundary separation—and the general factorization obstruction when equal adequacy codes hide unequal payloads. Do not count the definition-level joint-sufficiency equivalence alone as a theorem contribution; add a nontrivial construction, minimal quotient/factorization result, or channel-family obstruction. State covariance or normalization requirements under margin rescaling. Verify exact primary CPWL/ReLU results, size/depth, domain extension, seams, and architecture conventions. State what transfers to other architectures. Universal approximation alone establishes none of semantic compatibility, proof production, efficiency, learnability, calibration, or interpretability.

  Completed 2026-07-14. Created [`ml/03_representation_theorems.md`](ml/03_representation_theorems.md) and adjudicated all seven frozen targets. Proved the architecture-neutral kernel/quotient characterization for public status and finer audit consumers; the conservative approximate-statistic theorem with raw propagated margin radius `r` and guaranteed exact recovery outside the explicit `2r` ideal-margin band; the exact coordinatewise finite-ReLU realization of global fixed-dimensional CPWL statistic maps with audited depth, size, and domain-extension conventions; the conforming hard-seam iff, discontinuity obstruction, and rank-one codimension-one corollary; fixed-index versus shared candidate scoring plus the finite-evaluation non-closure counterexample; the equal-code obstruction and a named state-plus-surplus code that is minimal for a coordinate-complete dual-use consumer family, including boundary, approximation, and scale-covariance results; and exact proof-erased payload/grade realization for finite CPWL annotated plans while leaving certificates external. Added [`verification/representation_theorems.py`](verification/representation_theorems.py) and seventeen regressions in [`verification/test_representation_theorems.py`](verification/test_representation_theorems.py). All 95 tests and the repository link checker pass. No new central claim was falsified; `F11–F14`, `F29`, `F31`, and the formal part of `F32` are supported at their stated scopes, while efficiency, learning, calibration, semantic alignment, and the empirical side of dual-use channels remain open.

- [x] **Task 18 — Decide the training objective.**

  Create `ml/04_losses.md`. Analyze certificate-region/risk regression, calibrated prediction error, positive/countercertificate evidence, learned validity, open/missingness, auxiliary atom-state losses, ranking/selection, selective-risk objectives, reject options, and cross-entropy. Keep four levels separate: task criterion `L_q`, a model estimating outcomes or `L_q`, optimization loss, and the proof/certificate relation used to license the resulting estimator or system. Choose a structured primary objective learning sufficient statistics and uncertainty/calibration plus a simple classification baseline. Derive states and public outcomes symbolically; direct status and self-grant heads are auxiliary/ablations. Training targets must be externally defined, and optimization success is not a proof. Keep router loss separate and state how score-as-content channels are normalized or regularized without assuming their semantic alignment.

  Completed 2026-07-14. Created [`ml/04_losses.md`](ml/04_losses.md). Selected schema-balanced, standardized center–radius statistic regression with squared center error and a central interval score, followed by a disjoint held-out residual-expansion calibration proposal and an exact mode/checker boundary. Kept task criterion, estimator, optimization, and certification distinct; specified polarity-aware symbolic `WF + K_3` decoding, exact missingness, conservative-only learned rejection, typed payload/grade auxiliaries, and unit-covariant dual-use surplus. Chose independent per-atom three-way cross-entropy as the simple nonauthorizing baseline, and separated exact-mask pairwise router ranking plus joint selective-risk/coverage/fallback accounting from licensing. Added [`verification/losses.py`](verification/losses.py) and fourteen regressions in [`verification/test_losses.py`](verification/test_losses.py). Added primary proper-scoring and conformal-risk-control references. All 109 tests and the repository link checker pass. No new claim was falsified; the preference for the structured objective over direct atom classification is recorded as falsifiable `F35/T0`, with project impact specified if the experiment reverses it.

- [x] **Checkpoint C — Reassess the roadmap after the neural representation and objective.**

  Create `notes/checkpoints/C_neural_blueprint.md` and apply the checkpoint protocol. Check that the encodings, hybrid ReLU architecture, representation results, and objective form one implementable/falsifiable system under Tasks 14B–14C, including payload/grade/certificate separation, dual-use joint sufficiency, and grounded system assessment. Select at most one trained non-ReLU comparison unless two hypotheses are adequately powered. Leading candidates remain monotone/lattice and hard MoE; a graph/set model now has a concrete recursive-plan hypothesis but still requires a fair powered comparison. Specify matched outputs, capacity/compute, and fairness; retain ReLU alone if alternatives add breadth without evidence. Revise Task 19 onward before implementation.

  Completed 2026-07-14. Created [`notes/checkpoints/C_neural_blueprint.md`](notes/checkpoints/C_neural_blueprint.md). Verified that Tasks 14B–18 define one implementable hybrid pipeline from address-local inputs through statistic/payload/grade proposals, proposal-bound accepted calibration, exact evidence and `WF + K_3` decoding, active masking, routing/fallback, and independently ranked system audit. Repaired the stale top-level claim that a positive ReLU output grants or zero quarantines and recorded the correction as an author-intuition repair: arbitrary positivity, predicted positive slack, accepted certificate-relative surplus, and a full profile license are four different objects. Accepted Fable's C17–C19 positioning corrections and added primary phantom/frame and Kripke–Fitting references. Fixed the minimum publishable empirical core as `F35/H18.1`, registered marginal calibration `F36/H18.2`, the core succession fixture, and ablations 1–3 and 5, with the `F18` sign/masking counterexample retained as a deterministic integration witness. Authorized no alternative architecture in that core; hard MoE is the sole optional comparator, conditional on a separately powered seam study, while monotone/lattice and graph/set comparisons are deferred. Added a pilot/freeze task, a post-experiment checkpoint, and a reader audit; revised every pending item; and recorded a provisional answer to the paper's lead question. All 109 tests and the repository link checker pass.

- [x] **Task 19 — Preregister the decisive experiment.**

  Create `experiments/01_design.md` without implementing or running the experiment. Preregister an independently defined synthetic succession generator and one reusable Newtonian-like narrative fixture—not an empirical model of actual physics—with all four public outcomes, supported/open/refuted atoms, missing/invalid evidence, simultaneous licenses, a gap/fallback, lapse versus rebuttal, relevant/irrelevant updates, tolerance change, and a later finite-set dominator. Reuse and extend Task 16's canonical `A and I and C` vector/sign example rather than introducing incompatible meanings for zero, slack, surplus, or grant. Freeze a pre-outcome scorer whitelist, immutable proposal/calibration/checker binding schema, exact oracle semantics, and five blocked roles: train, envelope calibration, reject/router validation, system audit, and final confirmation, grouped by latent world/trajectory, provenance root, and plan family.

  Make the **minimum publishable core** `F35/H18.1`, registered marginal calibration `F36/H18.2`, false-support/refutation and atom/query fidelity at matched `K_3` outputs and after shared exact profile aggregation, an in-regime noninferiority guard, and Task 18 ablations 1–3 and 5. Add a deterministic `F18` witness—not a powered endpoint—that contrasts arbitrary positive activation, predicted positive slack, accepted certificate-relative atom surplus, and the full exact profile license, and that reproduces both supported equality at zero and bias/bypass failure of zero-quarantine. Freeze the structured arm's learned-head factorization separately from its required semantic channels; count deterministic margin/ReLU/state operations as decoder channels rather than learned heads, and treat any shared-versus-separate learned-head performance claim as empirical rather than logically required. Give numerical superiority/noninferiority margins, endpoint hierarchy and multiplicity rule, replication unit, paired uncertainty/test rule, target-distribution reweighting, boundary-case density, sample-size/power criterion, seed roles, stopping rule, and final-test embargo. The structured arm symbolically decodes its accepted region while cross-entropy predicts `K_3` directly; both receive the same permitted inputs and share exact `WF`/evidence/polarity handling and profile aggregation. Their reference labels come only from the generator oracle. Do not conflate region coverage, class-probability calibration, profile coverage, selected risk, or system adequacy.

  Preregister routing/seam and certificate/system work as separately labeled extensions. Hard MoE is the only eligible architecture comparator and proceeds only if Task 19A can power a paired conforming-versus-mismatched seam interaction under the Checkpoint C fairness contract; otherwise omit it prospectively. The system extension must separate lower-ranked audit evidence from untouched confirmation, include a learned-grade-to-checked-composite-certificate adapter, and treat self-confidence-only pseudo-licensing as invalid. Activation alignment remains exploratory.

  Completed 2026-07-14. Created [`experiments/01_design.md`](experiments/01_design.md) without implementing or running the experiment. Preregistered an independent CPWL synthetic succession law, exact oracle/evidence semantics, the reusable `A and I and C` Newtonian-like fixture, all required public/atom/update/atlas cases, a closed pre-outcome scorer firewall, immutable proposal binding, and five lineage-blocked evidential roles. Fixed a single vector-valued structured statistic head separately from deterministic semantic channels; matched it to direct atom cross-entropy under common exact `WF`/evidence/polarity/profile handling. Set 5-point superiority margins, a 2-point in-regime noninferiority guard, Holm-controlled endpoint families, paired world-cluster bootstrap inference, target/design reweighting with 20% boundary design mass, a pilot-derived 90%-power rule with a 400-world floor and 5,000-world cap, eight paired fit seeds, stopping/embargo rules, required ablations, and the deterministic `F18` sign/masking witness. Corrected the calibration estimand to marginal target-in-proposal coverage over every eligible case—including infinite proposals—while keeping finite usable-region coverage explicitly selected/descriptive. Hard MoE and system/certificate claims remain separately powered optional extensions; activation alignment remains exploratory. No empirical claim was tested or adjudicated.

- [x] **Task 19A — Build, pilot, and freeze the generator and protocol.**

  Implement the independent generator, oracle/evidence constructor, exact semantic pipeline, split manifest, and metric skeleton under `experiments/`, with focused tests for every core state, update, overlap/gap, lapse/rebuttal, proposal-binding mismatch, scorer-firewall violation, and certificate case. Use only a disjoint pilot seed bank to validate oracle balance, boundary prevalence, clustered variance, numerical effect margins, runtime, and the Task 19 power calculation. Treat generator worlds/trajectories—not atom rows or training seeds—as independent units. Write `experiments/00_pilot.md` and a versioned machine-readable frozen protocol/configuration. Do not inspect final-confirmation worlds. If the hard-MoE or system extension fails its separate feasibility/power gate, demote it prospectively to an illustration or future study before Task 20.

  Completed 2026-07-14. Created [`experiments/protocol.py`](experiments/protocol.py), [`experiments/run_pilot.py`](experiments/run_pilot.py), [`experiments/00_pilot.md`](experiments/00_pilot.md), frozen [`experiments/protocol_v1.json`](experiments/protocol_v1.json), compact implicit [`experiments/manifests_v1.json`](experiments/manifests_v1.json), and raw [`experiments/pilot_results_v1.json`](experiments/pilot_results_v1.json). Generated only 256 pilot-role worlds: every per-world atom/context/public-outcome/focal/boundary quota and the succession fixture passed, oracle-region balance was 0.896875 for `J` and 0.896387 for `T`, and the lineage audit found no world/trajectory/provenance/plan-family overlap. Repaired the narrative dominator by requiring strictly negative paired-difference certificates because the marginal intervals overlap. Recorded that pre-learner paired fidelity variance cannot be empirically estimated without beginning Task 20; froze transparent worst-coupling design-alternative bounds instead of proxy-arm effects. The in-regime guard binds at 4,925 worlds, rounded to 5,000; role counts are 20,000 train and 5,000 each calibration, validation, audit, and final confirmation. Production manifests contain identifiers/hashes only, final materialization raises by default, and no production payload was generated. Hard MoE failed its separate feasibility/power gate and is prospectively omitted; the system extension is deterministic-witness-only; activation alignment remains exploratory. Added 18 focused regressions in [`verification/test_experiment_protocol.py`](verification/test_experiment_protocol.py); all 127 repository tests and the link checker pass. No `F35` or `F36` result was observed or adjudicated.

- [x] **Task 20 — Implement the experiment.**

  Against the frozen Task 19A generator and protocol, implement the matched ReLU structured learner, independent atom-cross-entropy baseline, required core ablations, calibration/checker binding, symbolic `WF + K_3`, indexed diagnostics, simultaneous/empty sets, exact active masking, routing/fallback, signed and rectified margins, and machine-readable traces. Include the deterministic `F18` sign-semantics witness frozen in Task 19 so the implementation and eventual paper cannot silently regress to positivity-as-grant or zero-as-quarantine. Keep the scorer input firewall explicit and test it. **Do not implement hard MoE:** its Task 19A gate failed prospectively. Implement the system/certificate tier only as a deterministic integration witness with typed payload/grade/evidence records, the learned-grade-envelope versus checked-certificate distinction, a composite-certificate transformer, grounded dependency ranks, separate audit records, and no powered empirical system claim. Provide one reproducible frozen entry point; do not inspect or tune on final results.

  Completed 2026-07-14. Created [`experiments/learner.py`](experiments/learner.py), [`experiments/implementation.py`](experiments/implementation.py), [`experiments/system_witness.py`](experiments/system_witness.py), the single guarded [`experiments/run_experiment.py`](experiments/run_experiment.py) entry point, the human-readable [`experiments/02_implementation.md`](experiments/02_implementation.md), frozen [`experiments/implementation_v1.json`](experiments/implementation_v1.json), and pilot-only [`experiments/implementation_smoke_v1.json`](experiments/implementation_smoke_v1.json). Implemented a closed 25-coordinate common input, matched two-hidden-layer ReLU structured/CE arms within 0.8% parameters at both budgets, common-prefix paired initialization with isolated heads, center-then-radius fitting, the full 18-trial joint capacity rule, eight paired final-fit paths, all required ablations, independent-world target-weighted calibration, immutable binding, exact `WF + K_3`/diagnostics/masks/routes/fallback, full signed/rectified channels, compact replayable traces, and the `F18` regression. Prospectively clarified the 100/100 epoch split, world-batch cycling, shared-budget selection, one-score-per-world calibration, explicit four-cell boundary panel, and the 16 structurally realizable tolerance-transfer macro cells before generating any production payload. The deterministic system witness separates learned grade proposals from externally checked certificates and verifies proof erasure, grounded ranks, invalid-certificate/cycle rejection, and audit/confirmation lineage separation without a powered claim. Hard MoE remains absent. A 16-world pilot-role smoke fit completed twice with identical model/prediction hashes; it adjudicates no empirical claim. All production roles and the final confirmation remain unmaterialized; `F35/F36` remain untested. Added 20 focused regressions; all 147 repository tests and the link checker pass.

- [ ] **Task 21 — Run and analyze the experiment.**

  Invoke the already frozen `python -m experiments.run_experiment --task21-final RUN-VALUE-LOGIC-FINAL-V1` entry point exactly once, then create `experiments/02_results.md` plus machine-readable analysis and figures from its raw result/trace artifacts. Lead with the minimum core and report paired uncertainty across independent worlds and training seeds at their correct levels, absolute registered calibration, every null/negative outcome, and every protocol deviation. Treat the boundary panel and the 16 monotonicity-compatible transfer cells exactly as frozen in Task 20; do not manufacture the eight structurally impossible Cartesian cells. Report fixed routing and the system/certificate witness in separately labeled sections at their actual deterministic-evidence grade; hard MoE remains omitted, and no powered empirical system comparison may be added. Keep functional fit, region/class calibration, proof/certificate validity, routed risk, semantic alignment, architecture geometry, and interpretability conclusions separate. Update every tested ledger claim and propagate the overall project impact of any falsification.

  **Paused 2026-07-15 before adjudication.** Attempt 1 ended after approximately 35 minutes in a fatal CPython 3.13.3 evaluator state, after fitting/calibration and 191 of 400 trace shards but before any raw result; no metric was read. The registered unread-infrastructure exception was used under CPython 3.13.14 with all frozen scientific inputs and package versions unchanged. It reproduced the Task 20 smoke hashes and the attempt-1 final-model archive hash, but the project author stopped attempt 2 after approximately 20 minutes because total Task 21 work had exceeded one hour; calibration and final evaluation had not completed. See [`experiments/task21_failure_attempt1.md`](experiments/task21_failure_attempt1.md), [`experiments/task21_interruption_attempt2.json`](experiments/task21_interruption_attempt2.json), and [`experiments/task21_runtime_diagnosis.md`](experiments/task21_runtime_diagnosis.md). The profile exposes 33.6 million redundant payload audits and 100.8 million structured decodes in the frozen confirmation. Task 21 remains undone; no empirical claim changes status. Before another final release, add and pilot-equivalence-test a versioned execution-only amendment with stage checkpoints, block-first reuse across seeds, batched inference, and semantics-preserving firewall/binding vectorization, or explicitly abandon the frozen empirical run.

- [ ] **Checkpoint C1 — Adjudicate the frozen experiment before interpretation.**

  Create `notes/checkpoints/C1_empirical_adjudication.md` and apply the checkpoint protocol to the frozen protocol, results, deviations, and claim ledger. Decide separately whether `F35/H18.1`, `F36/H18.2`, any hard-MoE seam claim, and empirical system-audit claim are supported, falsified, inconclusive, or merely illustrated; propagate their project impacts into Tasks 22 onward and the paper contract. Reject unregistered reruns unless a versioned repair study is justified, and distinguish such a study from the original confirmatory result.

- [ ] **Task 22 — Audit the policy–value and recursive-judgment claims.**

  Create `notes/policy_value_judgment.md` as a bounded identifiability and literature audit, recording the exact inspected commit of `policy_value_isomorph`; do not turn it into a second implementation project. Replace “isomorphism” with the strongest defensible result. Cover reward non-identifiability, occupancy measures, environment dependence, rationalization of policies, recursive evaluation, and conditions under which judgment reveals stable structure. State the assumptions Task 22A must test and decide provisionally whether each surviving claim belongs in the formal paper, only the motivating/Substack post, a companion case-study section, or future work. This task is not a dependency of the core calculus.

- [ ] **Task 22A — Prove, refute, or demote the recursive-judgment information claim.**

  Create `formalism/09_judgment_information.md`. Give B01 a precise latent-task prediction model and determine whether above-baseline calibrated transfer judgment entails nonzero mutual information about task structure under explicit non-leakage and identifiability assumptions. Prove the strongest valid information lower bound or construct a countermodel and update B01 with project impact; proof, refutation, or demotion are equally valid outcomes. Use the result to decide whether recursive judgment remains in the formal paper. Keep this result independent of the license calculus, Task 14C system assessment, and Checkpoint C1 empirical status: mutual information or calibrated self-judgment does not by itself license the judging system without grounded external evidence.

- [ ] **Task 23 — Design the policy-to-value interpretability bridge.**

  Create `notes/policy_value_interpretability.md` only at the strength surviving Tasks 22 and 22A. Use `policy_value_isomorph` as a conditional companion case study, not evidence for universal policy–value equivalence. Specify how a reconstructed `V` or `Q` could feed the Task 11A-selected license/comparison interface and expose domains, margins, local rules, transitions, abstentions, and traces. Separate behavioral/value fidelity, domain validity, representational alignment, causal faithfulness, and human inspectability. Propose probes, shared/independent encoder comparisons, counterfactual rankings, interventions/ablations, shift coverage, and readability tests; allocate each test to this repository, the companion repository, or future work. Without representational or causal evidence, place the bridge in a companion/future-work section rather than the core contribution set.

- [ ] **Task 24 — Build the counterexample and limitations section.**

  Create `notes/limitations.md` as a paper-selection matrix rather than a laundry list. Group boundaries under roughly six headings: finite scope/search; evidence, calibration, and trusted bases; recursion/self-reference; representation, scale, and boundaries; atlas/routing/shift; and policy/value/interpretability/truth. For each retained limitation record the failed assumption, theorem/counterexample/experiment witness, surviving claim, overall project impact, and proposed main-text/appendix/future-work location. Cover the existing required cases, including modeled-loss regress, checker versions, ungrounded cycles, dual-use and rectification limits, architecture/comparator fairness, nonstationarity, activation misalignment, and surrogate-without-mechanism, without repeating equivalent caveats under multiple names.

- [ ] **Task 25 — Freeze the paper’s claims and outline.**

  Create `paper_outline.md`. Select only claims supported by completed proofs, verified literature, or experiment results. Freeze a **core contribution set** of roughly three to five headline contributions separately from standard formal integrations, optional extensions, empirical results, policy/value case study, and interpretive motivation. Separate the architecture-neutral semantic interface, ReLU reference construction, and comparative evidence; do not imply architectural uniqueness or optimality. Freeze (a) a roughly 500-word plain-language answer to “Why this value-logic design, and why hope it fits neural networks?”, (b) a 100–150-word abstract-level version, and (c) one running succession example inherited from Task 19. Put the fallback-derived origin of `epsilon` before the classifier/ReLU example. Write a section-level outline with word budgets, theorem/evidence dependencies, notation cross-check, countermodel map, main-text versus appendix allocation, and a cut list that removes claim IDs, audit banners, historical record taxonomies, routine lemmas, and unused extension machinery. Add a Substack skeleton using physics succession → fallback/`epsilon` → open licenses/overlap → classifier/dual-use margin → limits.

- [ ] **Checkpoint D — Reassess the roadmap before drafting the public artifacts.**

  Create `notes/checkpoints/D_predraft.md` and apply the checkpoint protocol. Audit whether the formal, neural, experimental, policy/value, counterexample, and interpretability results support a coherent paper of the intended scope and whether the outline answers the lead question as a human argument rather than inventorying the repository. Revise Task 26 onward, including the paper structure and any missing proof/experiment repair tasks, before prose drafting begins. Treat the frozen claim set as a proposed publication contract, not an obligation to preserve unsupported ambitions.

- [ ] **Task 26 — Draft the formal paper’s motivation and related work.**

  Begin `paper.md` with title, abstract, and a human-readable introduction answering the frozen lead question. Open with theory succession and reliance before final truth; derive and delimit `Pi(M,D,epsilon)`; explain external and fallback-derived tolerance before neural notation; then introduce the running example, relative model granularity/finite recursive composition, modeled loss, the Task 11A judgment choice, the representation question, licensed covers versus activation complexes, contributions, scope, and related work. Preserve the necessary distinctions among truth, adequacy/usability, finite relative comparison, current use/selection, and retention without pretending the terminology or neural factorization was forced from the outset. State that ReLU compatibility was engineered at a typed boundary, mathematically delimited, and remains empirically testable.

- [ ] **Task 27 — Draft the formal definitions and semantics sections.**

  Extend `paper.md` with the compact `E,Q,S` signature, world index, typed atoms/refinement, profiles, `WF + K_3`, diagnostics, consequence/update, read/write locality, comparison/use, and open-ended stages, using the Task 19 running example throughout. Distinguish object-model results, internal value-logic derivations, metatheorems, and empirical certificate judgments. Present the finite certificate-carrying plan extension `(payload, grade, evidence)`, proof erasure, license lifting, grounded provenance, and stratified system assessment; distinguish all of these from cyclic fixed points and unrestricted reflection. Keep the main definitions sufficient to follow later results; put detailed record, reason, footprint-key, and atlas taxonomies in appendices or repository references.

- [ ] **Task 28 — Draft the theorem and proof sections.**

  Extend `paper.md` with only the settled theorem spine and representation results, including countermodels where stronger desired claims fail. Lead with the most explanatory characterizations, impossibilities, quantitative bounds, and exact representation conditions across the profile/stability/update-locality, proof-carrying/stratified-system, and Task 17 clusters. Present transport/routing and imported database, fixed-point, and program-logic machinery as standard mathematics integrated with the calculus rather than automatic novelty. Give every displayed result a short significance paragraph tied to the running example. Do not headline Task 17's coordinate-complete minimality; lead that cluster with the equal-code separator, boundary collision, scale obligation, and positive exact construction. Move routine results and full proofs where appropriate to appendices.

- [ ] **Task 29 — Draft the neural representation, learning, and interpretability sections.**

  Extend `paper.md` in reader order: architecture-neutral contract; ReLU reference witness; three grouped boundary families—continuity/seams, information/boundary/scale, and evidence/open-endedness; learning/calibration objective; exact mask and router; supported comparator evidence; and transparency grades. Cover query quotient versus audit diagnostics, atom statistics, uncertainty bands, external registry/provenance, flat versus DAG records, payload versus grade versus certificate, and task criterion versus estimator versus optimization loss. Present dual-use activations only after accepted evidence and exact state. Reuse Task 16's canonical `A and I and C` vector and calculation to distinguish arbitrary positive activation, predicted positive margin, accepted certificate-relative atom surplus, full license, and later ranking, together with the supported-equality and zero-quarantine counterexamples. State that discrete-code bits do not imply width, neural scores do not imply proofs, and a hard MoE result says nothing beyond its seam hypothesis. Include system and policy/value material only at the frozen evidence grades.

- [ ] **Task 30 — Draft the experiment, discussion, and conclusion.**

  Complete the substantive draft of `paper.md` by reporting the confirmatory minimum core first, followed by separately labeled powered extensions, deterministic integration witnesses, exploratory analyses, philosophical interpretation, interpretability implications, limitations, future work, and conclusion. Include negative/null outcomes and project-impact consequences, four-way/status and risk–coverage failures, update/retention and seam behavior, and only scoped architecture comparisons. Separate functional fidelity, marginal calibration, certificate validity, routed risk, and activation alignment. Do not let synthetic success imply scientific realism, mechanistic transparency, system self-certification, or ReLU optimality.

- [ ] **Task 31 — Perform the mathematical and citation audit.**

  Check every symbol, definition, theorem dependency, proof, numerical claim, and citation. Verify compact/elaborated signatures; Task 14B footprints and phantom/frame positioning; Task 14C certified constructors, proof erasure, grade propagation, license lifting, ground sources, evaluator ranks, and Kripke–Fitting boundary; criterion/estimator/training-loss/certificate separation; and fixed-point claims. Audit calibration-record binding, blocked split roles, guarantee levels, query families, tags, bits versus width, inclusive boundaries, dual-use joint sufficiency and scale covariance, Task 17's consumer-relative minimality, proof/checker versions, empirical-versus-deductive evidence, universal approximation, and architecture comparisons. Ensure no reason taxonomy or record schema is primitive without necessity; classify results honestly and confirm three paper-carrying clusters survive. Require primary sources or repository proof/experiment and authoritative physics. Optionally add the current ReLU depth-lower-bound frontier only if it sharpens a claim actually retained. Update the ledger and propagate every `X1` impact into `paper.md`; Task 33 must carry the surviving impacts into the later Substack adaptation.

- [ ] **Task 31A — Perform the reader and narrative audit.**

  Read the technically audited paper as a first-time mathematically literate reader. Check that the lead question is answered before machinery, fallback-derived `epsilon` appears early, every section answers a human question, every headline theorem has a significance sentence, notation is introduced before use, and the single running example remains visible. Remove claim IDs, task language, audit banners, duplicated qualifications, inventory-style transitions, and implementation schemas not needed for the argument. Check the promised three-to-five contribution spine, main/appendix allocation, word budgets, and the future Substack adaptation. Record the audit and repairs in `notes/reader_audit.md`.

- [ ] **Checkpoint E — Reassess publication readiness after the full audit.**

  Create `notes/checkpoints/E_publication_readiness.md` and apply the checkpoint protocol after both the mathematical/citation and reader/narrative audits. Decide whether any failure requires returning to proof, experiment, interpretation, or drafting work; revise Task 32 onward and add narrowly scoped repair tasks if needed. Proceed to Gist/Substack formatting only if the surviving claim set is mutually consistent, supported, and consumable by a human reader.

- [ ] **Task 32 — Make `paper.md` Gist-compatible and publication-ready.**

  Verify GitHub Markdown and math delimiters, links, references, figures, Unicode, heading structure, and copy/paste behavior. Remove repository-only scaffolding from the public artifact and confirm that appendix/repository links do not interrupt the main narrative.

- [ ] **Task 33 — Produce the Substack adaptation.**

  Create `substack_post.txt` from the audited paper. Use plain prose, Unicode where helpful, at most one displayed formal judgment, no Markdown formatting, and the running concrete example. Follow the narrative order physics succession → fallback/status quo and the origin of `epsilon` → open licenses, overlap, and revision → classifier/dual-use ReLU intuition → the explicit correction from positive activation to predicted margin to certificate-relative surplus to full license → hybrid boundaries and results. Adapt Task 16's canonical adequacy/improvement/constraint example in plain language so zero, open, refuted, missing, surplus, full license, and post-license ranking remain concrete without reproducing the full vector table. Carry every surviving claim-ledger `X1` impact from the audited paper into this adaptation. Retain the ReLU-reference-not-uniqueness caveat and all other necessary qualifications without introducing claims absent from the paper.

- [ ] **Task 34 — Cross-check and finalize both artifacts.**

  Compare `paper.md` and `substack_post.txt` claim by claim using an explicit crosswalk, proofread both, lint Markdown and plain-text constraints, verify references/figures/copy-paste behavior, ensure the repository resume instructions are current, and record the final completion note here.

## Decision log

- **2026-07-10 — Loss-first rather than vectors-first.** The current preferred neural route derives adequacy targets from observed or simulated loss, then learns embeddings that predict that loss. Hand-labeled capability/demand vectors remain allowed as an interpretable baseline, not as evidence of discovery.
- **2026-07-10 — “License” is provisional terminology.** It usefully emphasizes defeasibility and scope, but Task 5 may rename the calculus if the term conflicts with established usage.
- **2026-07-10 — No commitment to cross-entropy yet.** Cross-entropy remains a requested baseline. The semantics may require multi-label or selective objectives, so Task 18 owns the final choice.
- **2026-07-14 — Structured statistics are primary; atom cross-entropy is the baseline.** The reference learns externally defined atom statistics with standardized center–radius regression and an interval score, then uses disjoint held-out residual calibration and the exact evidence checker. Independent three-way atom cross-entropy measures frozen-state fidelity but does not authorize use or preserve quantitative tolerance transfer by itself.
- **2026-07-14 — Calibration, learned rejection, and routing cannot self-license.** A predicted radius or confidence is a proposal until a versioned mode/checker accepts a held-out record. Learned validity may only withhold. Selection is trained separately over the exact active set, and selective risk is always reported with coverage and fallback-inclusive deployed risk.
- **2026-07-14 — Checkpoint C accepts the hybrid blueprint, not an end-to-end neural logic.** Address-local learned statistic/payload/grade proposals, proposal-bound accepted calibration, exact evidence and `WF + K_3`, profile masking, routing/fallback, and independently ranked system audit form one implementable chain. The scorer never receives target-bearing oracle fields, and the accepted-envelope record must bind scorer, split, scope, polarity, checker, version, and provenance.
- **2026-07-14 — The minimum empirical core spends no power on an architecture alternative.** The confirmatory core tests `F35/H18.1`, registered marginal calibration `F36/H18.2`, the core succession fixture, and ablations 1–3 and 5. Hard MoE is the sole optional comparator because it addresses the seam theorem, and it proceeds only after a separate powered gate; monotone/lattice and graph/set comparisons are deferred.
- **2026-07-14 — Experiment roles are blocked and sequential.** Train, envelope calibration, reject/router validation, system audit, and final confirmation are separated by latent world/trajectory, provenance root, and plan family. Task 19 preregisters numerical margins and power; Task 19A builds/pilots/freezes the generator; Task 20 implements learners; Task 21 runs once; Checkpoint C1 propagates the result before interpretation.
- **2026-07-14 — The decisive experiment is preregistered before its generator exists.** `F35` requires 5-point structured-arm superiority on both tolerance-transfer and boundary fidelity plus in-regime noninferiority within 2 points; `F36` separately tests 90% marginal target-in-proposal coverage with a 2-point lower-bound tolerance. Independent worlds, not rows or fit seeds, determine power. Boundary cases are deliberately oversampled and exactly reweighted. Infinite conformal proposals count for the marginal coverage estimand but remain unusable evidence and force the atom Open; finite-usable coverage is selected/descriptive. No final world may be inspected before the Task 19A pilot and protocol freeze.
- **2026-07-14 — Marginal overlap does not prove the narrative dominator.** `N`'s displayed loss/latency intervals overlap `O/S`; Task 19A therefore binds separate paired-difference certificates whose upper endpoints are strictly negative. Finite dominance follows from those paired comparisons over the exact evaluated set, not from the marginal table or an assumption of independence.
- **2026-07-14 — Power planning does not invent pre-learner effects.** A generator-only pilot cannot estimate the paired fidelity variance of Task 20 learners. The frozen calculation uses disclosed worst-coupling bounds at the registered design alternatives, not proxy-arm output presented as data. The 2-point in-regime guard binds at 4,925 worlds and rounds to the 5,000-world cap. Hard MoE is omitted prospectively, the system tier is deterministic-witness-only, and production/final payloads remain ungenerated.
- **2026-07-14 — Calibration samples worlds, not correlated atom rows.** The Task 20 held-out expansion takes one target-blind, target-weighted eligible probe per schema from each calibration world, producing 5,000 independent scores per group. Treating all within-world probes as exchangeable calibration units would inflate the effective sample size and weaken the intended world-blocked evidence boundary.
- **2026-07-14 — Boundary and transfer macros respect scalar monotonicity.** The boundary endpoint uses four explicitly constructed query positions because continuous strict strata almost surely do not land at the exact `.25` cutoff. Tolerance shifts realize 16 rather than 24 schema/state/offset cells: negative shifts cannot create Supported in the excluded direction, and positive shifts cannot create Refuted in the opposite direction. These structural zeros are reported, not imputed or manufactured.
- **2026-07-14 — Matched learning shares capacity and initialization without sharing authority.** Both arms use one common selected parameter budget, matched within 0.8%, and a paired common-prefix trunk initialization with isolated head streams. Learned centers, radii, logits, shadow uncertainty, and self-confidence remain proposals or predictions; exact evidence/checker state, `WF + K_3`, the active mask, and fallback alone authorize production use.
- **2026-07-14 — The Task 20 run is armed but the final result remains unseen.** The frozen entry point verifies source/config hashes, refuses the wrong Task 21 token or any existing run artifact, and writes a fit-complete marker before it can call the final generator. A 16-world pilot-role smoke fit established code execution and exact reproducibility only. All production payloads remain ungenerated and `F35/F36` retain `T0` status.
- **2026-07-14 — The public narrative starts from reliance and the origin of tolerance.** The order is physics succession, fallback/status quo and `epsilon_F=J_q(F_q)-Delta`, finite-stage open licenses, then the classifier/ReLU intuition. Neural fit is described as deliberately engineered at a typed interface and mathematically delimited, not inevitable and not the claim that every continuous target is CPWL.
- **2026-07-14 — Theorem positioning follows the same standard as theorem correctness.** Task 17's minimality is coordinate-complete-consumer-relative and will not be headlined. Task 14B's negative reads/frame rule are positioned against predicate-lock and separation-logic patterns, and Task 14C's deferred cyclic checklist against Kripke–Fitting semantics, without importing their source theorems into value logic.
- **2026-07-10 — No commitment that usefulness equals truth.** The project studies bounded, revisable warrants. Any stronger metaphysical conclusion must be separately argued.
- **2026-07-10 — `Pi(M,D,epsilon)` is a derived compression, not an unexplained primitive.** `M` and `D` come directly from domain-sensitive theory succession; loss and `epsilon` enter when pragmatic comparison is turned into an operational reliance threshold. The loss, confidence, purpose, agent, and stage indices may be suppressed only after they are fixed.
- **2026-07-10, corrected and emphasized 2026-07-14 — ReLU sign has inherited, not intrinsic, semantics.** For `z=ReLU(s)`, positivity unconditionally says only `s>0`. A named predicted margin makes it predicted positive slack; a conservative margin bound to accepted evidence makes it certificate-relative strict surplus for one atom. Neither is a full profile license or target-world truth. The original “licenses the positive branch” wording is superseded, and `F18` also falsifies zero-as-quarantine: only exact evidence/state/profile evaluation and the active mask authorize or quarantine downstream use.
- **2026-07-14 — Reuse one canonical neural-semantics witness.** Task 16's `A and I and C` example combines loss adequacy, improvement over fallback, and latency. It is the project-wide source for negative/support/refutation margins, boundary zero, open/refuted/missing/invalid states, normalized surplus, exact vector encoding, fixed ReLU conjunction over decoded support bits, full license, and post-license ranking. It distinguishes learned heads from coordinates and fixed decoder channels: multiple channels per atom are generally necessary, two rectified channels are still insufficient, and one shared vector-valued interval head may serve several atoms. Tasks 19, 29, and 33 extend or adapt this witness rather than invent new meanings.
- **2026-07-10 — Keep two atlas notions separate.** The intrinsic polyhedral atlas of a ReLU function is a structural representation result. A scientific-model atlas additionally requires model identity, domain licensing, routing, approximate bridge conditions, and possibly gaps. Alignment between them is a central theorem/experiment target.
- **2026-07-10 — Policy-to-value reconstruction is environment- and return-relative.** The companion repository correctly reconstructs `V^pi`/`Q^pi` using game dynamics, terminal utilities, perspective conventions, and sampled states. The general target is therefore reconstruction from a policy coupled to this background structure, not from network weights or action outputs alone.
- **2026-07-10 — Behavioral reconstruction is the first interpretability grade, not the last.** Independent value regression can explain decisions extensionally while missing the policy's internal mechanism. Claims of transparency must report behavioral fidelity, domain validity, internal alignment, causal faithfulness, and human inspectability separately.
- **2026-07-10 — The companion implementation is a case study, not yet a shared architecture.** Its current MLPs use `tanh` and separate raw-state encoders; this project studies a ReLU license calculus. Task 23 will design principled cross-project tests before proposing architectural changes to the companion repository.
- **2026-07-10 — Falsifiability precedes truth-likelihood labels.** Task 1 classifies how a claim could be tested, proved, or refuted and whether it is currently operational enough to do so. “Likely false” or “refuted” requires verified prior research on a sufficiently similar claim or a project-supplied proof, countermodel, counterexample, or experiment. Positive and negative claims use the same evidential standard.
- **2026-07-10 — `epsilon` has two principal sources.** A tolerance may be externally imposed by safety, precision, law, or task requirements, or endogenously induced by a fallback/status quo: `epsilon_B(D)=J(B,D)-Delta`. These sources can coexist and must not be conflated.
- **2026-07-10 — Separate adequacy, improvement, and admissibility.** A candidate can beat the fallback while remaining unsafe, or meet a hard adequacy threshold without being worth its cost. Comparative “best known” status is a third, library-relative condition. The provisional full `Pi` is their factored conjunction, not a primitive scalar test.
- **2026-07-10 — Preserve the zero boundary explicitly.** Inclusive adequacy uses signed margin `s>=0`, whereas `ReLU(s)>0` implements a strict inequality and maps equality together with failure to zero. The provisional recommendation is to retain the signed score for the formal comparison and use ReLU only to propagate positive slack.
- **2026-07-10 — Structured semantics precede vectorization.** `Pi` begins as a typed record whose model, domain, loss, tolerance, evidence, fallback, library, and provenance fields need not share a semantic type. Neural encoders may compress these fields, but the project must state which target each encoding is sufficient for.
- **2026-07-10 — Preserve component and signed margins.** A combined minimum can implement conjunctive licensing but loses failure identity; ReLU loses all nonpositive magnitudes. The preferred transparency interface retains `(s_hard,s_base,s_library,...)` and either the signed preactivations or paired channels `(ReLU(s),ReLU(-s))`, while routing content only through licensed positive channels.
- **2026-07-10 — Loss-derived embeddings identify competence profiles, not unique ontology.** Bilinear and neural encodings admit basis transformations and observational equivalence classes. Coordinate-level semantic claims require constraints, probes, stability tests, or causal interventions beyond held-out loss prediction.
- **2026-07-10 — Domain encodings are risk-relative.** A vector sufficient for expected risk can discard tails or geometry required for worst-case and safety claims. The domain representation must be evaluated against the exact risk functional it supports.
- **2026-07-10 — Scientific domains form an overlapping partial cover, not necessarily disjoint tiles.** Multiple models may be simultaneously licensed on a large overlap, may share the same domain, or may leave uncovered gaps. Active-set cells record exactly which model subset is licensed without forcing a single winner.
- **2026-07-10 — Separate three atlas-like geometries.** The scientific licensed cover stores model-indexed domains and evidence; a ReLU network has an intrinsic activation complex of affine restrictions; a router induces a selection partition. Alignment may be many-to-one and must be proved or tested.
- **2026-07-10 — Bridges are typed relations, not universally exact seams.** Scientific overlaps may support exact, approximate, statistical, decision-equivalent, asymptotic, translated, or unresolved bridges. Continuous blending does not require exact equality when outputs are commensurate, but numerical smoothness does not justify mixing incompatible ontologies.
- **2026-07-10 — Retention is broader than selection.** A lower-error model can be selected while the older adequate model and its domain remain stored for fallback, comparison, interpretation, changed tolerances, or future revision.
- **2026-07-10 — Open-ended atlases require expandable memory.** A fixed finite ReLU MLP represents only a finite-stage library. The provisional open-ended implementation is a sequence of finite scorers/routers coupled to an external or expandable model/domain/evidence registry.
- **2026-07-10 — The target is a license logic, not a replacement truth semantics.** The central judgment is a finite-stage, evidence-relative permission to rely on an instantiated model for a typed domain and purpose. The formalism remains neutral on whether truth exists or whether usefulness constitutes truth.
- **2026-07-10 — “Logic” carries a minimum formal burden.** The proposal must define a typed language, semantics, consequence, update/defeat rules, metatheoretic results, and a neural correspondence. A loss function or score by itself is not enough.
- **2026-07-10 — Neural fit has four grades.** Representability, empirical learnability, semantic/interpretive alignment, and open-ended implementation are separate claims and must be reported separately.
- **2026-07-10 — Indefinite succession means extendable finite stages.** Every finite record/library may be revised or extended without an internal finality rule. The project does not require a fixed network to store an actual infinity or require every revision sequence to converge.
- **2026-07-10 — The posts determine motivation, not warrant.** Their recurring progression from action/value before certainty through recursive evaluation to recovered structure and policy/value reconstruction will guide exposition. Public technical claims still require definitions, proofs, experiments, or verified literature.
- **2026-07-10 — The project is a synthesis across fields, not an instance of one existing logic.** Nonmonotonic logic supplies defeasible consequence; selective prediction supplies abstention/risk–coverage; MoE supplies routing; CPWL theory supplies finite ReLU representation; formal learning supplies convergence without known arrival; intertheory work supplies bridge distinctions; and IRL supplies identifiability limits. None supplies the complete license calculus.
- **2026-07-10 — Selective prediction is the main precedent for fallback-induced `epsilon`.** Reject/deferral costs can induce an operational acceptance threshold and require reporting risk jointly with coverage. The license system must extend this with multiple simultaneous models, retention, provenance, and domain bridges.
- **2026-07-10 — Truthlikeness and adequacy remain different projects.** Formal truthlikeness compares theories relative to a truth target or world similarity. The proposed license can compare task performance without assuming that the truth is in the library or that loss measures truth distance.
- **2026-07-10 — GL is conditional, not foundational.** Löb and Solovay results apply to formal provability under strong derivability/self-reference assumptions. A finite-stage empirical license modality is not a provability predicate unless a later formal translation proves that it is.
- **2026-07-10 — ReLU representation claims are finite and typed.** Exact CPWL representation is supported under explicit scalar/vector, domain, architecture, and layer-count conventions. It does not imply SGD learnability, semantic atlas alignment, efficient size, or open-ended storage.
- **2026-07-10 — Reward/value recovery is only partial without extra structure.** Policy behavior generally identifies equivalence classes rather than a unique reward/value or mechanism. Additional environments, discounts, state information, assumptions, and causal tests may narrow the class; behavioral agreement alone remains the weakest transparency grade.
- **2026-07-11 — The core signature is many-sorted and task-typed.** Cases, predictions, outcomes, and actions are dependent on a task; a shared neural vector does not erase those semantic types.
- **2026-07-11 — A domain is a typed evaluation scope with multiple views.** Its carrier set, optional measure/sampler, task family, contextual conditions, frame, coverage evidence, and provenance remain distinguishable. A risk specification declares which views it requires.
- **2026-07-11 — `EvalSpec(D,L,rho)` types risk and tolerance.** Loss is pointwise; `rho` aggregates it over declared domain views. `epsilon` lives in the resulting preordered risk space, allowing scalar, vector, interval, or Pareto thresholds without overloading domain identity.
- **2026-07-11 — Model identity is intensional and versioned.** Two models may be behaviorally equivalent on a domain while remaining different entries because their theory references, frames, derivations, costs, robustness, or provenance differ.
- **2026-07-11 — Records retain corrections rather than overwriting history.** A finite stage record is a versioned event structure; corrections/retractions change admissible evidence views while preserving an audit trail. Record heredity still does not imply license monotonicity.
- **2026-07-11 — Library and search closure are finite and relative.** A stage-local library is not the model universe. Search traces must state scanned/retrieved/evaluated sets, budgets, stopping reasons, and failures before supporting a no-recorded-dominator claim.
- **2026-07-11 — The base object language has no global truth or finality predicate.** Model-local satisfaction is allowed, while `TrueTheory`, `Final`, `Complete`, or `BestPossible` require separately defined meta-level semantics in later tasks.
- **2026-07-11 — Phase checkpoints are first-class work items.** Checkpoints occur after the finite-stage foundations, core metatheory, neural blueprint, claim/outline freeze, and final audit. Each consumes one prompt, reviews all remaining work prospectively, may restructure the unfinished roadmap, records its rationale in `notes/checkpoints/`, and stops before executing the revised next task.
- **2026-07-11 — Finite-stage assessment has four operational outcomes.** A request is granted, refused by valid contrary evidence, withheld because evidence/search/comparison is insufficient, or undefined because it is ill-typed/non-executable. Only grant makes `Lic` obtain; the other outcomes must not collapse into “false theory.”
- **2026-07-11 — Target, empirical, and certified adequacy are separate.** `Adeq` concerns target risk, `EmpAdeq` concerns a recorded finite evaluation, and `CertAdeq` requires a valid certificate connecting evidence to the target under an explicit statistical or deterministic interpretation.
- **2026-07-11 — Certificate semantics is mode-specific.** Frequentist coverage is not a posterior probability; Bayesian posterior support is assumption-relative; conformal coverage certifies only its declared target; and calibration tests do not become risk guarantees without a proved implication.
- **2026-07-11 — Full reliance requires an explicit fallback.** `NoFallback` is permitted for adequacy-only judgments but makes the unqualified full use-license ill formed. Reject, defer, status quo, information gathering, or another model/policy must be represented and compared.
- **2026-07-11 — The full license is a factored grant.** Hard certified adequacy, certified improvement over fallback, hard constraints, search-relative admissibility, and provenance must all pass. Narrower permissions must use an explicit requirement profile or a different judgment name.
- **2026-07-11 — Relative evaluated closure is the default.** A finite search can grant only that no certified dominator was found among validly evaluated candidates in the recorded scope. Declared-library closure is stronger and can be withheld by timeouts or omissions; global closure is unavailable without a completeness theorem.
- **2026-07-11 — Gaps force fallback rather than forced choice.** When the active licensed model set is empty, a safe selector must reject, defer, preserve a stated status quo, gather information, or use a separately licensed fallback. Raw argmax over unlicensed models is forbidden.
- **2026-07-11 — Evidence defeat splits rebuttal from lapse.** A countercertificate can refuse a former license; invalidation, expiry, or scope failure without contrary evidence merely withholds it. Discovery of a dominator defeats admissibility but not automatically hard adequacy or archival retention.
- **2026-07-11 — Consequence has three layers.** Model-local object consequence may be classical within one typed context; fixed-stage license consequence is monotone over explicit premises; current consequence is defeasible because its active-license and admissible-evidence base is recomputed at each stage.
- **2026-07-11 — License application yields labeled reliance, not bare truth.** A current license plus a permitted model derivation supports `MayUse`, `LicensedOutput`, or `MayRely` with the model/version/frame/task/domain/purpose/record context intact. There is no truth-detachment rule that erases this label.
- **2026-07-11 — Contradictions are quarantined by typed context.** Differently labeled contrary outputs produce disagreement or decision conflict, not explosion. Cross-context inference requires a valid restriction, bridge, equivalence, or approximate transport witness with its error retained.
- **2026-07-11 — Domain transport is risk-specific.** Set inclusion alone does not move an expected-risk license to a subset, union, intersection, point domain, or extension. Every required domain view and certificate must be reconstructed; uniform worst-case restriction is a conditional special case.
- **2026-07-11 — Raw heredity is not belief or license monotonicity.** Corrections and retractions append to raw history while changing admissible evidence views. Certificates, current licenses, and selections can lapse or be refused without deleting their historical derivations.
- **2026-07-11 — Updates are dependency-directed and stratified.** Primary events feed admissible views, then certificates/comparisons, licenses, and routing. Impact cones permit conservative persistence outside changed dependencies and widen for library/search changes that may affect many admissibility judgments.
- **2026-07-11 — Request records are immutable and versioned.** Changing a domain, tolerance, confidence convention, fallback, cost/purpose, model, or provenance creates a linked new request. Expiry is derived from explicit validity/review conditions, and branch merges recompute rather than union current licenses.
- **2026-07-11 — Dominance compares eligible use plans under a named profile.** Coverage and fallback behavior belong to a model–gate–fallback plan, not always to the bare model. Hard adequacy and safety filter candidates before scalar or Pareto comparison.
- **2026-07-11 — Scalar and Pareto dominance remain distinct.** Scalar dominance requires an explicit monotone scalarization and policy-chosen units/weights. Pareto dominance is the strict product preorder over typed loss, coverage-deficit, cost, robustness, or other coordinates and allows multiple frontier members.
- **2026-07-11 — Certified dominance requires joint uncertainty treatment.** Every required coordinate must be comparable under a mode-correct joint certificate, with a fixed strict witness for Pareto defeat. Missing or boundary-straddling coordinates yield `Unknown`, not a win for either entity.
- **2026-07-11 — Target and operational frontiers differ.** The default relative `UFront` means no certified dominator was found in the evaluated eligible set; `CFront` additionally resolves every required comparison. Neither implies a global Pareto frontier over unknown future models.
- **2026-07-11 — Retention has four layers.** Archive, hard-adequacy, competitive/frontier, and selected-now retention are different. Competitive retention equals the Pareto frontier only under a complete profile/comparison policy; dominance never entails archive deletion.
- **2026-07-11 — Comparison granularity is explicit.** Whole-domain aggregate dominance, declared-cell dominance, and uniformly certified pointwise dominance are different claims. Aggregate superiority can coexist with reversal on a subdomain.
- **2026-07-11 — Partial domination is not automatically a split.** A local/refinement policy must construct a measurable partition, risk-appropriate child measures/samplers or dynamics, corrected certificates, fallbacks, boundaries, and provenance. Without `SplitReady`, local routing is withheld or the valid aggregate policy remains unchanged.
- **2026-07-11 — Comparative defeat is narrower than adequacy revocation.** A dominator can supersede selection on a whole domain or cell while the incumbent remains adequate elsewhere or archived. Adequacy revocation requires a same-scope countercertificate; archive removal requires explicit authorization.
- **2026-07-11 — Falsifications require project-impact propagation.** Every `X1`/refuted ledger disposition must state its significance for the central goal, affected formal/neural/empirical/public artifacts, surviving narrower claim, and roadmap consequence. This prevents a locally falsified premise from remaining silently embedded elsewhere.
- **2026-07-11 — Licensed model cover is weaker than literal atlas structure.** The base scientific object is a finite partial overlapping cover of typed model/use-plan charts. “Scientific licensed atlas” additionally records bridges, seams, routing, frontiers, and provenance; differential-geometric status requires separate manifold and transition-map axioms.
- **2026-07-11 — Overlaps and seams are different.** An overlap may be a thick region with several active charts; a seam is a hard/soft routing transition. The atlas keeps coincident, nested, overlapping, gap, and unknown regions without forcing a disjoint scientific tiling.
- **2026-07-11 — Bridge obligations are operation-specific.** Exact, approximate, statistical, decision, asymptotic, translation-only, incompatible, unknown, and not-required statuses are distinct. Comparison, transport, blending, and gluing require different evidence; overlap alone does not force a successful bridge.
- **2026-07-11 — Approximate stitching is not exact gluing.** Bridge errors compose, triple-overlap path defects must be audited, and convex blends are new use plans with their own semantic, safety, and certificate obligations. Smooth output does not establish theoretical equivalence or truth.
- **2026-07-11 — The atlas router is not one CPWL predictor.** Scientific chart scopes, router selection regions, and ReLU activation cells are separate geometries. Hard expert routing may be discontinuous even with affine experts, whereas an ordinary finite ReLU output is continuous CPWL; exact identification needs seam agreement or an external discrete decision.
- **2026-07-11 — The physics bridge example is deliberately narrow.** Newtonian and relativistic normalized kinetic-energy predictors admit an explicit low-speed approximation bound on a shared observable. This does not make the full theories diffeomorphic charts, establish ontological equivalence, or certify final truth.
- **2026-07-11 — Checkpoint A identified a license/comparison ambiguity, resolved by Task 11A.** An older model may remain in actual use under another purpose, profile, subdomain, resource regime, or fallback role; it may instead remain merely adequate or archived after a successor is preferred. Task 11A compared layered, parameterized-profile, and strong-license designs without presupposing separate predicates, and selected mandatory profile-indexed `Lic_P` with named reliance and preference profiles.
- **2026-07-11 — Checkpoint A narrows the publishable core.** Four-way assessment, scalar typed risk, explicit fallback, labeled reliance, current-view update, gaps, finite comparison status, and extendable stages form the core candidate. Pareto/splitting, full bridge/atlas machinery, certificate-mode instantiations, merges, and policy/value reconstruction are typed extensions or optional case studies.
- **2026-07-11 — Open-endedness will use continuation semantics, not Löbian analogy.** Task 12 must distinguish extendability, stabilization, known stabilization, and finality with explicit continuation countermodels. GL/Löb enters only through a genuine derivability/fixed-point translation; “anti-Löbian” is not an inherited theorem.
- **2026-07-11 — Neural work targets structured finite-stage outputs.** The reference architecture must preserve signed component margins, derived four operational outcomes, simultaneous/empty usable sets, comparison and selection information according to Task 11A's chosen design, fallback, atom-indexed diagnostics, and registry/provenance pointers. A fixed finite baseline and expandable external-memory design are both required.
- **2026-07-11 — Synthetic domains must be independent of learned regions.** The decisive experiment must generate semantic scopes before training and measure scientific-cover, router-partition, and activation-complex alignment separately, avoiding circular success definitions.
- **2026-07-11 — Licensing is profile-indexed.** The primitive judgment is `Lic_P`; omitting `P` is `Undefined(MissingLicenseProfile)`. Profiles contain finite required and report-only atoms, so common “basic reliance” and “preferred use” language becomes readable aliases rather than competing primitives.
- **2026-07-11 — Comparison strength is explicit rather than universal.** `P_rely` does not require library closure; `P_pref-rel` requires no certified dominator in the evaluated set while disclosing unknown pairs; `P_pref-cert` withholds until every relevant comparison is resolved. Neither establishes global optimality.
- **2026-07-11 — Selectors declare their required license profile.** Actual use of an older model can remain valid under another purpose, subdomain, resource regime, fallback role, or weaker justified profile even when a stronger comparison profile fails. Usable-but-unselected and archive-only retention remain distinct.
- **2026-07-11 — Updates and stability are profile-local.** Evidence changes propagate through every profile requiring the affected atom; comparison changes alter required or report-only comparison atoms without automatically rewriting adequacy or unrelated profiles. Current license sets, active sets, and Task 12 stability claims must carry `P`.
- **2026-07-11 — Universal license closure is a superseded default; forced closure is falsified.** E05 is `D1` when read as a proposed universal definition and `X1` only when read as the formal claim that every coherent action-authorizing interface must include a no-better-model clause. The supported replacement is that profiles authorizing comparative preferred use must name finite search/closure strength. This is an interface and claim-discipline correction, not a threat to the project goal.
- **2026-07-11 — World facts and finite-stage status are separate semantic sorts.** Core models are pairs `<W,S>`: population/target adequacy belongs to `W`, while certificates and operational licenses belong to `S`. Soundness must state a certificate-mode bridge rather than treating an agent's stage structure as an oracle.
- **2026-07-11 — Literal profile inclusion is provisional.** Task 11A's required-atom set inclusion order is too sparse for parameterized real requests. Task 13 must define atom entailment/refinement and lift it to profiles before claiming useful antitonicity.
- **2026-07-11 — Aggregate status is derived, diagnostics remain primary.** Neural work will supervise atom assessments and derive the four-way tag symbolically. `Diag` or a lossless safety projection is mandatory because aggregate precedence can hide a certified safety reason in presentation.
- **2026-07-11 — The integrated witness becomes executable before open-endedness.** Task 11B is a semantic reference implementation and test suite, not early neural experimentation. It must make every Task 11A table, transition, scope, and provenance dependency machine-checkable.
- **2026-07-11 — The original recursive-judgment promise must be cashed or demoted.** Task 22A will prove, refute, or narrow B01 in a latent-task information model before the formal paper can use that motivation as more than speculation.
- **2026-07-12 — The paper core targets three carriers, not 28 primitive sorts.** Use plans `E`, reliance contexts `Q`, and epistemic states `S` form the principal carriers; worlds index target semantics, profiles are finite requirement families, and the detailed Task 7 records become a typed elaboration/implementation layer. Compression may not erase the listed scope, evidence, comparison, transport, or provenance distinctions.
- **2026-07-12 — Status uses `WF + K_3`.** Failed well-formedness yields `Undefined`; meaningful atoms are refuted, open, or supported and aggregate by meet to `Refused`, `Withheld`, or `Granted`. Flat reason codes derive from atom identity and witnesses/obstacles/provenance rather than extending the core syntax.
- **2026-07-12 — Formal weight comes from a theorem spine.** The project now targets distinct stability, update/profile/diagnostic, transport/routing, and neural representation clusters. At least three paper-carrying results from different clusters must survive; definitions and standard lemmas do not count by relabeling.
- **2026-07-12 — Model atomicity is interface-relative and recursion is disciplined.** A core use plan `e` may elaborate to a finite DAG of object/idealization, frame, formulation, solver, evaluator, and action choices; extensionally equal outputs do not identify plans when cost, robustness, or trace differs. `L_q` names the criterion, while a model estimating consequences or loss is another plan component or evidence procedure and may receive a higher-order license. Finite well-founded nesting is allowed. Cyclic self-reference requires explicit fixed-point/iterative semantics and is not silently admitted by the core.
- **2026-07-12 — ReLU is the reference witness, not the unique or presumptively ideal architecture.** ReLU MLPs are retained because their affine margins, threshold gates, finite CPWL representation, and activation complexes make the logic-to-network correspondence unusually explicit. The semantic output contract is architecture-neutral. Checkpoints B and C must decide whether a narrowly chosen alternative better tests monotonicity, routing, variable libraries, composition, calibration, or interpretability; universal approximation alone is not evidence of semantic fit, efficiency, learnability, or transparency.
- **2026-07-12 — Update persistence is observable- and realizability-relative.** A dependency/impact graph that contains every real influence makes path absence sufficient for invariance. The converse requires every reachable observed coordinate to have a realizing allowed update path that changes the exact observable. Full diagnostics, ternary values, public status, and continued grant need separate influence views.
- **2026-07-12 — Diagnostic cardinality follows the realizable query quotient.** Supported profile queries induce the coarsest sufficient quotient on the realizable atom-vector set `V`. Singleton profiles distinguish all of `V`; the `3^n` and `ceil(n log_2 3)` worst case additionally requires full ternary independence. `WF` and its derivation occupy a separate tagged channel.
- **2026-07-12 — Operational safeguards are not automatic world guarantees.** Atom support needs a mode-specific `<W,S>` bridge before it becomes a target fact; gap fallback prevents unlicensed expert use but does not certify the fallback as harmless; and finite component grants do not certify a composite plan without direct evaluation or a propagation theorem. These countermodels preserve the central finite-stage logic while limiting public and neural claims.
- **2026-07-12 — Free expected-risk restriction is almost-sure pointwise control.** A parent mean at most `epsilon` transports to every positive-measure measurable subdomain iff loss is at most `epsilon` almost surely. Aggregate, prespecified-cell, selected-subset, pointwise, and uniform certificates remain different evidence modes.
- **2026-07-12 — Router risk is selection- and severity-aware.** Deployed risk separates correct-route, misroute, fallback, and coverage terms. A whole-cell mean cannot be rescaled by selected mass without selection-aware or pointwise evidence, and misroute frequency without loss severity does not bound harm.
- **2026-07-12 — Bridges and compositions carry quantitative condition numbers.** Prediction disagreement controls task risk only through a typed regularity bridge such as Lipschitz loss; component error propagates with downstream path sensitivities and tube-valid premises; resource costs use declared aggregators. Exact group-valued pairwise bridges yield global frames only when all cycle products are identity.
- **2026-07-12 — Checkpoint B clears the neural transition only after one focused repair.** Profile relative completeness is the cleanest current logic result; the stability package needs precision and classical positioning; and the update characterization needs a concrete atom-locality/change-completeness theorem. Task 14B was the only new repair gate and was completed 2026-07-14; broader metatheory remains rejected unless a later consumer exposes a specific gap.
- **2026-07-12 — Future influence is keyed, not exhausted by present provenance nodes.** `Dep_s(r,a)` over existing history explains the current diagnostic but cannot see future inserts, invalidations, or dominators. The canonical persistence interface uses atom query/read footprints, including negative reads and validity closure, plus event write footprints; conservative impact paths remain sufficient, while necessity requires observable-specific behavioral realizability.
- **2026-07-12 — Neural fit means statistic factorization plus symbolic decoding.** The preferred learned module predicts dependency-scoped certificate/risk statistics and calibrated uncertainty. Exact metadata, mechanically decidable `WF`, profile roles, `K_3` meet, active-set masking, fallback, registry identity, and provenance normally remain symbolic or external.
- **2026-07-12 — Query quotients constrain codes, not neural width.** Exact consumers require codes that refine the relevant `V/~_F` and well-formedness observations; full diagnostics require a finer interface. Finite bit counts apply to discrete codes, not unrestricted real output dimensions, and `Ill/Well` is a canonical decodable normal form rather than a mandated internal datatype.
- **2026-07-12 — Task 17 is designed before Tasks 15–16.** Its frozen targets are architecture-neutral quotient factorization, robust recovery away from uncertainty boundaries, exact finite ReLU computation of CPWL statistics with an external decoder, the conforming-polyhedral hard-seam iff/obstruction, and the fixed-output versus expandable-library limitation.
- **2026-07-12, resolved at Checkpoint C 2026-07-14 — Architecture alternatives require distinct hypotheses.** Monotone/lattice models are nominated only for proved monotone coordinates, and hard MoE only for seam discontinuity/routing. The neuro-symbolic decoder is shared infrastructure and maxout is presently redundant. Task 15 operationalizes the graph/set amendment, but Checkpoint C selects no comparator for the minimum core, retains only hard MoE behind an optional seam-power gate, and defers the other alternatives.
- **2026-07-12 — The experiment will be tiered rather than omnibus.** Core succession, status, evidence, fallback, and model-addition claims receive the primary tier; routing/seams form a second tier; composition, bridge, and fallible-estimator stressors are optional. Functional fidelity, calibration, and risk–coverage are confirmatory, while activation/domain alignment remains exploratory.
- **2026-07-12 — Proof-assistant and notation automation are deferred, not rejected in principle.** A narrow Lean formalization may be reconsidered at Checkpoint D after the distinctive neural theorems are known. The Task 13 glossary remains canonical; a notation script is not a current proof gate and should wait for a stable machine-readable grammar.
- **2026-07-12 — Recursive plans should compute and carry evidence.** The ambitious composition target is an annotated executor returning a computational payload, a quantitative grade/bound, and a checked certificate/provenance term. Typed plan constructors transform all three; a composite certificate may lift to a license, but component grants never compose by simple conjunction.
- **2026-07-12 — Curry–Howard is a bounded analogy.** Formal proof terms and internal value-logic derivations may receive a Curry–Howard-style reading in a named theory/checker. Empirical confidence, calibration, and neural margins are certificate-mode evidence, not unconditional proofs of target-world facts. Task 14C has now separated object results, internal derivations, metatheorems, and empirical certificate judgments and verified the boundary against primary sources.
- **2026-07-12 — Dual-use ReLU activations are conditionally coherent.** If a channel is tied to a named hypothesis and its preactivation is a commensurate, certificate-valid adequacy margin—or a certified approximation with boundary separation—the rectified positive certificate-relative surplus can also be a downstream feature. Arbitrary hidden activations do not inherit this meaning; inclusive boundary support and open/refuted/missing information require additional channels; and one adequacy scalar is computationally sufficient only when the declared downstream content factors through it.
- **2026-07-12 — Margin magnitude carries units and scale obligations.** A downstream computation using positive adequacy slack must declare normalization or covariance under allowed rescalings of loss and tolerance. Multiplying arbitrary payloads by a ReLU margin defines a new plan and is not a semantics-free gate.
- **2026-07-12 — Value logic may assess a frozen implementation of itself only stratifiably.** Reify the implementation/checker as `e_VL` under an independently fixed `q_VL`, with grounded base evidence or lower-ranked verifiers. Its self-endorsement is not its certificate. Ungrounded cycles and unrestricted reflection remain outside the finite core without explicit fixed-point semantics.
- **2026-07-12 — Task 14C is an author-directed pre-encoding theorem task.** It follows the narrow audit repair in Task 14B and precedes Task 15 so the neural interface is designed around proof-carrying plans, theorem levels, and grounded system assessment rather than retrofitted afterward.
- **2026-07-14 — Typed locality closes the Checkpoint B repair gate.** Every frozen slot evaluator is a deterministic function of a finite typed read projection, negative reads use collection indices that future insertions must write, and the canonical event-to-key-to-slot graph is change-complete. Disjoint writes preserve complete diagnostics; absence of a path is necessary only when the reachable influence is realizable for the exact observable and update class. Current provenance remains explanatory rather than a complete future-influence map.
- **2026-07-14 — The pre-neural three-cluster theorem bar is cleared without novelty inflation.** Profile relative completeness counts on an instantiation fiber, the stability package counts only as a classically positioned project synthesis, and typed locality makes the update cluster calculus-specific. Diagnostic quotients and Task 14A remain supporting bridges/integration; Task 17 subsequently supplies the scoped neural representation/seam cluster.
- **2026-07-14 — Finite recursive plans admit checked annotation and erasure.** For a finite typed plan DAG with total deterministic payload/grade transformers, independently accepted primitive evidence, and locally sound certificate transformers, topological induction constructs a checked root bundle `(payload, grade, certificate/provenance)` whose erasure is the ordinary computation. Task 14A's path-sensitivity recurrence is a concrete grade transformer. Only the root composite certificate lifts into `WF + K_3`; component grants never substitute for it.
- **2026-07-14 — System assessment is grounded and ranked, not reflective.** In the finite DAG fragment, every zero-premise support must be a typed accepted base and every supported required atom traces to such a base. Strict finite evaluator ranks plus total deterministic local evaluators yield a unique assessment of a frozen `e_VL`. Self-endorsement is not target-factive, same-rank cycles are outside this theorem, and any cyclic extension must declare its operator, fixed-point selection, and soundness theorem.
- **2026-07-14 — Atom encodings are address-local and profile-consumed.** The exact architecture-neutral input is an instantiated atom address plus its canonical Task 14B dependency projection. Profiles form addresses and assign required/report/safety roles; once an address is fixed, changing only its role cannot change the atom value. Learned modules propose statistics and envelopes, while exact metadata, missingness, checker/certificate/provenance, `WF`, `K_3`, masking, and fallback remain external.
- **2026-07-14 — Status compression is consumer-relative.** `WF` observations plus `V/~_F` are sufficient only for the declared status family. Audit and named future-profile consumers retain unquotiented address-indexed diagnostics, safety roles, statistics/envelopes, and exact evidence handles over a declared horizon. No finite code is claimed sufficient for arbitrary future queries, and the discrete status bit count is not a neural-width bound.
- **2026-07-14 — Variable libraries share scorers, not identities.** The fixed indexed baseline is finite-registry-specific. A candidate-conditioned scorer can serve a growing external registry equivariantly, but cold-start accuracy and lossless indefinite compression do not follow. Sparse pair evaluation records only requested comparisons; certified non-domination for all candidates may still require quadratic evidence.
- **2026-07-14 — Plan structure is a falsifiable but deferred comparator hypothesis.** Flat and typed-DAG encodings both retain exact plan, loss-estimator, checker, and version identity. The explicit graph is motivated when equal observed payloads hide differences in sharing, cost, robustness, grade propagation, or explanation and must be invariant under exact typed DAG isomorphism. Checkpoint C defers the trained comparison until many independent structural separators can power it; deferral is not an empirical verdict.
- **2026-07-14 — ReLU proposes regions; accepted calibration licenses their use.** The scalar reference head outputs a center, nonnegative predicted half-width, uncertainty score, and conservative-validity proposal. A separately versioned calibration/checker path supplies any accepted error radius and polarity. The symbolic decoder applies inclusive support, strict refutation, and open uncertainty bands; a learned score is never its own evidence.
- **2026-07-14 — Dual-use channels are named and normalized.** A positive ReLU activation may be both certificate-relative adequacy surplus and a downstream feature only when its preactivation is the registered conservative margin and the channel registry names address, domain, normalization semantics/scale, accepted calibration/checker versions, and consumers. This is a constructed interpretation, not a property discovered from activation sign alone. The exact support bit and diagnostic remain beside the activation; separate payload/grade channels are the default.
- **2026-07-14 — Licensing precedes selection.** Production routing is restricted to the exact profile active set, uses an explicit tie rule, and returns the named fallback on gaps. Aggregate-status, flat-reason, self-grant, unmasked argmax/softmax, and predicted-certificate heads may appear only as disconnected ablations.
- **2026-07-14 — ReLU zero is not logical quarantine.** The explicit counterexample `3 ReLU(-10)+2+5=7` falsifies the unqualified inherited claim that a zeroed failed unit supplies non-explosion or authorization safety. Exact symbolic masking is the supported replacement. This correction strengthens the hybrid architecture and experiment design without weakening the core logic or the positive-margin representation program.
- **2026-07-14 — Alternative architectures keep one semantic interface.** A monotone/lattice head is eligible only for learned coordinates with proved monotonic targets; tolerance monotonicity already belongs to the decoder. Hard MoE is eligible only for discontinuous-seam hypotheses and retains route/fallback/risk evidence. The typed-DAG hypothesis remains optional. Checkpoint C selects no alternative for the minimum core and makes hard MoE the sole optional comparator behind a separate power gate.
- **2026-07-14 — Exact representation factors through the declared consumer.** A code realizes the public profile family exactly iff equal codes preserve the required `WF` observation and `V/~_F` class; audit consumers use a finer quotient. `Ill/Well` is a decoded normal form, and finite discrete-bit counts do not bound real neural width.
- **2026-07-14 — Conservative recovery has two error scales.** Statistic error propagates to a raw boundary radius `r`; interval decoding is always sound, while uniform exact recovery of the ideal sign is guaranteed outside the `2r` ideal-margin band. One-sided modes withhold the unsupported polarity.
- **2026-07-14 — ReLU represents CPWL quantities, not the exact license machine.** Global finite CPWL statistic and proof-erased payload/grade maps receive exact finite ReLU realizations. `WF`, evidence validity, inclusive boundaries, direct `K_3`, profile meet, masking, fallback, and certificates remain external.
- **2026-07-14 — Hard affine seams have an exact gluing boundary.** A finite conforming hard assembly is continuous exactly when adjacent expert traces agree on every relevant common face. Disagreement at a two-sided accumulation face is a discontinuity obstruction for ordinary ReLU outputs; an external hard router remains possible.
- **2026-07-14 — Named state-plus-surplus channels have a minimal scoped theorem.** Exact atom states together with normalized positive margins form the minimal quotient for the coordinate-complete consumer family and may feed declared downstream layers. Equal adequacy with unequal payload, supported equality at zero, and unnormalized rescaling defeat stronger scalar interpretations.
