# Final Public-Artifact Crosswalk

Completed: 2026-08-01

## Durable result

The technically audited [`paper.md`](../paper.md) and reader-facing
[`substack_post.txt`](../substack_post.txt) express the same claim set at
different levels of detail. The Substack adaptation introduces no theorem,
empirical result, causal attribution, architecture comparison, or policy/value
identification claim absent from the paper. The final pass added an explicit
definition of “value logic” and made the omitted hard-MoE, empirical seam,
architecture-ranking, powered-system, matched-coverage-replacement, and
confirmation-rerun boundaries explicit in the adaptation.

## Claim crosswalk

| claim or boundary | technical paper | Substack adaptation | final disposition |
|---|---|---|---|
| Meaning of value logic | §3.1 defines it as the logic of task-relative reliance value and keeps truth/value and true-utility questions separate. | The opening now gives the same definition immediately after introducing a license. | Aligned. Neither artifact identifies truth with utility. |
| Central finite-stage object | §§1–3 use a versioned plan, context, epistemic state, and profile; a license is present, scoped authorization. | “What I Mean by a License” explains the same four-part request in ordinary language. | Aligned; no minimal-tuple claim. |
| Atom and public states | §3.2 separates `Supported/Open/Refuted` from `Undefined/Refused/Withheld/Granted`. | “What Happens When the Models Overlap?” gives the same states and distinct next actions. | Aligned, including inclusive favorable equality and malformed-before-evidential ordering. |
| Profiles, selection, and retention | §§3.2–3.3 keep reliance, finite comparison, active-set selection, and archive retention distinct. | The overlap discussion separately treats permission, finite-set preference, routing, and preservation. | Aligned; no global-optimality claim. |
| Open succession and local revision | §4 proves scoped refinement, constructive stability regimes, the finite-prefix barrier, open-library direction, and typed locality. | The narrative explains the same constructive uses and the polarity correction to the earlier instability slogan. | Aligned; the adaptation does not present eventual stability as known arrival or semantic finality. |
| Composition, routing, and fallback | §5 requires a checked root plan, gives routed-risk and path-sensitivity bounds, and denies automatic fallback safety. | The middle sections give the `.06+.06` composition separator, read-footprint rule, selected-subdomain warning, fallback-severity warning, and path amplification. | Aligned; component grants and parent averages are not transported automatically. |
| Neural-fit answer | §6 motivates signed margins, exact finite-CPWL ReLU realization, and changed-threshold reuse while keeping exact evidence, decoding, masks, and fallback external. | “Why Signed Margins Looked Useful,” “From an Activation to a License,” and the conclusion give the same constructive answer. | Aligned; ReLU is a reference witness, not a uniqueness, efficiency, learning, alignment, or mechanistic theorem. |
| Aggregate `F35` result | §7.2 calls the result mixed because the registered components have decisive opposing effects and the reverse aggregate rule also fails. | “The Experiment Was Genuinely Mixed” explicitly says mixed means large opposing effects, not noise or low power. | Aligned; aggregate disposition remains inconclusive under the frozen conjunction. |
| `F35a` changed-tolerance transfer | §7.2 reports `.9436` versus `.7570`, difference `+.1866`, interval `[.1860,.1873]`, and supports the registered superiority component. | The experiment section reports the same numbers and no-retraining, tolerance-conditioned scope. | Aligned; neither artifact claims literal invariant-region reuse or isolates the retained statistic causally. |
| `F35b` boundary result | §7.2 reports `.5196` versus `.7808`, difference `-.2612`, interval `[-.2636,-.2587]`, refuting registered structured superiority at its margin. | The adaptation reports the same numbers and explains that the observed reverse advantage was not a preregistered reverse-superiority claim. | Aligned. |
| `F35c` in-regime result | §7.2 reports `.7764` versus `.8773`, difference `-.1009`, interval `[-.1022,-.0997]`, refuting registered noninferiority. | The adaptation reports the same values, `.02` allowance, and descriptive status of the reverse advantage. | Aligned. |
| `F36` proposal coverage | §7.3 reports `J=.9098` and `T=.9044` with their intervals and Holm lower bounds, only for marginal target-in-proposal coverage. | The adaptation reproduces the values and excludes conditional, profile, route, deployed-loss, safety, and world-truth readings. | Aligned; coverage is not operational usefulness. |
| Descriptive population references | §7.3 distinguishes target prevalence `.35/.65`, target-weighted fallback `.9962/.9139`, target-weighted issued-Granted mass `.0038/.0861`, and unweighted conditional Granted accuracy `.0124/.1811`. | The experiment narrative gives the same four populations and labels. | Aligned. Issued-Granted mass is not recall; `.0124/.1811` are not target-weighted recall. |
| Unavailable weighted conditional result | §§7.3 and E.3 state that compact traces omit the weights needed for target-weighted conditional Granted accuracy or a target-weighted outcome decomposition. | The adaptation states the same missing-data boundary immediately after the conditional figures. | Aligned; no reconstruction is implied. |
| Noncausal pipeline comparison | §§7.3–7.4 say the two complete pipelines differ without identifying the contribution of objective, fit, radius, calibration, threshold construction, decoder, or generator. | The adaptation explicitly rejects an identified “wrapper effect” and lists the same possible sources. | Aligned; the dead-band account is theory-consistent, not a causal decomposition. |
| C1 no-claim boundaries | §§7.4, 10.2, and E.4 report no outcome-selected replacement, no hard-MoE result, no architecture ranking, no powered empirical system claim, no activation-alignment result, and no matched-coverage rescue. | The final experiment pass now states all six boundaries together. | Aligned. Deterministic system fixtures remain integration witnesses only. |
| Policy/value existence boundary | §8 and Appendix F prove exact finite encoder-image existence for a declared action code and decoder, while standard-return semantics needs an environment/return contract. | “The Policy–Value Bridge” preserves the finite isomorphism and separately explains `Q`, scalar-`V` harness, identification, and practical reconstruction. | Aligned; difficulty recovering a natural value does not refute existence, and existence does not identify true utility. |
| Recursive-judgment information | §8 and Appendix F prove the Bayes-baseline log-loss identity and mediation-qualified transfer to the outcome-identifiable task quotient. | The adaptation states the `δ`-nat result, arbitrary-baseline regret correction, mediation condition, and leakage/copying limits. | Aligned; it is not presented as interpretability, ontology, or system authorization. |
| Interpretability evidence | §8 reports seven non-collapsible axes and says alignment, causal, human, off-support, and trajectory evidence remain unmeasured. | The policy/value section names the same seven axes and says success on one does not upgrade the others. | Aligned; the bridge remains prospective beyond its finite and conditional results. |

## Formatting and publication checks

- `paper.md` remains the LaTeX-heavy, GitHub-compatible technical artifact;
  the repository verification suite checks the known unsupported math
  fragments and local links.
- `substack_post.txt` remains a prose adaptation with one standalone equation,
  the requested italicized AI-assistance credit, and exactly two public
  Markdown links.
- Both artifacts retain the same running succession, fallback-derived
  threshold, inclusive support convention, explicit fallback, and
  architecture-neutral authorization boundary.
- The public artifacts contain no unresolved TODO placeholder and no claim-ID
  syntax from the internal ledger.

The original paper roadmap is complete. Future work is controlled by
[`TODO_v2.md`](../TODO_v2.md).
