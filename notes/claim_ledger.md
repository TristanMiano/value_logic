# Inherited Claim Ledger

Created: 2026-07-10  
Task: TODO Task 1  
Status: Living falsifiability ledger; later task results and scoped adjudications are recorded row by row.

## Purpose

This ledger extracts the major claims inherited from the six local source files and the inspected `TristanMiano/policy_value_isomorph` repository. Its purpose is to make each claim precise enough to research, prove, refute, or test later. It is not a ranking of which claims sound plausible.

Positive and negative statements receive the same treatment. A universal denial is not accepted merely because it sounds cautious, and an ambitious positive statement is not rejected merely because it sounds strong.

## Status vocabulary

### Role

- `MOT`: motivating observation or intuition.
- `DEF`: proposed definition or choice of formalism.
- `FORM`: mathematical/logical proposition or conjecture.
- `EMP`: empirical hypothesis.
- `INT`: interpretive or philosophical thesis.
- `LIT`: purported established result that must be checked against primary literature.
- `IMPL`: claim about the current companion implementation.

### Testability

- `F`: formally falsifiable by a proof, counterexample, or countermodel once terms are fixed.
- `E`: empirically falsifiable after an operationalization, dataset, and metric are fixed.
- `Lit`: addressable by identifiable research literature.
- `U`: underspecified but potentially testable after reformulation.
- `UF`: likely unfalsifiable in its present formulation. This is a diagnosis of wording, not a truth verdict.

### Evidence state

- `U0`: source assertion only; no evidence checked.
- `C0`: source contains a construction, derivation, or toy example that has not been independently verified.
- `L0`: source cites or names relevant literature, but the attribution/result has not been verified.
- `R0`: companion repository contains code or tests bearing on the claim, but they were not run in this task.
- `T0`: a test or proof strategy is proposed below but has not been performed.

Later tasks may add `S1` (supported within a stated scope), `X1` (falsified within a stated scope), `I1` (tested but inconclusive), or `D1` (a proposed design/default was superseded for stated reasons rather than truth-evaluated). These dispositions are now assigned row by row as proofs, countermodels, experiments, verified literature, or explicit design comparisons become available. `D1` must not be used to disguise a falsified formal or empirical claim.

## Adjudication rule

Every row states what later evidence would count as:

- **support**: evidence for the scoped formulation, not for an unrestricted neighboring claim;
- **falsification**: a verified counterexample/result against the scoped formulation;
- **inconclusive**: an outcome that fails to distinguish the claim from alternatives.

“Likely false” or “refuted” may be added only after a sufficiently similar claim is verified as falsified in the literature or after this project supplies a valid proof, countermodel, counterexample, or experiment.

### Required project-impact analysis for falsification

Every row receiving `X1`, “falsified,” “refuted,” or an equivalent negative disposition must include a **Project impact** statement in its adjudication cell. That statement must explain:

1. whether the result threatens the central research question, a supporting mechanism, a theorem's scope, an empirical plan, terminology, or only an optional embellishment;
2. which existing or planned artifacts, definitions, proofs, experiments, neural outputs, or public claims must change;
3. which narrower claim or alternative mechanism survives, if any;
4. whether the TODO roadmap needs a new repair task, reordered dependency, checkpoint question, or no structural change;
5. the significance for the final paper and Substack post—especially whether the falsification should be a central limitation/result or a brief qualification.

The impact analysis must not soften a genuine falsification. Its purpose is to propagate the result through the project so that a locally rejected claim is not silently used elsewhere.

## Source key

- `JVR`: `posts/judgement_value_fact_recovery.txt`
- `UAYN`: `posts/utility_all_you_need.txt`
- `HCW`: `posts/condensed-response-hidden-complexity.txt`
- `UPLNN`: `posts/utility_preference_logic_nn.md`
- `GPT`: `llm_convos/chatgpt.txt`
- `FABLE`: `llm_convos/claude.txt`
- `AUDIT-F5`: `llm_convos/claude_audit_2026-07-11.md`
- `PVI`: [`TristanMiano/policy_value_isomorph`](https://github.com/TristanMiano/policy_value_isomorph), inspected at `main` on 2026-07-10

Line references identify the current local files and may move if those source files are edited.

---

## A. Theory succession, adequacy, and truth

| ID | Scoped restatement | Source | Role | Testability | Evidence | Proposed adjudication and outcomes |
|---|---|---|---|---|---|---|
| A01 | There exist scientific model pairs `(M_old,M_new)` and domains `D_small ⊂ D_large` such that `M_new` improves predictive performance on `D_large`, while `M_old` remains adequate on `D_small`. | `JVR:120–145`; `UPLNN:203–273`; project prompt | MOT/EMP | E/Lit | U0 | Compare historical case studies using explicit loss and domain definitions. **Support:** old model remains below tolerance on a documented subdomain while successor expands/improves coverage. **Falsify:** a selected case fails this pattern; that falsifies only the universalized version, not existence. **Inconclusive:** domains or losses cannot be made commensurate. |
| A02 | For a bounded agent at a finite stage, current evidence need not certify that its present best theory is final. | `JVR:136–176`; `FABLE:1–8, 40–52` | MOT/FORM/INT | F after an epistemic model; UF as an unrestricted claim about all agents | C0 | Define stage/refinement semantics and a certificate notion. **Support:** construct frames where every finite record has an admissible defeating refinement. **Falsify:** derive finality under the same assumptions. **Inconclusive:** finality or admissibility remains undefined. |
| A03 | Comparative pragmatic adequacy can rank `M1` above `M0` without asserting either model finally true or false. | `JVR:120–174` | DEF/INT | F/U | U0 | Give semantics where risk/dominance order is independent of a truth predicate. **Support:** coherent models and inference rules produce rankings without `True(M)`. **Falsify:** show the proposed ranking necessarily presupposes the forbidden truth predicate. **Inconclusive:** “works better” remains unanalyzed. |
| A04 | A scalar tolerance `epsilon` should be read as the largest task-relative loss treated as negligible, not as an intrinsic “amount of falsity.” | `GPT:1–169`, especially `8`, `107–165` | DEF | F/E | C0 | Type the loss and decision context, then test monotonicity and calibration. **Support:** the semantics behaves coherently across tasks and units when `L` is explicit. **Falsify:** two uses with identical `(M,D,epsilon)` but different implicit losses yield contradiction, showing the abbreviated signature is inadequate. **Inconclusive:** no operational task is fixed. |
| A05 | One scalar `epsilon` is sometimes insufficient; adequacy may require a loss vector, tolerance vector, partial order, or Pareto criterion. | `GPT:107–119`; `UPLNN:60–69, 111–118`; `formalism/04_dominance_retention.md`, Theorems 2 and 4 | FORM/EMP | F/E/Lit | S1 (formal scope)/L0 | Formally supported for policies that preserve noncompensable typed coordinates: explicit risk/cost vectors make a risk-superior model and a cost-superior model Pareto-incomparable, while different scalarizations reverse the ranking. This proves that a scalar is not forced by the comparison problem; it does not show scalarization is impossible when representation axioms/preferences justify one. Empirical relevance still requires elicited trade-offs. |
| A06 | For an acting agent, decision regret may be more relevant than raw predictive discrepancy, because small prediction errors can have large action consequences and vice versa. | `GPT:120–136`; `UPLNN:86–107` | EMP/INT | E/Lit | C0/L0 | Compare predictive-loss and regret-based model rankings across decision problems. **Support:** regret predicts task performance better or distinguishes decision-critical errors. **Falsify:** within a scoped task class, predictive loss is a sufficient statistic for regret. **Inconclusive:** utility/action model is unobserved. |
| A07 | “Truth is the ideal limit/explanation of the long-run success of increasingly useful theories.” | `JVR:146–176`, especially `152–156` | INT | UF as stated; U/Lit after formalization | U0 | Split into convergence, correspondence, and explanatory claims. **Support:** only relative to a specified formal learning model or philosophical argument. **Falsify:** a formalized convergence theorem fails or a countermodel has stable utility without target correspondence. **Inconclusive:** “truth,” “limit,” or “non-accidental” remains metaphorical. |
| A08 | When an agent has a fallback/status-quo option `B` and a scalar task loss `J`, the fallback can induce a contextual threshold `epsilon_B(D)=J(B,D)-Delta`; the signed margin `J(B,D)-J(M,D)-Delta` measures surplus over the outside option. | User follow-up during Task 2; `notes/motivation_pi_epsilon.md`, §§5, 13–14; `ThomasTheocharousGhavamzadeh2015`; `GhavamzadehPetrikChow2016`; `LarocheTrichelairTachetDesCombes2019` | DEF/FORM/EMP | F/E/Lit | S1 algebra/operational precedent; T0 project empirical | The threshold identity is algebraically exact when candidate and fallback share the same scalar objective. Primary safe-policy-improvement sources verify strong operational precedents: high-confidence lower-bound tests, robust regret relative to a baseline, and local return to a behavioral baseline under uncertainty. Their MDP, return, data, policy-class, and uncertainty hypotheses do not automatically apply to arbitrary model use. **Support:** a project instantiation preserves those assumptions or independently validates calibrated baseline-relative routing. **Falsify:** under the fixed objective it selects candidates that do not beat the fallback. **Inconclusive:** the fallback outcome, common objective, or translation is unavailable. |
| A09 | Beating a fallback and satisfying a hard adequacy/safety constraint are logically independent; a complete use-license may require both. | User follow-up during Task 2; `notes/motivation_pi_epsilon.md`, §6 | FORM/DEF | F/E | C0 | Construct four cases crossing `beats baseline` with `meets hard threshold`. **Support:** examples realize both off-diagonal cases, showing neither condition implies the other. **Falsify:** under added assumptions one condition provably entails the other, restricting rather than universally defeating the claim. **Inconclusive:** the two criteria use incomparable scales. |

---

## B. Recursive judgment and recovery of factual structure

| ID | Scoped restatement | Source | Role | Testability | Evidence | Proposed adjudication and outcomes |
|---|---|---|---|---|---|---|
| B01 | If judges are rewarded for predicting future task performance, and task types have stable differences in skill transfer, then sufficiently successful judges must encode information about those task distinctions. | `JVR:18–45`, especially `32–43` | FORM/EMP | F/E/Lit | C0 | Formalize a latent-task prediction problem and an information lower bound. **Support:** above-chance calibrated judgment entails nonzero mutual information with task structure under stated assumptions. **Falsify:** construct a successful judge satisfying assumptions while independent of task distinctions. **Inconclusive:** performance is driven by leakage or unstable tasks. |
| B02 | Recursively evaluating judges can turn value judgments into measurements of value-relevant factual structure. | `JVR:18–45, 214–252` | INT/EMP | U/E | U0 | Operationalize recursive evaluation, baselines, and recovered structure. **Support:** meta-evaluation improves out-of-sample prediction and recovers known latent factors. **Falsify:** repeated meta-evaluation fails against controls or converges on spurious factors. **Inconclusive:** “measurement” lacks a calibration target. |
| B03 | Independent judgments of “best” objects may cluster around multiple excellence centroids rather than a single optimum, and the clusters can reveal shared latent causal dimensions. | `JVR:47–118`, especially `61–116` | EMP/INT | E | U0 | Use a domain with measurable attributes and independent raters; compare clustering to nulls and causal predictors. **Support:** stable clusters replicate and predict held-out outcomes/attributes. **Falsify:** choices match random/idiosyncratic nulls or clusters fail replication. **Inconclusive:** rater dependence or selection effects dominate. |
| B04 | The task-delineation operation `T` is equivalent to judgment applied to judgment, `J^2`. | `UAYN:75–140`, especially `91, 112, 140` | FORM/INT | UF as stated; F after typed definitions | C0 | Define domains/codomains and an equality/equivalence criterion for `T` and `J∘J`. **Support:** construct mutually inverse translations or prove extensional equivalence. **Falsify:** typed counterexample separates their outputs. **Inconclusive:** `J^2`, “task,” or equality remains self-referential prose. |
| B05 | An improving agent must be able to evaluate or revise value-related representations, so some form of value change is possible in learning or evolution. | `UAYN:143–149` | FORM/INT | F/U/Lit | U0 | Separate meta-preferences, parameter learning, and terminal-value change. **Support:** theorem or model shows self-improvement requires evaluation over value representations under stated architecture. **Falsify:** improving system satisfying scope has fixed value representation and no meta-valuation. **Inconclusive:** any parameter update is defined as “value drift.” |

---

## C. Policy–value correspondence

| ID | Scoped restatement | Source | Role | Testability | Evidence | Proposed adjudication and outcomes |
|---|---|---|---|---|---|---|
| C01 | There is an isomorphism between arbitrary policy representations and utility/value representations over a shared state domain. | `HCW:35–53`; `UAYN:39–61` | FORM/INT | F after defining objects/morphisms; U otherwise | C0 | Specify environment, policy class, value class, equivalence, and maps both ways. **Support:** bijective structure-preserving maps under stated restrictions. **Falsify:** two policies/values violate injectivity, surjectivity, or preservation under those restrictions. **Inconclusive:** “isomorphism” means only informal rationalizability. |
| C02 | Given a value function plus environment dynamics and a decision rule, one can induce a policy by selecting value-maximizing reachable outcomes. | `HCW:47–51`; `UAYN:39–43` | FORM | F/Lit | C0 | State an MDP/game theorem including ties, stochasticity, discounting, and computational limits. **Support:** construction yields a well-defined policy and stated optimality. **Falsify:** scoped assumptions hold but no policy or claimed behavior follows. **Inconclusive:** transition or decision rule is missing. |
| C03 | Normalized state-visitation counts from policy rollouts can serve as the policy's utility values. | `UAYN:45–49`; `HCW:49–53` | DEF/EMP | F/E/Lit | C0 | Compare occupancy measures with return-based values and induced action rankings. **Support:** under explicit restricted dynamics/objective, visitation ordering reconstructs policy preferences. **Falsify:** construct a recurrent/avoided-but-valued state example where occupancy and preference diverge. **Inconclusive:** “value” is defined to mean visitation frequency. |
| C04 | Consistent policy choices imply at least a local revealed-preference ordering over available actions or successor prospects. | `JVR:179–193` | FORM/Lit | F/Lit | U0 | Formalize revealed preference with feasible sets and stochastic choice. **Support:** representation theorem under stated consistency axioms. **Falsify:** policy satisfies claimed consistency but induced comparison is cyclic or context-incoherent. **Inconclusive:** feasible alternatives are not observed. |
| C05 | For a fixed policy `pi`, dynamics, return convention, and state distribution, rollout estimates of `V^pi` or `Q^pi` can train a value network whose greedy choices reproduce a substantial part of `pi`. | `JVR:179–193`; PVI `README.md`, “Project thesis”; `rollout_value.py`; `q_mlp.py` | EMP | E | R0 | Run controlled train/test experiments, including off-policy states and intentionally suboptimal/stochastic policies. **Support:** preregistered action/ranking fidelity and calibration thresholds are met. **Falsify:** failure persists with sufficient data/capacity under scoped conditions. **Inconclusive:** errors could be sampling, optimization, ties, or representation limits. |
| C06 | In deterministic Markov settings with no intermediate reward and a fixed perspective, `Q^pi(s,a)` equals the appropriately signed value of successor state `V^pi(T(s,a))`. | PVI `README.md`, “When do V and Q coincide?”; `DESIGN.md`; `q_mlp.py::estimate_q_pi` | FORM/Lit | F/Lit | C0/R0 | Derive from Bellman equations with explicit sign convention. **Support:** equality follows under all listed assumptions. **Falsify:** a counterexample satisfying every assumption. **Inconclusive:** reward timing or perspective is ambiguous. |
| C07 | Enlarging the state to a sufficient history/belief state can restore a Markov value representation when a coarse visible state loses action-relevant information. | PVI `README.md`, “entire trajectory”; `DESIGN.md`, “When path/history matters” | FORM/Lit | F/Lit | S1 | Supported within the scoped POMDP setting by Smallwood–Sondik (1973) and Kaelbling–Littman–Cassandra (1998): a correctly maintained belief state is a sufficient information state for control under the assumed model. This does not support arbitrary learned history compressions. **Falsify:** proposed enlarged state remains insufficient. **Inconclusive:** “full history” makes the claim vacuous but not compressive. |
| C08 | A full value representation must be at least as complex as the capable agent/policy whose behavior it represents. | `HCW:63–69`; `UAYN:151–169` | FORM/INT | U/F/Lit | U0 | Choose Kolmogorov, circuit, parameter, or sample complexity and an approximation tolerance. **Support:** lower bound reduction from policy computation to value representation. **Falsify:** family with compact value representation and arbitrarily complex induced policy under the same background machinery. **Inconclusive:** complexity is shifted into dynamics/decision procedure. |
| C09 | Frontier language models approximately learn human values because they learn capability/policy-like representations from human-generated data. | `HCW:35, 55–71` | EMP/INT | E/U/Lit | U0 | Define “learn,” target value representation, possession versus prediction, and behavioral probes. **Support:** cross-context, causal evidence of internal value representation beyond imitation baselines. **Falsify:** scoped probes distinguish capability from value and show systematic divergence. **Inconclusive:** outputs alone admit both explanations. |
| C10 | Value-fragility examples based on a short list of terminal values are misleading when the acting system's value representation ranges over the full rich input/state domain. | `HCW:57–71`; `UAYN:151–169` | INT/FORM/EMP | U/F/Lit | U0 | Formalize the fragility examples and compare compact objectives with distributed value models. **Support:** claimed catastrophes require omitted complexity inconsistent with the scoped agent model. **Falsify:** construct a capable rich-input agent with a compact misspecified objective producing the example. **Inconclusive:** objective/optimizer boundary is movable. |
| C11 | A policy alone does not uniquely determine the standard return-based `V^pi`; dynamics, rewards/preferences, perspective, and state/history conventions are additional inputs. | PVI architecture; current `TODO.md` cross-project section | FORM/Lit | F/Lit | T0 | Prove non-identifiability by constructing two environments/reward functions with the same policy and different `V^pi`. **Support:** counterconstruction plus scoped uniqueness conditions. **Falsify:** a claimed policy-only reconstruction theorem under the same unrestricted setting. **Inconclusive:** “value” is redefined as a policy-derived statistic. |

---

## D. Preference logic and optimization as logic

| ID | Scoped restatement | Source | Role | Testability | Evidence | Proposed adjudication and outcomes |
|---|---|---|---|---|---|---|
| D01 | Utility-valued and preference logics evaluate formulas/worlds using ordered desirability objects and define consequence through preservation of value or preference rather than truth alone. | `UPLNN:9–31, 35–118` | LIT/DEF | Lit/F | S1 | Primary sources verify multiple scoped families: Rescher/Van-Dalen measure semantics, ordered-world modal preference logic, and CP-net conditional preferences. There is no single universal preference consequence relation. **Falsify:** the schema misdescribes a named system; revise taxonomy rather than all preference logic. **Inconclusive:** “preference logic” is used too broadly. |
| D02 | Scalar, vector, ordered-world, and uncertainty-plus-aggregation semantics naturally pair with different consequence relations. | `UPLNN:35–118` | DEF/LIT | F/Lit | I1 | Literature confirms the component families, but the exact four-way pairing is a project taxonomy rather than a canonical theorem. Replace “naturally” with explicit design choices and derive each consequence relation. **Falsify:** a proposed pairing conflicts with its stated semantics. **Inconclusive:** naturalness remains stylistic. |
| D03 | Rescher–Van Dalen preference systems form a soundness/completeness ladder corresponding to increasingly constrained measure semantics. | `UPLNN:122–183`, especially `142–175` | LIT | Lit | I1 | Van Dalen (1974), its title, venue, DOI, and focus on variants/completeness are verified. The inherited “ladder” wording and exact system-to-model correspondence were not verified theorem-by-theorem; do not publish that stronger paraphrase yet. |
| D04 | Different decision criteria applied to the same qualitative preference base can yield opposite plan rankings. | `UPLNN:218–273` | FORM/LIT | F/Lit | C0/L0 | Check the umbrella example and general conditions. **Support:** explicit model validates both opposite rankings under distinct criteria. **Falsify:** calculation or preference lifting is invalid. **Inconclusive:** criteria encode extra premises, making “same base” misleading. |
| D05 | A training loss induces a preorder over hypotheses, `f1 >= f2 iff Loss(f1) <= Loss(f2)`. | `UPLNN:442–452` | DEF/FORM | F | C0 | Check preorder properties and equivalence classes. **Support:** reflexivity/transitivity follow for scalar loss. **Falsify:** loss is stochastic/contextual without a fixed comparison relation. **Inconclusive:** “hypothesis” or evaluation distribution shifts during comparison. |
| D06 | Therefore, a neural network trained by loss minimization implements a utility/preference logic whose consequence is improvement under the objective. | `UPLNN:295–334, 442–498` | INT/FORM | U/F/Lit | U0/L0 | Require syntax, semantics, consequence, and correspondence to training. **Support:** define a calculus and prove representation/soundness results. **Falsify:** optimization preorder cannot satisfy the advertised logic under fixed definitions. **Inconclusive:** “logic” means only metaphorical ordering. |

---

## E. Finite-stage licenses, revision, and open-endedness

| ID | Scoped restatement | Source | Role | Testability | Evidence | Proposed adjudication and outcomes |
|---|---|---|---|---|---|---|
| E01 | A basic adequacy judgment can be defined as `Adequate(M,D,L,epsilon) iff Risk_L(M,D) <= epsilon`. | `GPT:1–169`, especially `8–9`; `formalism/08_metatheory.md`, Theorem 19 | DEF/FORM | F/E | S1 formal | The typed core realizes this as membership in `Acc_q`. For fixed plan, scope, loss, aggregation, certificate mode, and certificate region, acceptable-set inclusion proves tolerance monotonicity; in the scalar smaller-is-better case `epsilon_1<=epsilon_2` transports adequacy support. The result does not transfer a full license if fallback, constraints, frame, scope, or certificate mode also change. Empirical estimability and purpose-relevance remain task-specific. |
| E02 | Sampling uncertainty motivates a calibrated form such as `P(Risk_L(M,D) <= epsilon \mid evidence) >= alpha`. | `GPT:137–158`; `VovkGammermanShafer2005`; `ShaferVovk2008`; `notes/literature_core_supplement.md`, §7 | DEF/EMP | F/E/Lit | I1 inherited formula; S1 conformal mode | The displayed conditional probability is not mode-neutral: it looks Bayesian unless a different probability space is supplied. Primary conformal sources support exchangeability-based marginal prediction-set validity at a chosen miscoverage level, not a posterior probability that task risk is below `epsilon`. Keep `epsilon_task`, certificate confidence/failure, and conformal miscoverage separate. **Support:** a named Bayesian, confidence-bound, conformal-plus-risk-translation, or other procedure proves and empirically meets its exact guarantee. **Falsify:** the named guarantee fails under its stated assumptions. **Inconclusive:** probability space, exchangeability, downstream loss, or calibration target remains unspecified. |
| E03 | Adequacy can be nested over theories, built models, frames, idealizations, parameters, solvers, policies, loss estimators, and adequacy judgments themselves. | `GPT:170–487`, especially `197–212, 392–424, 470–487`; user clarifications 2026-07-12; `formalism/07_core_calculus.md`, §§2.1–2.2; `formalism/08_metatheory.md`, Theorems 27–28; `formalism/08a_transport_routing.md`, Theorem 15; `formalism/08c_proof_carrying_plans.md`, Theorem 1 and Corollaries 2–3 | FORM/DEF | F/U | S1 finite proof-carrying DAG/T0 broader recursion | Finite well-founded nesting is now constructively typed: a finite acyclic component graph with matching interfaces, termination, composite output/cost/frame, and provenance can be reified as one core plan; its annotated executor jointly computes payload, typed grade, and a checked composite certificate whose erasure is the ordinary computation. A loss estimator can receive a higher-order request. Task 14A's path-weighted error bound supplies one certificate transformer, but amplification, interface mismatch, correlated error, and resource aggregation remain explicit conditions. Composite authorization consumes the root certificate rather than component grants. Cyclic evaluator/license dependence remains outside the core absent fixed-point semantics. **Project impact:** Task 15 and final exposition must preserve plan structure, payload/grade/evidence, and target loss versus modeled loss versus training loss; the positive result is finite structural recursion, not unrestricted self-reference. |
| E04 | Local error budgets can compose into a global adequacy guarantee when total loss is bounded by the sum of component losses. | `GPT:258–289`; `formalism/08a_transport_routing.md`, Theorem 15 and Corollary 16 | FORM | F/Lit | S1 conditional weighted form | For a finite typed DAG, tube-valid intrinsic errors satisfy `e_v<=delta_v+sum_u L_uv e_u`; unrolling gives an output bound whose weights are sums of products of downstream Lipschitz constants along paths. An outer Lipschitz task loss converts this to excess risk. The simple unweighted sum is valid only when the relevant path sensitivities make it so. Amplification and nominal-input-only certificates are explicit counterexamples. Resource costs require a separately declared aggregator. |
| E05 | An agent-level license includes a closure clause: no better model was found in the retrieved/searched library under the available budget. | `FABLE:1–8`; project `TODO.md` working thesis; `formalism/05a_integration.md`, Proposition 9; `formalism/08_metatheory.md`, Theorem 23 and Countertheorem 25; `AUDIT-F5:C7` | DEF/FORM | F/E | D1 universal design default; X1 forced/global-closure claims; S1 profile-indexed finite replacement | As a proposed definition, universal closure is superseded (`D1`) by the selected profile-indexed interface rather than falsified merely by stipulation. As the narrower formal claim that every coherent action-authorizing interface satisfying the retained requirements must include closure, it is falsified (`X1`) by `P_rely`. Task 14 also gives the two-world/continuation countermodel showing that even supported finite `RelUndom` or `CertUndom` cannot entail global optimality. Supported replacement: a profile authorizing comparative preferred use must state its finite closure strength—`P_pref-rel` requires no certified dominator in the evaluated set, while `P_pref-cert` also resolves every relevant comparison. **Project impact:** This is a central interface correction, not a failure of the project question. `Lic_P`, active sets, selector requirements, neural labels, experiments, and stability claims must carry `P` and exact evaluated/search scope. The correction improves the older-model retention story by allowing comparison changes to affect only profiles that require comparison. The paper and Substack post should present the universal closure default as a rejected design, the forced/global-closure propositions as falsified overstatements, and the profile-indexed finite replacement as a main result. |
| E06 | Licenses have two defeat modes: empirical rebuttal from new records and comparative defeat from discovering a dominator; partial domination causes domain splitting rather than total revocation. | `FABLE:3–8`; `formalism/02_license_semantics.md`, §§16–17; `formalism/04_dominance_retention.md`, Theorems 5–6; `formalism/05a_integration.md`, §§6 and 15; `formalism/08_metatheory.md`, Theorem 23 | FORM/DEF | F/E | S1 profile-scoped; X1 universal comparative/split reading | Evidence rebuttal/lapse and comparative defeat are formally distinct. Task 14 proves the profile-local rule: a new certified dominator refuses profiles requiring the affected comparison atom, changes only diagnostics when comparison is report-only, and leaves a profile with no comparison slot unchanged if its dependencies are preserved. Conditional split-retention holds only when `SplitReady` reconstructs measurable child domains, measures/samplers, certificates, fallbacks, and provenance. The universal readings “every license is comparatively defeated” and “partial domination causes splitting” are falsified. **Project impact:** This removes automatic domain subtraction and profile-independent comparative defeat from the core. Neural/experimental work must preserve profile identity, predict `split-ready` versus `split-withheld`, and route unresolved cases to fallback or information gathering. The final artifacts should present both countermodels and the conditional replacements as substantive results. |
| E07 | A bounded rational agent's retained model set is the Pareto frontier of its retrieved library over coverage, loss, cost, and robustness. | `FABLE:5–8`; `formalism/04_dominance_retention.md`, §§7–8; `formalism/05a_integration.md`, §§5 and 9 | FORM/EMP | F/E/Lit | X1 as stated; S1 under scoped frontier axioms | Falsified in the unrestricted reading because archive retention may include dominated models, hard constraints can exclude resource-undominated models, unknown comparisons enlarge the operational undefeated set, and other retention purposes may preserve non-frontier entries. The characterization holds when “retained” is defined as competitive retention, the eligible finite library and full resource profile are fixed, comparisons are resolved, and policy retains all and only undominated entities. Empirical rationality remains untested. **Project impact:** This rejects a simple universal retention theorem but strengthens the overall architecture rather than threatening the main goal. Archive memory, adequacy/use atoms, profile-indexed undefeated/frontier status, and current selection must remain recoverably distinct fields or outputs; a single Pareto-frontier bit cannot represent all retained knowledge. The external/expandable registry remains necessary even for competitively dominated models, which is important for revision history and interpretability. No new task is required because Tasks 10–11A and the planned neural interface preserve these distinctions; Checkpoint C must verify that the implementation does not collapse them. The paper and post should feature the scoped frontier characterization while presenting the unrestricted version as a falsified overstatement. |
| E08 | A scientific theory library naturally forms an atlas of local model charts whose domains cover supported regimes and whose overlaps require bridge/correspondence conditions. | `FABLE:3–8, 80–221`; `GPT:923–1129`; `formalism/05_atlas.md`, §§1–7 | DEF/FORM/INT | F/E/U | I1 | The project-defined construction is coherent: a finite licensed model cover plus typed overlap, bridge-status, seam, routing, and provenance records represents the intended examples. However, “naturally” is interpretive rather than theorem-level, and overlap does not universally require a successful bridge—archival storage may need none, while comparison, transport, blending, and gluing impose different obligations. Use `licensed model cover` as the weakest term and treat the richer atlas as an explicit design. |
| E09 | Internal finite-stage certification and external eventual stability are distinct operators; the former may have Löb-like behavior while stability over open-ended refinement is non-Löbian. | `FABLE:9–52`; `formalism/06_open_endedness.md`, §§4–7 and 11; `formalism/08b_audit_repairs.md`, §5; `Artemov2008`; `FaginHalpern1988`; `Wald1945`; `DarlingRobbins1967`; `Robbins1970` | FORM/LIT | F/Lit | S1 separation; D1 Löb framing | Task 12 gives exact continuation semantics and countermodels separating current assessment, pathwise eventual stability, permanent current stability, and scheme-relative certified stability. Task 14B repairs the statistical joint index and adapted stopping-time regime theorem and classifies the finite-prefix/sequential-decision steps as classical patterns, not novelty. Justification logic verifies that explicit evidence calculi exist, awareness logic verifies formal treatments of limited representation, and sequential-analysis sources supply neighboring stopping/confidence-sequence machinery; none transfers a world-sound stability theorem to empirical licenses. A deterministic atom can be certified stable under an enforceable freeze invariant, while an actually stabilizing statistical path can lack any sound finite arrival certificate. The Löb/non-Löb wording is superseded (`D1`): no arithmetized provability translation, derivability conditions, or diagonal fixed-point machinery has been constructed. |
| E10 | No procedure in the intended open-ended system should certify `Final(M)` or `Complete(Library)` from a finite stage. | `FABLE:5–10, 40–52, 80–84`; `formalism/06_open_endedness.md`, Theorems 1 and 5, Corollary 4; `formalism/08b_audit_repairs.md`, §5; `Kelly1996`; `Wald1945`; `FaginHalpern1988`; `HeifetzMeierSchipper2006` | DEF/FORM/INT | F after formalization; UF as a universal metaphysical claim | S1 conditional; U0 unrestricted | Supported under explicit continuation assumptions: no sound stage-local procedure certifies permanent current stability when the same finite state has a status-changing compatible continuation; no sound complete-library certificate exists when every finite state admits a proper relevant `AddModel` extension. Task 14B makes the zero-error result an adapted finite regime-declaration theorem; a trajectory claim needs additional almost-sure coverage and shrinkage. Kelly/Wald supply classical context rather than the project theorem, while awareness/unawareness sources support the structural represented/unrepresented distinction. The unrestricted metaphysical denial remains unsupported and likely unfalsifiable as worded. Absence of `Final` from the base syntax is only non-expressibility by design. Deterministic freeze certificates show why this result must not be broadened into “no finite-stage stability claim is ever certifiable.” |
| E11 | Licensed model domains may overlap with positive extent, nest, or coincide; adequacy is therefore naturally set-valued rather than a single winner label. | User guidance during Task 4; `notes/atlas_questions.md`, §§3–7; `formalism/05_atlas.md`, §4 | DEF/FORM/EMP | F/E | S1 formal/T0 empirical | The finite-stage semantics permits simultaneous licenses and explicitly represents coincident, nested, and positive-extent overlaps through active chart sets. No adequacy axiom forces a unique winner; dominance and selection are separate. Whether set-valued retention empirically improves comparison/fallback behavior remains for the planned experiments. |
| E12 | For finitely many licensed domains, the sets of points having exactly each active model subset form an active-set decomposition; with two models this yields the two exclusive regions, their overlap, and an optional uncovered gap. | User guidance during Task 4; `notes/atlas_questions.md`, §4; `formalism/05_atlas.md`, Theorem 1 | FORM | F | S1 | Proved for finite ordinary set-valued use scopes: every point has one unique membership index set, so the nonempty active cells partition the target scope. Unknown/undefined or graded applicability requires the generalized status-cell construction rather than being forced into the Boolean theorem. |
| E13 | Scientific model bridges need not be exact equality; useful overlap relations include approximate predictive, statistical, decision-equivalent, asymptotic, translated, and explicitly unresolved bridges. | `notes/atlas_questions.md`, §§8–10; `FABLE:215–221`; `formalism/05_atlas.md`, §§6–8; `formalism/08a_transport_routing.md`, Theorems 11–12 and 20 | DEF/FORM/EMP | F/E/Lit | S1 formal/T0 empirical | Each bridge kind has a typed target, direction, scope, certificate, and provenance. Exact gluing, approximate prediction deviation/composition, Lipschitz bridge-to-task-risk and blend bounds, and the exact group-valued cycle/potential characterization are proved under their distinct hypotheses. Translation-only, incompatible, unknown, and not-required statuses prevent manufactured continuity. Which bridge types and task regularity constants are empirically useful remains task-specific. |
| E14 | A finite-stage scientific atlas may be partial: if no model is licensed on a region, the gap should be represented explicitly and routed to fallback, information gathering, or abstention. | `notes/atlas_questions.md`, §§3, 11, 19–20; `formalism/05_atlas.md`, Proposition 5; `formalism/08_metatheory.md`, Theorem 20; `formalism/08a_transport_routing.md`, Theorems 5–6 | DEF/EMP | F/E | S1 operational design/T0 empirical | A certified empty active set cannot produce `UseChart`; it must use the explicit fallback, information action, or abstention. Task 14A's routed-risk decomposition makes fallback mass and loss an explicit global-risk term rather than treating abstention/status quo as free. Certified gap and unknown applicability remain separate. Target-world safety of `F_q` still requires its own certificate, and empirical harm/calibration remain experiments. |
| E15 | In an open library, comparison atoms cannot receive sound permanent-stability certificates whenever each stage admits a history-preserving valid dominator extension. | Task 12 roadmap target; `formalism/06_open_endedness.md`, Theorem 5 and Proposition 6 | FORM | F | X1 polarity-unrestricted; S1 directional replacement | The polarity-unrestricted claim is falsified by a persistent existing dominator: a refuted relative-undefeated or certified-undominated atom remains refuted in every continuation even while new models are added. The supported replacement is directional: when the comparison atom is currently supported and a validly evaluated dominator can be added while preserving the other requirements, support and any dependent preferred-use grant are not permanently stable and cannot receive a sound permanent-support certificate. **Project impact:** This narrows rather than threatens the open-endedness result. The paper must distinguish positive non-domination from persistent negative defeat, and any neural stability target must preserve atom polarity and the dominator witness. No roadmap repair is needed because Task 12 already proves the corrected theorem and Task 13’s `K_3` representation retains polarity. The counterexample and replacement should be a central mathematical qualification in both final artifacts, not a footnote. |
| E16 | On well-formed requests, finite conjunction of meaningful atom states is algebraically Strong-Kleene conjunction under `refuted↦false`, `open↦undefined`, and `supported↦true`; request-level `Undefined` is separate. | Checkpoint A1 compact kernel; `verification/kernel.py`; `formalism/07_core_calculus.md`, §§5–6; `formalism/08_metatheory.md`, Theorems 1–3; `Kleene1952`; `BochvarBergmann1981`; `notes/literature_core_supplement.md`, §6 | FORM/LIT | F/Lit | S1 algebra; D1 infectious four-chain | The meet is minimum on the three-element chain and therefore exactly has Strong-Kleene conjunction's finite algebraic behavior. This imports commutativity, associativity, idempotence, and coordinate monotonicity, not Kleene's interpretation or a source completeness theorem. Task 14 proves exact two-phase normal form and scoped four-outcome observational minimality. The historical four-chain is superseded (`D1`) by checking `WF` first; meaningful atoms never take `Undefined`. |
| E17 | No coordinate of the compact request `(s,e,q,P)` can be omitted while preserving `Assess` over every finite core model. | `formalism/07_core_calculus.md`, Theorem 3 | FORM | F | S1 | Proved by four finite separators. With the other three coordinates held fixed, changing the evaluated plan can change adequacy, changing the context tolerance can change adequacy, changing the finite state can change evidence from supported to open, and changing the profile can add a refuted comparison requirement. The result establishes extensional interface necessity, not four separately identifiable neural modules. **Project impact:** The later encoding and representation tasks may compress or jointly embed the coordinates, but must preserve every assessment-relevant distinction among plan, context, finite evidence state, and requested authorization profile. An ablation that removes one coordinate is therefore knowingly incomplete unless its experimental family makes that coordinate constant or derivable. |
| E18 | Robust invariance over an update class is equivalent to absence of an impact path to the observed coordinates when the impact graph is change-complete and every reachable observed coordinate has a realizing allowed status-changing path; without realizability, path absence is only sufficient. | `formalism/08_metatheory.md`, Theorem 5 and Countertheorem 6; `formalism/08b_audit_repairs.md`, Theorem 1 and Corollaries 2–3; `verification/footprints.py`; `verification/test_audit_repairs.py`; `notes/checkpoints/B_core_metatheory.md` | FORM | F | S1 calculus-specific sufficiency/conditional iff | The abstract observable-indexed theorem is now instantiated for every frozen operational atom family. Task 14B defines slot-instantiation/read footprints over typed keys, including negative collection indices, validity/correction closure, verifier versions, exact evaluated sets, searches, and pairs; defines event writes; proves complete-diagnostic locality by cases; and derives canonical graph change-completeness for diagnostics, values, assessment, and grant. A real state/assessment witness retains a conservative graph path while remaining granted, so necessity still requires path realizability for the exact observable/update class. **Project impact:** Task 15 may use these dependency-scoped inputs as a proved interface. Conservative overapproximations remain sound for persistence but cannot support causal/explanatory “iff” claims without realizability. Current provenance stays explanatory and must not be substituted for the future-influence footprint. |
| E19 | The frozen typed atom-refinement calculus is support-sound, and its lifted profile preorder is sound and relatively complete for finite independently realizable atom fragments. | `formalism/08_metatheory.md`, Theorems 8–10; `formalism/08b_audit_repairs.md`, §6.1; `verification/test_metatheory.py`; `notes/checkpoints/B_core_metatheory.md` | FORM | F | S1 instantiation-fiber/fragment-relative | Each primitive rule is proved under exact scope/mode conditions. At a reference base `beta`, realizing states vary within the profile-instantiation fiber `[beta]_(P,Q)`, preserving well-formedness, addresses, and refinement rules. If `P >=_prof^beta Q` fails, the downward closure of `Req_beta(P)` yields a finite fiber model that grants `P` and withholds `Q`. Completeness is relative: genuine conjunctive semantic entailments require conjunctive syntax and are excluded from the independent fragment. Task 14B repairs the former schema-level notation and makes the open-outside-downset/fiber hypothesis explicit; schema-level completeness would require it uniformly plus `WF` transfer. |
| E20 | Singleton profiles force `3^n` distinguishable meaningful states and an `n log_2(3)`-bit lower bound for `n` atoms. | Checkpoint A1 theorem target; `formalism/08_metatheory.md`, Theorems 11–14 and Countermodel 13; `formalism/08b_audit_repairs.md`, §§6.2–6.3; `ml/03_representation_theorems.md`, Theorem 1 and Corollary 2; `verification/test_metatheory.py`; `notes/checkpoints/B_core_metatheory.md` | FORM | F | X1 singleton-only statement; S1 realizable-space/discrete-code replacement | Singleton profiles separate every vector in the **realizable** set `V`, so the exact state count is `card(V)` and fixed-length **discrete-code** bound is `ceil(log_2 card(V))`. The `3^n` / `ceil(n log_2 3)` result holds when all ternary vectors are independently realizable. With two atoms and sound refinement `a=>b`, only seven of nine vectors are realizable although singleton profiles distinguish all seven; Task 14B supplies and executes address-indexed region witnesses for every surviving vector. `Ill(WFDiag)/Well([v]_F)` is a canonical decoded normal form, not a mandatory internal datatype. **Project impact:** Task 17 uses the query-induced quotient, requires independence before the worst case, and proves that this discrete-bit count is not a real neural-width bound without precision/robustness restrictions. Neural status requirements may be smaller in constrained families, while full diagnostic/provenance payloads may be much larger. |
| E21 | Operational support or `Lic_P` grant entails the corresponding target-world fact without a certificate-mode bridge. | Tempting factivity strengthening; `formalism/08_metatheory.md`, Theorem 15 and Countertheorem 16 | FORM | F | X1 unqualified; S1 mode-scoped schema | Falsified by two worlds sharing the same finite state and supported adequacy atom while assigning different target risks. A valid soundness conclusion requires a named certificate mode, admissible `<w,s>` class, and deterministic or statistical bridge with its exact assumptions. **Project impact:** The paper must describe licenses as finite-stage warrants rather than truth, and neural status fidelity must remain separate from target-world calibration/soundness. This protects rather than weakens the central project distinction. |
| E22 | Licenses of all components of a composed model/use plan entail a license of the composition. | Recursive-modeling clarification; `formalism/08_metatheory.md`, Theorems 27–28; `formalism/08a_transport_routing.md`, Theorem 15 and Proposition 17; `formalism/08b_audit_repairs.md`, §7; `verification/test_audit_repairs.py` | FORM | F | X1 unconditional; S1 finite-DAG reification and conditional budget | Falsified unconditionally: two component requests with certified error `0.06` each are actually assessed `Granted` at tolerance `0.10`, while the reified composite's certified error `0.12` makes its actual request `Refused`. The supported replacement is that a finite acyclic typed component DAG can be reified as one plan; Task 14A then bounds composite error under tube-valid local bounds and downstream sensitivities, while resource costs use declared aggregators. **Project impact:** Plan-DAG encoders may expose component statuses, but the symbolic decoder cannot meet them into a composite grant; it needs direct evaluation or the propagation certificate. Cyclic self-reference remains a fixed-point extension. |
| E23 | Differently labelled local conclusions can disagree without entailing arbitrary unlabelled conclusions in the compact core. | `formalism/03_consequence_update.md`, §§4; `formalism/08_metatheory.md`, Theorem 17 and Corollary 18 | FORM | F | S1 scoped | Proved by derivation induction: `USE` introduces `[e,q]` and authorization labels, and no core rule erases them or derives `True_w`. This is label conservativity, not global paraconsistency; a classical inconsistent local object theory may still explode within its own label. |
| E24 | The four public outcomes and safety projections are minimal/lossless without qualification. | Checkpoint A1 compactness target; `formalism/08_metatheory.md`, Theorem 3 and Proposition 4 | FORM | F | S1 scoped; X1 unqualified wording | Four outcomes are observationally minimal when callers require exact distinction among ill-formed, refuted, open, and supported requests and all four are realizable; a grant Boolean needs only two. `Alarm` and `PendingSafety` losslessly retain selected refuted/open diagnostic records and recover safety atom values from their domains, but they do not retain supported-safety witness payloads or nonsafety diagnostics. **Project impact:** Later neural compression claims must name the consumer query family and cannot call a projection globally lossless. |
| E25 | Every history-preserving update consistent with old records preserves an existing grant. | Structural rational-persistence analogue; `formalism/08_metatheory.md`, Countertheorem 24 | FORM | F | X1 unrestricted; S1 impact-scoped replacement | Falsified by a valid `AddModel` extension: it preserves all old records and noncomparison support while a new dominator refuses `P_pref-rel`. This is a state-transition countermodel, not a literal refutation of a KLM premise postulate. **Project impact:** Append-only provenance and consistency with old evidence do not justify grant persistence; use the observable-specific impact characterization or an explicitly frozen update class. |
| E26 | Routing to the declared fallback on an empty active set guarantees target-world safety. | Gap/fallback strengthening; `formalism/08_metatheory.md`, Theorem 20 and Countermodel 21 | FORM/EMP | F/E | X1 unqualified target-safety; S1 no-unlicensed-use rule | Falsified by a world in which the explicit status-quo fallback has catastrophic loss. The supported core theorem is operational: a certified gap cannot use an unlicensed library plan and must return the declared fallback/information/abstention action. **Project impact:** Fallback identity, loss, and evidence must remain explicit; experiments must measure fallback harm, and public prose must not equate abstention/status quo with safety absent a certificate. |
| E27 | A cyclic request in which the grant determines the loss/evidence used to determine that same grant has a unique status under the ordinary core. | Recursive-modeling clarification; `formalism/08_metatheory.md`, Counterexample 30 | FORM | F | X1 implicit-core reading; S1 explicit-extension requirement | Falsified as an implicit core claim: Boolean equations `g=not g` and `g=g` give no fixed point and multiple fixed points respectively, while adopting a three-valued fixed point changes the semantics and may still require selection. **Project impact:** Recurrent/self-evaluating implementations must state their operator, state space, convergence, existence/uniqueness, and selection convention. The ordinary finite decoder accepts only a resolved well-formed request; unresolved cycles remain a fixed-point extension rather than self-certifying licenses. |
| E28 | Expected-risk adequacy on a parent domain transports to every positive-measure measurable subdomain. | Domain-transport target; `formalism/08a_transport_routing.md`, Theorem 1 and Counterexample 3; `verification/test_transport_routing.py` | FORM | F | X1 aggregate-only reading; S1 iff replacement | Falsified from a parent mean alone: loss `1` on ten percent of a domain and `0` elsewhere has parent risk `0.1` but conditional risk `1` on the bad subdomain. The exact replacement is an equivalence: all positive-measure conditional means are at most `epsilon` iff loss is at most `epsilon` almost surely. **Project impact:** Aggregate certificates cannot be copied to learned or post-selected cells. Domain encodings, split readiness, experiments, and public claims must identify whether support is parent-average, prespecified-cell, selected-subset, pointwise, or uniform. |
| E29 | A hard router's deployed expected risk is controlled by correct-cell local risks plus explicit misrouting and fallback penalties. | `formalism/08a_transport_routing.md`, Theorems 5–6, Corollary 7, and Counterexamples 8–10; `verification/test_transport_routing.py` | FORM/EMP | F/E | S1 scoped; X1 naive selected-mass/probability-only variants | The exact partition identity separates correct-route, misroute, and fallback integrals. With selected-subset certificates and bounded penalties, risk is at most `sum_i mu(G_i)epsilon_i + L_M mu(M)+L_F mu(B)`. A whole-cell mean supports only the conservative `mu(C_i)epsilon_i` term unless pointwise/selection-aware evidence is available. Misroute probability alone fails under rare catastrophic loss, and omitted gap mass invalidates the decomposition. **Project impact:** Router evaluation must report selected-scope risk, severity as well as frequency of mistakes, fallback mass/loss, and coverage; routing accuracy alone is not a safety metric. |
| E30 | Small bridge disagreement between model predictions guarantees small task-risk disagreement or safe blending. | `formalism/05_atlas.md`, Theorem 3; `formalism/08a_transport_routing.md`, Theorems 11–12 and Counterexample 13; `verification/test_transport_routing.py` | FORM/EMP | F/E | X1 unqualified; S1 Lipschitz replacement | Prediction deviation becomes a task-risk bound only after a common typed frame and a `K`-Lipschitz task-loss bridge: mean disagreement `delta` gives risk difference at most `K delta`, with corresponding blend excess-risk bounds. Arbitrarily close scores can cross a zero-one decision boundary and differ in loss by one; squared loss has unbounded sensitivity on an unbounded range. **Project impact:** Neural bridge heads must preserve disagreement, task frame, and the regularity certificate; smooth interpolation or output closeness cannot be marketed as adequacy or safety, and blends remain separately licensed plans. |
| E31 | Component errors and costs in a recursively composed use plan can be added without sensitivity or accounting qualifications. | Recursive-plan transport target; `formalism/08a_transport_routing.md`, Theorem 15, Proposition 17, and Counterexamples 18–19; `verification/test_transport_routing.py` | FORM | F | X1 unqualified; S1 path-weighted/resource-specific replacement | A downstream map `y->K y` amplifies upstream error `delta` to `K delta`, and a downstream certificate valid only at the nominal input can fail after upstream perturbation. The supported error budget sums intrinsic errors with path-product sensitivity weights over a finite DAG and requires validity on the reachable perturbation tube. Scalar cost adds only under common-unit additive accounting; latency, memory, caching, and contention use different aggregators. **Project impact:** Plan registries and encoders need interfaces, local-bound domains, downstream sensitivities or a direct composite certificate, and typed resource aggregation. |
| E32 | Exact invertible pairwise bridges on an overlap graph arise from global frame potentials exactly when every cycle product is identity. | `formalism/08a_transport_routing.md`, Theorem 20 and Counterexample 21; `verification/test_transport_routing.py` | FORM | F | S1 | Proved componentwise for finite group-valued overlap graphs. Potentials telescope to identity around cycles; conversely, cycle identity makes root-to-node path products well defined. A triangle of additive translations `(0,0,1)` is pairwise invertible but cycle-inconsistent. **Project impact:** Pairwise bridges do not by themselves justify one global coordinate system; path-dependent comparisons must expose cycle defects and provenance. |
| E33 | Every finite well-founded typed plan assembled from certified primitive computations and constructors with valid payload, grade, and certificate transformers has an annotated composite executor; an acceptable verified root grade can support a composite adequacy atom. | Project-author Checkpoint B guidance 2026-07-12; `notes/checkpoints/B_core_metatheory.md`; `formalism/08c_proof_carrying_plans.md`, Theorem 1 and Corollaries 2–3; `verification/proof_plans.py`; `verification/test_proof_plans.py`; `Howard1980`; `Hoare1969`; `Necula1997`; `McConnellEtAl2011` | FORM/LIT | F/Lit | S1 conditional integration/Lit | Proved for the finite-DAG fragment by topological induction. Every node uses the same deterministic payload transformer before and after erasure, a typed grade transformer, and an independently registered local certificate rule; the canonical root term preserves provenance and checks the composite claim. Task 14A's tube-valid path-sensitivity recurrence supplies a concrete grade/certificate transformer, and an actual kernel request becomes `Granted` only from the acceptable checked root grade. The executable witness rejects an unregistered local certificate and a plan cycle. **Project impact:** the positive recursive-program goal is realizable, but the theorem does not meet component grants, certify an arbitrary composition, or turn a score into evidence. Curry–Howard, Hoare logic, refinement/quantitative types, PCC, and certifying algorithms are prior machinery; the scoped contribution is their defeasible quantitative-license integration. |
| E34 | A frozen value-logic implementation can receive a finite-stage license under an independently fixed meta-context when its evidence graph is grounded and its total deterministic evaluators are finitely stratified; its own self-endorsement alone cannot establish target adequacy. | Project-author Checkpoint B guidance 2026-07-12; `notes/checkpoints/B_core_metatheory.md`; `formalism/08c_proof_carrying_plans.md`, Theorems 5, 8, and 10; `formalism/08_metatheory.md`, Counterexample 30; `verification/proof_plans.py`; `verification/test_proof_plans.py` | FORM/EMP/LIT | F/E/Lit | S1 finite theorem/T0 empirical system | Proved at the finite formal level. In an acyclic support graph where every zero-premise support is a typed base and rules preserve premise provenance, every supported required atom reaches typed evidence. Under strict finite evaluator ranks and total deterministic local evaluators, the global diagnostic/assessment assignment is unique by rank induction and remains grounded. Reifying `e_VL` is harmless when its meta-request reads lower-rank run records and independent evidence. A two-world countermodel falsifies the stronger claim that operational self-endorsement is target-factive. **Project impact:** the experiment may license a frozen system from an independent audit split, but self-confidence-only evidence is a required failure ablation; same-run self-grant is excluded, while genuine cycles still require an explicit operator, fixed-point selection, and soundness theorem. Empirical tracking of the declared target remains T0. |

---

## F. Neural encodings, ReLU semantics, and atlases

| ID | Scoped restatement | Source | Role | Testability | Evidence | Proposed adjudication and outcomes |
|---|---|---|---|---|---|---|
| F01 | Treating scalar statements as maps `p:Z→R` and vector statements as maps `s:Z→R^n` makes affine layers and pointwise activations type-preserving operations on one semantic carrier. | `UPLNN:371–389` | DEF/FORM | F | C0 | Write a typed category/algebra of maps. **Support:** closure follows by composition. **Falsify:** not applicable to the definition, but it can fail to provide required logical operations. **Inconclusive:** “statement” is only a renaming of function. |
| F02 | ReLU networks can implement score-level `max` and `min` exactly, and strict positive thresholds then implement union and intersection of induced regions exactly. | `UPLNN:346–361` | FORM | F | C0 | Verify identities and threshold set equalities. **Support:** algebraic proof. **Falsify:** counterexample to an identity/equality under stated real arithmetic. **Inconclusive:** finite precision changes implementation but not theorem. |
| F03 | Strict-positive regions of continuous scores are open sets and inherit a Heyting-algebra semantics; excluded middle and double-negation elimination can fail. | `UPLNN:336–369, 391–421` | FORM/LIT | F/Lit | C0/L0 | Prove topology results and distinguish representable opens from all opens. **Support:** explicit score witness and correct pseudo-complement/implication semantics. **Falsify:** claimed score connective fails to realize the Heyting operation, especially with zero plateaus. **Inconclusive:** topology/threshold convention changes. |
| F04 | ReLU feature maps can place positive probability mass on strict-threshold boundaries, making “neither true nor false” regions empirically frequent. | `UPLNN:423–437` | FORM/EMP | F/E | C0 | Verify the proposed distributional construction and measure boundary mass in trained models. **Support:** exact toy proof plus empirical occurrences. **Falsify:** universal version fails; specific construction is disproved only by arithmetic/topology error. **Inconclusive:** zero mass is numerical rather than structural. |
| F05 | A useful neural adequacy score is `s_i(x,epsilon)=log(epsilon)-predicted_log_loss(M_i,x)`, with `ReLU(s_i)` as positive license margin. | `GPT:650–760, 1179–1694`; `FABLE:226–310`; project `TODO.md`; `ml/02_relu_architecture.md`, §§2–3 | DEF/EMP | F/E | S1 sign equivalence/D1 default/T0 calibration | On strictly positive risk and tolerance, `log(epsilon)-log(J_hat)` has the same sign as `epsilon-J_hat`, so the threshold algebra is sound. Task 16 supersedes it as the reference default, not because it is false, but because center-radius regions and `epsilon-upper(U_safe)` expose additive error, zero/boundary cases, units, and evidence polarity more directly. The log form remains an allowed positive multiplicative-risk parameterization. **Support:** externally calibrated positive-margin reliability on its declared domain. **Falsify:** systematic false grants under the claimed envelope. **Inconclusive:** uncalibrated scores or zero/nonpositive inputs outside its scope. |
| F06 | The signed preactivation contains degree-of-inadequacy information that ReLU erases by mapping all nonpositive scores to zero. | `FABLE:99–118`; project `TODO.md`; `notes/representation_layers.md`, Proposition 1; `ml/02_relu_architecture.md`, §3; `verification/test_relu_architecture.py` | FORM | F | S1 | Supported exactly: every nonpositive input maps to zero, and the Task 16 boundary witness shows that supported equality also has zero activation. The production architecture therefore retains signed support/refutation margins, paired ReLU channels, and the exact diagnostic. This does not say negative magnitude alone determines refutation; boundary crossing and evidence validity remain separate. |
| F07 | Model and case/domain vectors should be derived from a model-by-record loss tensor rather than stipulated capability labels. | `GPT:1179–1319, 1660–1694`; `FABLE:226–245` | DEF/EMP | E/F/Lit | C0/L0 | Compare factorized loss embeddings with hand features on held-out loss prediction and routing. **Support:** derived embeddings generalize/calibrate better or recover known axes. **Falsify:** no advantage across scoped datasets or factorization is non-identifiable beyond usefulness. **Inconclusive:** data matrix lacks coverage/rank. |
| F08 | Tying `log(epsilon)` with coefficient `+1` makes licensed domains monotone: if `epsilon' >= epsilon`, then `D_M(epsilon') ⊇ D_M(epsilon)`. | `GPT:1423–1450`; `FABLE:269–272` | FORM | F | C0 | Algebraic proof. **Support:** follows from score definition. **Falsify:** counterexample indicates implementation violates tying or changes loss. **Inconclusive:** tolerance dimensions are vector-valued. |
| F09 | Unit-invariant physical-domain encoders must factor through dimensionless groups, and logarithms linearize multiplicative regime coordinates. | `FABLE:248–254` | FORM/LIT | F/Lit | C0/L0 | Verify exact use of Buckingham-π/group invariance and assumptions (positive quantities, completeness). **Support:** representation theorem under scoped symmetry. **Falsify:** invariant not expressible through chosen groups or logarithm invalid at zeros/sign changes. **Inconclusive:** learned encoder is only approximately invariant. |
| F10 | If model error is dominated by leading omitted perturbative corrections, then log loss is approximately a max of affine forms in log dimensionless coordinates and adequacy is a min of affine safety margins. | `GPT:1345–1421, 1541–1635`; `FABLE:255–269` | FORM/EMP/Lit | F/E/Lit | C0/L0 | Derive asymptotics for selected physical models and compare predicted versus measured error. **Support:** bounded approximation error over stated regimes. **Falsify:** cancellations/interactions/curved effects violate claimed bounds. **Inconclusive:** regime is outside perturbative validity. |
| F11 | Every finite ordinary feed-forward ReLU MLP with affine hidden maps and affine output partitions input space into finitely many activation-pattern polyhedra and computes a globally continuous affine map on each cell. | `FABLE:57–61, 123–160`; `UPLNN:295–306`; `AroraEtAl2018`, Theorem 2.1; `HeEtAl2020`, Lemma 2.1; `ml/03_representation_theorems.md`, §§1 and 4 | LIT/FORM | F/Lit | S1 | Supported under the stated architecture. Induction through affine maps and coordinatewise ReLU yields a finite polyhedral refinement and a CPWL output. Degenerate/empty activation patterns do not invalidate the result; operations such as hard routing, multiplication, normalization, or attention fall outside this scoped architecture unless separately analyzed. **Project impact:** CPWL statistics receive the Task 17 exact construction, but the scientific cover and learned activation complex remain distinct. |
| F12 | Adjacent affine pieces of one ordinary ReLU network agree pointwise on their shared boundary because the network is continuous, although their Jacobians may differ. | `FABLE:59, 133–160`; `ml/03_representation_theorems.md`, Theorem 4 | FORM | F | S1 | Supported as a special case of the finite closed-cover gluing characterization. Continuous ReLU outputs force equal traces on every two-sided accumulation face. Separately routed experts may disagree and form a discontinuous hard seam; an external router can represent that case, but one ordinary continuous ReLU output cannot. **Project impact:** exact single-network atlas gluing requires trace agreement; approximate or discontinuous scientific bridges remain external typed objects. |
| F13 | When adjacent affine cells agree across a codimension-one facet, their affine Jacobian difference has rank at most one; the one-active-unit ReLU switch is a special case. | `FABLE:141–160`; `ml/03_representation_theorems.md`, Corollary 4; `verification/test_representation_theorems.py` | FORM/Lit | F/Lit | S1 scoped | If the common facet lies in `n^T x=c`, trace equality forces `A_i-A_j=a n^T` and `b_i-b_j=-ac`, so the Jacobian difference has rank at most one. The original one-unit wording requires the downstream activation pattern to remain fixed. Multiple simultaneous switches, lower-dimensional intersections, degeneracy, or independently routed experts do not inherit the conclusion. |
| F14 | Every global finite continuous piecewise-affine scalar map has an exact finite ReLU representation; fixed finite vector maps follow coordinatewise under explicit architecture and domain conventions. | `FABLE:155–160`; `AUDIT-F5:C6`; `AroraEtAl2018`, Theorem 2.1; `HeEtAl2020`, Theorem 5.2; `ml/03_representation_theorems.md`, Theorem 3 | LIT/FORM | F/Lit | S1 | Arora et al. prove exact scalar CPWL/ReLU representation with total depth at most `ceil(log2(d+1))+1`, hence at most `ceil(log2(d+1))` hidden layers under their convention, using Wang–Sun's hinging-hyperplane representation. He et al. detail a finite but potentially enormous size bound in terms of distinct affine pieces and unique-order regions. Task 17 proves the vector parallelization and requires an explicit global CPWL extension for restricted-domain targets. Direct discrete `K_3`, inclusive boundary comparison, evidence checking, efficiency, and learnability do not follow. |
| F15 | Depth gives exponentially more efficient representations of some piecewise-linear functions than shallower ReLU networks; transfer to scientifically meaningful adequacy regions requires an additional construction. | `FABLE:161–176`; `AroraEtAl2018`, Theorems 3.1–3.5; `ml/03_representation_theorems.md`, §4 | LIT/FORM | F/Lit | S1 function separation/T0 semantic transfer | The primary depth-separation family supports an exponential or stronger size gap under its exact depth, size, and approximation conventions. Task 17 does not identify those hard functions with calibrated adequacy margins or scientific domains. **Project impact:** depth may matter for representation efficiency, but it is not evidence of recursive license semantics, scientific alignment, or better training; those interpretations remain falsifiable empirical hypotheses. |
| F16 | ReLU computation has a tropical/max-plus representation that can be interpreted as best-margin proof or license-path evaluation. | `FABLE:61, 177–181`; `formalism/08c_proof_carrying_plans.md`; `ml/03_representation_theorems.md`, §8 | LIT/INT/FORM | Lit/F/U | I1 numerical analogy; S1 proof-term distinction and proof-erased computation | Zhang–Naitzat–Lim (2018) support the tropical rational-map correspondence. No audited source establishes the proposed proof/license-path semantics. Task 14C distinguishes checked proof/certificate terms from numerical paths, and Task 17 proves exact ReLU realization only for the proof-erased CPWL payload/grade map of a fixed finite annotated plan. Neither result makes an arbitrary forward pass a proof; certificate construction and checking remain external. |
| F17 | Clipped-ReLU networks correspond, subject to coefficient restrictions, to functions definable in Łukasiewicz or rational Łukasiewicz logic. | `FABLE:181–186`; `UPLNN:308–319` | LIT/FORM | Lit/F | S1 | Castro–Trillas (1998) and Amato–Di Nola–Gerla (2002) support correspondences for truncated-identity networks with the stated integer/rational restrictions. This does not extend without proof to arbitrary real-weight, unbounded ReLU MLPs. |
| F18 | ReLU's zero branch implements quarantine/non-explosion because a failed license contributes zero rather than its negation. | `FABLE:63, 99–118, 186–205`; `ml/02_relu_architecture.md`, §9.1; `verification/test_relu_architecture.py` | DEF/FORM/INT | F | X1 unqualified; S1 masked replacement | Falsified as an architecture-independent claim. The finite network path `y=3 ReLU(-10)+2+5=7` remains active through a bias and bypass even though the named failed unit is zero. Zero silences only that one multiplicative contribution; it does not authorize, quarantine, or prove non-explosion for the full network. The supported replacement is exact symbolic active-set restriction before selection, with fallback on an empty set; Task 16 verifies that an inactive candidate with score `1000` cannot be selected. **Project impact:** the central logic and ReLU reference survive, but production routing must use the symbolic mask, Task 19 must include naive-gating/unmasked ablations, and the public artifacts must not call rectification alone logical quarantine or non-explosion. |
| F19 | Mixture-of-experts or maxout routing can implement selection of an undominated licensed model, including an explicit null/abstention option. | `GPT:1451–1505`; `FABLE:61, 181–189`; `ml/02_relu_architecture.md`, §§9, 12.2; `verification/test_relu_architecture.py` | FORM/EMP/Lit | F/E/Lit | S1 operational mask/fallback; T0 comparative performance | Task 16 supplies the scoped construction: compute the exact profile active set first, restrict hard or soft selection to it, require an explicit tie rule, and return the exact fallback on a gap. This guarantees no inactive plan is selected, but “undominated” still comes from the external comparison diagnostic and exact search scope, not router logits. Hard-MoE performance on discontinuous seams, selected-scope risk, and route calibration remain empirical. **Project impact:** a router can implement selection after licensing; it cannot replace licensing or certify global optimality. |
| F20 | The intrinsic activation-region atlas learned by SGD will align with scientifically meaningful model domains or interpretable licenses. | `FABLE:215–221`; project `TODO.md`, “two atlas notions” | EMP | E | T0 | Use synthetic known regimes, representation probes, causal interventions, and stability across seeds. **Support:** preregistered region/domain correspondence generalizes and interventions track semantics. **Falsify:** regions remain non-identifiable/polysemantic or fail out-of-sample alignment. **Inconclusive:** multiple equally valid decompositions fit behavior. |
| F21 | The two-channel encoding `(ReLU(s),ReLU(-s))` preserves a signed scalar exactly, while allowing only the positive channel to route licensed content. | `notes/representation_layers.md`, §§9–10; `ml/02_relu_architecture.md`, §3; `verification/relu_architecture.py`; `verification/test_relu_architecture.py` | FORM/DEF | F | S1 scoped | The identity `s=ReLU(s)-ReLU(-s)` is proved by cases and executable for negative, zero, and positive margins. Task 16 retains both channels but narrows the routing clause: the positive channel is a downstream feature only after exact evidence acceptance, and full plan selection uses the symbolic active mask. Supported equality, open, and refuted states still require the diagnostic/validity channels. |
| F22 | Bilinear model/domain embeddings are not uniquely identifiable: invertible paired basis transformations preserve all inner products and hence the same predicted losses. | `notes/representation_layers.md`, §§10–11 | FORM/Lit | F/Lit | C0 | Prove the paired transformation invariance and characterize extra constraints that reduce it. **Support:** exact algebraic equivalence. **Falsify:** only a more constrained model may be identifiable; this narrows the claim. **Inconclusive:** nonlinear scorer has a different symmetry class. |
| F23 | If predicted risk has a certified error bound `abs(J_hat-J)<=delta`, requiring `J_hat<=epsilon-delta` is sufficient for true adequacy `J<=epsilon`. | `notes/representation_layers.md`, §10; `ml/02_relu_architecture.md`, §3.2; `ml/03_representation_theorems.md`, Theorem 2; `verification/test_representation_theorems.py` | FORM | F | S1 conditional theorem/T0 empirical envelope | Supported by the triangle inequality and Task 17's general affine-boundary theorem. Conservative interval decoding is sound at raw radius `delta`; guaranteed agreement with the ideal two-sided decision for every admissible estimate requires `J<=epsilon-2delta` or `J>epsilon+2delta`. Values in the intervening ideal-margin band may open. Obtaining a current scope-valid `delta` remains a calibration/certificate obligation, and a one-sided bound cannot justify the opposite polarity. |
| F24 | A domain encoding is sufficient only relative to its risk functional; a summary adequate for expected risk can discard tail/set information required for worst-case or safety risk. | `notes/representation_layers.md`, §5; `ml/01_encodings.md`, §§1–3 | FORM/EMP | F/E/Lit | C0 | Task 15 makes the risk functional, scope, units, and address exact inputs and forbids their sole replacement by a latent domain code, but it does not prove a general statistical sufficiency result. Construct equal-summary domains with different tails/worst-case losses and compare encoders. **Support:** separating counterexample and empirical failures of underspecified summaries. **Falsify:** a scoped characteristic/injective domain representation preserves every required functional. **Inconclusive:** target risk never probes the lost information. |
| F25 | Multilayer composition makes finite nested CPWL payload/grade computation exactly representable, but depth alone does not establish that hidden layers semantically represent “adequacy of adequacy.” | `notes/representation_layers.md`, §13; `FABLE:57–69`; `formalism/08c_proof_carrying_plans.md`; `ml/03_representation_theorems.md`, Theorem 8 | FORM/EMP/INT | F/E | S1 finite CPWL expressivity/T0 semantic alignment | Task 17 proves by topological induction that a fixed finite proof-erased annotated plan with CPWL primitives/grades and conforming hard branches has an exact ReLU realization. Task 14C keeps proof/certificate terms external. Neither result identifies arbitrary intermediate layers with recursive licenses; basis changes, distributed representations, and alternative factorizations remain. **Project impact:** the recursive computation goal is realizable in the scoped finite CPWL fragment, while interpretability requires separate alignment and intervention evidence. |
| F26 | Reading each dense hidden unit as one incoming license test plus one identifiable outgoing content vector is an idealization that can fail under distributed representation, cancellation, basis symmetries, and polysemanticity. | `FABLE:57–65`; `notes/representation_layers.md`, §§11–12 | EMP/FORM/Lit | E/F/Lit | C0/T0 | Compare unit-level and subspace/path-level explanations under reparameterization and causal ablation. **Support:** unit meanings change or distribute while function remains stable. **Falsify:** a constrained monosemantic architecture proves stable one-unit/one-license correspondence. **Inconclusive:** interpretability method is not invariant to basis. |
| F27 | The intrinsic activation complex of a ReLU scorer/router and the scientific licensed model cover need not correspond one-to-one; many activation cells may refine one scientific active-set or selection region. | `notes/atlas_questions.md`, §§12–16; `formalism/05_atlas.md`, Proposition 7 | FORM/EMP | F/E | S1 formal/T0 empirical | Formal counterexamples establish both directions of non-bijection: canceled hidden features can refine activation cells without changing atlas state, while two charts can be simultaneously licensed on one domain represented by a single affine activation cell. Empirical alignment quality still requires purity/fidelity/stability tests. |
| F28 | Continuous overlap weights can stitch translated model predictions continuously without exact model equality, provided the outputs and weights are continuous and interpolation is semantically meaningful. | `notes/atlas_questions.md`, §§9–10; `formalism/05_atlas.md`, §§8.6 and Theorem 3; `formalism/08a_transport_routing.md`, Theorem 12 | FORM/EMP/Lit | F/E/Lit | S1 formal/T0 empirical | Continuity and component-deviation bounds follow from continuous translated outputs/weights and convex interpolation. Task 14A adds task-risk control when loss is Lipschitz on the reached range. Neither result proves truth, task meaning, invariant preservation, calibration, or safety; the blend is a new use plan requiring evaluation. |
| F29 | A fixed model-indexed output interface supports only its declared finite registry; an expandable interface requires candidate-conditioned shared scoring plus external identity/memory or a sequence of systems, and a fixed summary may still lose independent distinctions. | `notes/atlas_questions.md`, §§14, 17–18; `ml/01_encodings.md`, §6; `ml/02_relu_architecture.md`, §§6–7; `ml/03_representation_theorems.md`, §6; `verification/test_representation_theorems.py` | FORM/EMP/Lit | F/E/Lit | S1 interface and non-closure/T0 capacity | Supported: an `n`-indexed output has no coordinate for candidate `n+1`; pointwise shared scoring restores exact identities externally and is permutation-equivariant. Task 17 adds an indistinguishability proof that finite evaluated-set non-domination cannot imply closure under unseen registry extensions. This remains an interface theorem, not a finite-precision storage lower bound or a cold-start accuracy result. **Project impact:** Tasks 19–21 must test additions, forgetting, and sparse evidence while indexing every comparison claim by the exact evaluated set/search trace. Unbounded-precision real parameters remain outside scope. |
| F30 | For a finite library, preserving one signed score vector component per model is sufficient to recover the complete pointwise active licensed set; an immediate argmax is not. | `notes/atlas_questions.md`, §§15, 17, 19; `formalism/05_atlas.md`, Proposition 9 | FORM | F | S1 under signed-calibration assumptions | With indexed margins satisfying `i active iff s_i>=0`, componentwise thresholding exactly recovers the Boolean active set, including simultaneous and empty sets. Immediate argmax retains only a winner and cannot distinguish those cases. The result does not apply when scores are uncalibrated, identities alias, or applicability is genuinely graded/four-way without additional channels. |
| F31 | Compatibility with the license calculus is an architecture-relative interface property, not a ReLU uniqueness claim: a ReLU construction establishes one realization, while another architecture is compatible if it preserves the required typed inputs, atom-level sufficient statistics, diagnostics/missingness, and symbolic decoder contract. | User clarification 2026-07-12; `notes/project_spec.md`, §7; `formalism/07_core_calculus.md`, §14; `notes/checkpoints/B_core_metatheory.md`; `ml/01_encodings.md`; `ml/02_relu_architecture.md`; `ml/03_representation_theorems.md`, §9 | DEF/FORM/EMP | F/E | S1 interface and exact ReLU witness; T0 comparative performance | Tasks 15–17 now supply the architecture-neutral kernel/quotient contract and a conforming ReLU theorem witness for global finite CPWL statistics and proof-erased plans, with external calibration/checking, `WF + K_3`, audit diagnostics, masking, and fallback. The factorization, robust-margin, seam-gluing, library, and joint-sufficiency parts transfer to other architectures at the stated interface. Efficiency, learning, calibration, interpretation, uniqueness, and superiority do not. **Project impact:** Task 18 onward must train and compare architectures against matched semantic outputs rather than treat ReLU expressivity as empirical optimality. |
| F32 | A hypothesis-indexed ReLU activation can serve simultaneously as positive certificate-relative adequacy surplus and downstream computational feature exactly when the declared representation is jointly sufficient for the required license queries and downstream computation, with explicit certificate validity or a verified approximation envelope, common/normalized scale, boundary convention, and channel semantics. | Project-author Checkpoint B guidance 2026-07-12; `notes/checkpoints/B_core_metatheory.md`; `ml/01_encodings.md`, §11; `ml/02_relu_architecture.md`, §8; `ml/03_representation_theorems.md`, Theorems 6–7; `verification/test_representation_theorems.py` | FORM/EMP/INT | F/E | S1 named coordinate-complete family/T0 calibration and alignment | Formally supported at the scoped strength requested by the author. For named certificate-valid CPWL margins, `z_i=ReLU(m_i/sigma_i)` is exact positive normalized surplus and numerical input to declared later layers. The hybrid code of exact mode-relative states plus `z` is minimal up to relabeling for a consumer family containing every state and surplus projection. Verified margin error transfers through ReLU and Lipschitz consumers; status recovery uses the explicit conservative decision band. Equal adequacy with unequal payload, supported equality at zero, invalid evidence, and unnormalized rescaling prove the limits. **Project impact:** the paper may present the classifier construction as a theorem, but calibration, semantic alignment, and practical usefulness remain empirical; arbitrary hidden units and general payloads receive no adequacy semantics. |
| F33 | On tasks where extensionally equal observed payloads arise from plans that differ in sharing, cost, robustness, grade propagation, or explanation, an isomorphism-aware typed plan-DAG representation will preserve and generalize those distinctions better than a matched flattened plan-ID representation. | Project-author Checkpoint B guidance; `formalism/08c_proof_carrying_plans.md`; `ml/01_encodings.md`, §7 | EMP/FORM | E/F | T0 | Task 15 operationalizes the hypothesis and specifies the exact DAG isomorphism, but supplies no performance evidence. Compare matched outputs, capacity, compute, train/test structural splits, and grade/certificate tasks. **Support:** the explicit graph improves prespecified out-of-structure grade/cost/explanation fidelity without sacrificing payload fidelity. **Falsify:** it shows no reliable advantage under an adequately powered fair comparison, or a sufficient flat code matches it. **Inconclusive:** the dataset contains no structural separators or the graph model has materially different capacity. **Project impact if falsified:** omit the graph comparator and keep explicit DAGs as external audit records; the core logic, Task 14C theorem, and ReLU representation targets remain intact. |
| F34 | For an already instantiated atom address, the exact address plus its complete Task 14B dependency projection is sufficient for its complete diagnostic; changing only profile role or an outside-footprint record cannot change that atom value. | `formalism/08b_audit_repairs.md`, Theorem 1; `formalism/07_core_calculus.md`, §§3–5; `ml/01_encodings.md`, §2; `verification/test_encodings.py` | FORM/IMPL | F | S1 | Supported by Task 14B complete-diagnostic locality and by the core separation of address valuation from profile roles. Task 15's regression ignores an unrelated write, exposes a missing read, and changes when a verifier read changes. This is sufficiency, not necessity for every representation; necessity still needs observable-specific path realizability. **Project impact:** the scorer input may be atom-local and reusable across profiles, while `P` remains an exact symbolic selector/aggregator rather than a hidden cause of value. |
| F35 | At matched semantic outputs, capacity, compute, and data, standardized center–radius atom-statistic training plus held-out calibration will outperform independent direct `K_3` atom cross-entropy on tolerance-transfer and boundary-calibration fidelity. | `ml/04_losses.md`, §§3–7 and 12; Task 18 objective decision | EMP | E | T0 | Task 18 makes this a preregisterable comparison, not a verdict. Proposition 2 proves only the information separator: a statistic region can answer changed tolerances that one frozen status label may erase. **Support:** the structured system exceeds the direct classifier by the prespecified matched-study margin on tolerance-transfer and calibrated-boundary endpoints without unacceptable in-regime degradation. **Falsify:** an adequately powered matched experiment finds the classifier noninferior on all preregistered endpoints or reliably better on the claimed endpoints. **Inconclusive:** tolerance/certificate conditions do not vary, calibration targets leak, the comparison is underpowered, or capacity/compute differ materially. **Project impact if falsified:** retain the finite-stage logic, exact symbolic decoder, external certificate boundary, and Task 17 representation results; weaken the practical default for structured regression and permit direct atom classification for frozen contexts, while retaining quantitative heads wherever risk reporting, region inspection, or tolerance transfer is required. |

---

## G. Companion implementation and interpretability bridge

| ID | Scoped restatement | Source | Role | Testability | Evidence | Proposed adjudication and outcomes |
|---|---|---|---|---|---|---|
| G01 | The current companion repository implements a deterministic tic-tac-toe pipeline with heuristic/on-policy data, a one-hidden-layer policy MLP, rollout `V^pi`, value and Q regressors, recovery functions, metrics, tests, telemetry, and checkpointing. | PVI `README.md`; `src/policy_value_isomorph/*.py`; `tests/*.py` | IMPL | F by code inspection/execution | R0 | Install and run tests/CLI at a pinned commit in a later cross-project task. **Support:** documented workflows and tests pass. **Falsify:** missing/broken components. **Inconclusive:** environment incompatibility. |
| G02 | In the current implementation, policy imitation uses masked cross-entropy, while value and Q models use MSE on rollout-derived fixed-perspective returns. | PVI `policy_mlp.py::train_policy_mlp`; `value_mlp.py::train_value_mlp`; `q_mlp.py::train_q_mlp` | IMPL | F | R0 | Execute unit tests and inspect gradients/objectives. **Support:** code paths match description. **Falsify:** runtime selects different objective. **Inconclusive:** pending PyTorch migration changes main. |
| G03 | Existing tests provide behavioral evidence only at modest scoped thresholds (loss decrease, legal actions, action agreement, MAE, forced moves); they do not establish arbitrary-policy reconstruction. | PVI `tests/test_policy_mlp.py`, `test_value_mlp.py`, `test_q_mlp.py`, `test_rollout_and_recovery.py` | IMPL/EMP | F/E | R0 | Run tests and inventory assertions. **Support:** assertions match this scope. **Falsify:** broader benchmark/theorem exists elsewhere in repo. **Inconclusive:** passing unit tests do not estimate generalization. |
| G04 | An independently trained value/Q surrogate can reproduce policy actions without sharing or identifying the policy network's internal mechanism. | Project `TODO.md`, “Cross-project interpretability goal”; PVI separate model modules | FORM/EMP/Lit | F/E/Lit | T0 | Construct functionally equivalent policies with different internals and independently trained surrogates; compare representation/causal tests. **Support:** high behavioral fidelity coexists with low alignment/failed interventions. **Falsify:** under strong shared-architecture assumptions, behavior uniquely identifies mechanism. **Inconclusive:** alignment metric is basis-sensitive. |
| G05 | A license-logic representation could improve transparency by attaching value comparisons to validity domains, margins, abstentions, local charts, and decision traces. | Project `TODO.md`; synthesis of FABLE and PVI | EMP/FORM | E/U | T0 | Build a transparent surrogate and compare fidelity, selective risk, compactness, stability, and causal probes with baselines. **Support:** it improves prespecified transparency metrics without unacceptable fidelity loss. **Falsify:** no improvement across scoped tasks or traces are post-hoc/unfaithful. **Inconclusive:** human interpretability metrics disagree. |
| G06 | Behavioral fidelity, value fidelity, domain fidelity, representational alignment, causal faithfulness, and human inspectability are distinct grades of interpretability and should not be collapsed into one score. | Project `TODO.md`, “Cross-project interpretability goal” | DEF/FORM/EMP | F/E/Lit | T0 | Give pairwise separating examples and measure each grade. **Support:** systems can score differently across grades and the distinctions predict failure modes. **Falsify:** under adopted definitions, grades reduce to the same observable relation. **Inconclusive:** metrics have poor reliability. |

---

## Cross-claim dependency map

The most important dependencies to preserve in later work are:

1. `A01–A06` motivate `E01–E04`; they do not by themselves prove a particular license calculus.
2. `C01` is much stronger than `C04–C07`. Evidence for policy reconstruction (`C05`) must not be reported as evidence for a universal isomorphism (`C01`).
3. `D05` supplies only an ordering induced by loss. The claim that this is a logic (`D06`) additionally depends on a language, semantics, consequence relation, and representation theorem.
4. `F11–F15` concern the intrinsic CPWL structure of a ReLU function. Scientific semantic alignment (`F20`) is a separate empirical claim.
5. `F05` depends on a predicted loss model and calibration; ReLU alone does not supply adequacy semantics.
6. `E08` is a scientific-model atlas, whereas `F11–F12` describe an intrinsic network atlas. The bridge between them is `F20`.
7. `G01–G03` concern the present companion implementation. The interpretability ambitions `G04–G06` are future hypotheses.
8. `E09–E10` are now conditionally adjudicated by Task 12's exact continuation semantics. The separation and non-certifiability results may be used only with their stated freeze, indistinguishability, statistical-family, or `AddModel` assumptions; GL/Löb transfer remains excluded because no provability translation was constructed.
9. `F21–F26` separate information preservation and identifiability from functional fit. A low-loss score or faithful router does not by itself identify unique semantic coordinates or one-unit/one-license structure.
10. `E11–E14` define the overlapping partial scientific cover; `F27–F30` concern how a finite neural scorer/router can represent it. Neither set follows solely from the intrinsic CPWL claims `F11–F15`.
11. `F31` makes the neural interface architecture-neutral. Task 16 supplies one ReLU-specific reference construction, while `F11–F17` and Task 17 delimit its CPWL properties; none excludes alternative architectures or establishes empirical optimality. `F18` is falsified in its ReLU-only quarantine form.
12. `A08` has safe-policy-improvement precedents but still requires a project-specific translation outside MDP policy settings. `E02` must name its probability/certificate mode; conformal miscoverage is not task-risk tolerance. `E16` imports only the Strong-Kleene meet algebra, not its truth-value interpretation or completeness theory.
13. `E18–E27` are Tasks 14/14B's metatheory contract. Update persistence now has calculus-specific typed locality and canonical change-completeness, while necessity remains observable- and realizability-relative; profile completeness is instantiation-fiber/fragment-relative; diagnostic cardinality uses the realizable query quotient and gives a discrete-code rather than neural-width bound; world soundness, fallback safety, plan composition, and cyclic evaluation all require explicit bridges or extension semantics.
14. `E28–E32` are Task 14A's transport contract. Aggregate, selected-subset, pointwise, and uniform risks are different; routing requires severity/coverage terms; prediction bridges need task-loss regularity; plan composition needs sensitivity weights; pairwise bridges need cycle audits before global-frame claims.
15. `E33–E34` are now Task 14C's recursive-plan/system-assessment contract. Finite proof-carrying composition is supported only with typed payload/grade/certificate transformers and a checked root certificate; system-level adequacy is unique and grounded only under the finite-DAG/base-source and strict-rank hypotheses. Self-endorsement remains nonfactive, and direct cycles remain an explicit fixed-point extension.
16. `F32` is the exact dual-use activation question. It depends on `F05/F06/F21/F30`, the query quotient, and a declared downstream computation. Task 15 supplies the scalar obstruction and exact joint-consumer contract; Task 16 supplies the named channel architecture; Task 17 proves the minimal coordinate-complete state-plus-surplus construction and its scale/boundary/approximation results. Arbitrary hidden activations do not inherit the interpretation.
17. `F34` connects the concrete Task 14B locality theorem to the neural input, while `F33` is an empirical architecture hypothesis. Dependency-scoped sufficiency is supported; superiority of an explicit graph representation over a sufficient flat code is not.
18. `F18` cannot supply authorization or non-explosion to any later claim. The exact active mask in `F19` is the supported replacement; the distinction affects architecture, experiment ablations, limitations, and public terminology but not the core calculus.

## Claims most urgently needing operationalization

These are not judged false; they are currently the easiest to protect from counterevidence by shifting meanings:

- `B04`: `T = J^2`.
- `C01`: policy–value “isomorphism.”
- `C08`: value representation is at least as complex as the agent.
- `C09`: frontier models “learn human values.”
- `D06`: loss-trained networks “implement a logic.”
- `A07`: truth as the ideal limit of usefulness.
- `F16`: a forward pass as proof search.

Each remaining item must receive explicit types, quantifiers, equivalence criteria, and a possible counterexample before later prose treats it as a research claim. `F32` has left this list after Task 15 fixed its `F,C`, boundary, evidence, and scale contract; its positive construction and semantic-alignment test remain open.

## Claims with immediate formal sanity checks

The following can be checked early without large experiments:

- `D05`: loss-induced preorder.
- `E01`: threshold adequacy and tolerance monotonicity.
- `E04`: compositional error bound under explicit assumptions.
- `F02`: exact ReLU min/max identities.
- `F03`: open-set/Heyting witness, including plateau edge cases.
- `F06` (**completed Task 16/Task 3**): information loss under rectification, including supported equality at zero.
- `F08`: tied-`log(epsilon)` monotonicity.
- `F11–F14` (**completed Task 17**): finite ReLU CPWL structure, exact boundary agreement, scoped rank-one seam, and exact global CPWL representation with audited architecture/domain conventions.
- `F21` (**completed Task 16/Task 3**): exact paired-ReLU recovery of a signed margin, with exact masking still required for routing.
- `F22`: bilinear change-of-basis non-identifiability.
- `F23` (**completed conditionally Task 16/Task 3**): adequacy certification under a valid bounded risk-prediction error; empirical calibration remains open.
- `F24`: risk-functional dependence of domain sufficiency via separating examples.
- `E12`: active-set decomposition for overlapping domains.
- `F28`: conditional continuity and disagreement bounds for overlap blending.
- `F30`: active-set recovery from model-indexed signed scores.
- `E33` (**completed Task 14C**): structural/topological induction for annotated certified plan constructors.
- `E34` (**completed formal part in Task 14C**): grounded-provenance and finite-rank evaluator induction, with self-endorsement/cycle countermodels; empirical system audit remains pending.
- `F32` (**formal scoped result completed Task 17**): minimal named coordinate-complete state-plus-surplus construction, equal-margin/different-payload and boundary separators, verified approximation transport, and scale covariance; calibration and semantic alignment remain empirical.
- `F34` (**completed Task 15/14B**): dependency-scoped diagnostic sufficiency and profile-role reuse.
- `F35` (**Task 18 empirical target**): matched structured statistic/region objective versus direct atom cross-entropy on tolerance transfer and calibrated boundaries, with no current verdict.
- `C06`: successor-state `V/Q` equality under the exact game assumptions.
- `C11`: policy-only value non-identifiability via paired counterexamples.

## Handoff to later tasks

- Tasks 2–3 produced the motivation and representation notes, adding `A08–A09` and `F21–F26`; later formalism should preserve their factorizations and information distinctions.
- Task 4 separated `E08/E11–E14`, `F11–F15`, and `F20/F27–F30`; later work must keep scientific covers, activation complexes, and selection partitions distinct.
- Task 6 verified the primary-source map; later theorem/citation tasks must still match exact hypotheses before quoting strong results.
- Task 11A selected mandatory profile-indexed `Lic_P`: `P_rely`, `P_pref-rel`, and `P_pref-cert` are named profiles, and bare `Lic` is undefined. Tasks 12–14 and the neural work must carry the profile index rather than inherit an unqualified `Lic` target label.
- Tasks 13–14 fixed and audited the compact core, proving the abstract `E18` graph theorem, the base-local `E19` profile result, and `E23`; correcting the lower-bound and projection claims in `E20/E24`; and countermodeling the unqualified strengthenings in `E21/E22/E25–E27`. Task 14B completes the bridge: it instantiates `E18` for the actual atom clauses, repairs `E19` to its instantiation fiber, concretely realizes `E20`'s seven states, and prevents its discrete-code bound from being misread as neural width.
- Task 14A proves and scopes the quantitative extension cluster `E28–E32`, and upgrades conditional composition/bridge rows `E04/E13/E14/F28`. Checkpoint B classifies it as load-bearing integration of mostly standard mathematics rather than an independent novelty headline.
- Task 14C proves the finite formal parts of `E33–E34`, supplies the path-sensitivity certificate instance and actual kernel lift, and verifies the grounded/ranked boundary. Tasks 16–17 preserve the Task 15 payload/grade/certificate encoding and prove only the proof-erased CPWL numerical map, never assigning proof status to arbitrary hidden units.
- Task 15 instantiates `F31/F34`, sharpens the fixed/expandable-library claim `F29`, supplies the finite scalar obstruction in `F32`, and adds the still-open flat-versus-DAG hypothesis `F33`. Tasks 16–17 preserve its exact metadata/checker boundaries and settle the scoped ReLU-specific positive claims.
- Task 16 instantiates the ReLU reference side of `F31`, fixes the named dual-use architecture for `F32`, supports the scoped `F05/F06/F19/F21/F23` interfaces, and falsifies ReLU-only quarantine `F18` with project impact. Task 17 proves or delimits the seven frozen representation claims, including the minimal scoped positive part of `F32`.
- Task 18 selects standardized center–radius statistic/interval training, held-out residual calibration, independent atom cross-entropy as the nonauthorizing baseline, conservative learned rejection, and separate exact-mask router ranking. It adds `F35` without an empirical verdict.
- Checkpoint C must audit this objective against the Task 15–17 interface before selecting any architecture comparator. Tasks 19–21 should test `F05`, `F07`, `F19`, `F20`, `F32`, `F33`, `F35`, and the empirical system-audit side of `E34`.
- Tasks 22–23 should narrow `C01–C11` and design the interpretability tests `G04–G06`.
- Task 31 should replace every remaining `U0/C0/L0/R0/T0` with a final scoped disposition or an explicit unresolved status.
